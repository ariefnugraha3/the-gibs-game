# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

Browser **TOP-DOWN SHOOTER** (Alien Shooter-style; **pivoted from FPS on 2026-07-11** — see the "camera is a logic pivot" rule below): "Gibran vs Robot 3D", Three.js r128. **Controls: WASD = screen-axis movement, mouse = virtual aim cursor (bullets follow it), LMB = shoot/use, RMB = move-to-cursor-point (WASD cancels), 1/2/3 = the three weapon slots, Q cycles weapons, 4 medkit, F melee, Shift dodge/evade (brief-invincible tumble). Crouch, jump, ADS (2026-07-11 pivot), sprint (replaced by the dodge 2026-07-11), R/reload (magazine system removed 2026-07-11) and the thrown grenade (replaced by the Grenade Launcher weapon 2026-07-11) do not exist.** **All user-facing UI text MUST be in English — permanent user rule (2026-07-05), no exceptions, applies to every future change.** Code comments are in Indonesian. **Two game modes** picked on the `#modeSelect` screen at load (plus an EASY/NORMAL/HARD difficulty row — `applyDifficulty` recomputes CFG from the pristine `CFG_BASE`, per-difficulty high scores): **Survival** (round-based waves — you start with **only a pistol (full ammo pool)**, wave 1 spawns 30 robots [`robotsPerWaveBase` + `Step`·(n−1)]; robots come in three **classes** — C (melee grunt), B & A (ranged shooters, from waves 3 & 6), clearing a wave shows "WAVE CLEARED" + a `shopCountdownSec` countdown, then the between-wave **Field Shop** auto-opens [a clickable mouse-driven menu — buy ammo/health refills, a **Medkit** (held, up to 2; equip with 4, then hold left-click 2 s to use for +70% HP — slowed & no sprint while using), Heal/Strengthen Monas, the **Radar** (Survival starts without it), and the Shotgun, Assault Rifle & **Grenade Launcher** — **max 3 weapons, buying a 4th prompts which to replace** — plus **per-weapon damage UPGRADES** (2026-07-12; **MERGED 2026-07-17 into ONE card per weapon** — the same card sells the weapon when unowned, then its Lv2/Lv3 upgrades once owned, Vitality-style; each level +25% of base damage; the pistol card appears only while owned [pistol is never sold]), **three SEPARATE ARMOR cards I/II/III** (2026-07-13: blocks 30/50/70% of incoming damage with durability 100/200/300 — any tier buyable directly [skipping allowed]; durability absorbs the FULL base damage of every hit and the armor SHATTERS at 0; re-buying the worn tier while damaged = full repair, an intact worn tier shows "Worn", tiers below the worn one are rejected), and character upgrades **Vitality I/II/III** (three tiers, max HP → 150/200/250, heals the increase) + **Ammo Capacity I/II/III** (three tiers, each +20% of every weapon's base ammo cap except the Grenade Launcher at +15%); click an item then its **Buy** button, then **Start Next Wave** → "Are you ready?" confirm]; **robots prioritize the Monument** — every robot attacks Monas by default and only aggros onto the player when the player is within `playerAggroMeters` (15 m) of that robot (returning to Monas past it) — and once a robot reaches Monas and starts hitting it, its aggro SHRINKS to `monasLockAggroMeters` (5 m) and a `monasLockChance` (50%) roll may LOCK it fully onto Monas (never chases the player again), `monasHp` 0 does NOT cut straight to game over — it plays a cinematic **MONAS COLLAPSE** (2026-07-13, see the Monas-collapse bullet) then "THE MONUMENT HAS FALLEN"; score = shop currency; from wave 4 on, each wave has a rising chance to trigger a thick grey Monas-fog event) and **Campaign**, a linear 4-stage mode — Stage 1 "Gedung Terbengkalai (Lantai 2)" (indoor 60×60 m grid building; **layout rebuilt 2026-07-11 to a reference floor plan**: START = descending stairs top-left [player came down from Floor 3] → narrow corridor → Office/Conference/Supply Room [ammo+medkit, top-right] → central Main Hall → Security/Break/Restroom/Storage → END = descending stairs bottom-right [down to the floor below = Stage 2]; 9 robot spots, connectivity + door-clearance BFS-verified), Stage 2 "Gedung Terbengkalai (Lantai 2)" (**rebuilt 2026-07-13 from the old ruined-highway into a second indoor building matching a reference floor plan** — same grid engine as Stage 1: START = stairs down-from-Floor-3 [top-left] → OFFICE / HALLWAY [long corridor w/ rubble] / STORAGE [archive, top row] → WAITING / OPEN OFFICE / SUPPLY ROOM [ammo+medkit, middle] + right branching corridor → LAB/RESEARCH / CONTROL / END = stairs down-to-Stage-3 [bottom-right]; 40×28 grid, 9 robot spots [~35 robots], connectivity BFS-verified; reaching the END stairs transitions to Stage 3 — always open, exactly like Stage 1's exit; NO boss [removed on user request 2026-07-13]), Stage 3 (final — **rebuilt 2026-07-13 from the night Monas park into a THIRD indoor building matching a reference floor plan**, same grid engine as Stages 1–2: a central **ATRIUM/VOID** [railing-enclosed hole, rendered as a dark pit + railing, non-walkable] ringed by a corridor loop with rooms around the perimeter — START stairs [top-left] → ARCHIVE / narrow corridor / OFFICE [top] → READING / ATRIUM / SUPPLY ROOM [middle] → CAFETERIA / ELECTRICAL / STORAGE / END stairs [bottom-right]; 42×30 grid, 10 robot spots [~39 robots], connectivity BFS-verified; reaching the END stairs → **Stage 4** [no longer the final stage]; NO boss), and Stage 4 "Jalan Menuju Alun-Alun" (final — **new 2026-07-13, an OUTDOOR level; LAYOUT REBUILT 2026-07-17 to the user's sketch: the TRAIN STATION was REMOVED and replaced by a TOWN SQUARE (alun-alun)** [not a grid — walkable = a UNION of 3 rectangles: `stage4Walk`]: START = building exit at the **PARKING LOT** [west, north of the road — shrunk again 2026-07-17 to ~60×30 m] → cross the **JALAN RAYA highway** [~500 m west→east, 2 lanes, cover: wrecked cars/bus/barriers] → **TOWN SQUARE COMPLEX** at the east end (`SQ` ~86×86 m): a central **ALUN-ALUN field (`ALUN`) fully RINGED by a 2-lane ring road** (`RING_W` = highway width; the whole SQ rect is walkable — ring vs field is visual only [asphalt ring strips + grass field + paving cross]); 13 robot spots [~44 robots] — **ALL west of the gate: robots exist only in the parking lot + highway, the alun-alun is robot-free**; a **GATE** (`S4_GATE`, cracked panel mesh + a solid `gateBlocker` at the ring mouth — enforced via `resolve`, NOT via the walk union, so nav/flood-fill stay connected) **blocks entry to the square while ANY robot lives** (approaching it shows a hint); **killing ALL robots opens the gate (`openGate`: mesh hidden + blocker spliced out; restored on `enter()`) and spawns the BOSS at the CENTER of the alun-alun (`BOSS_POS = S4_END`)**; **destroying the boss = MISSION COMPLETE directly** (`gameOver(true)` after a short `WIN_DELAY_SEC` 2.5 s explosion beat — there is NO finish trigger anymore); geometry connectivity verified. **The Stage-4 boss is a TANK (2026-07-14, `entities/tank.js`) — replaced the giant robot boss.** It's a self-contained entity (NOT a member of the `robots` array — the shared robot rig/gore/celebrate all assume a humanoid frame; the tank has its own mesh, its own player-bullet hit-test [`tankBulletHits` over `bullets`, `CFG.campaign.tank.hitRadius`], and its own death = a big explosion, no humanoid gib), driven by `stage4Scene.updateMode` (`spawnTank`/`updateTank`/`disposeTank`). **`buildTankMesh` was overhauled 2026-07-15 to a WWII German TIGER I look (visual-only, mechanics/return-contract UNCHANGED): boxy overhanging superstructure, big overlapping road wheels [spun via `wheels[]`/`spinTracks`; wheel geo baked axle→local-X and wrapped in a yaw-90° group so `rotation.x` = rolling], drive sprocket/idler, long 88 mm gun with a round double-baffle muzzle brake + a dark BORE HOLE at the muzzle (hollow-looking barrel, 2026-07-15), mantlet, commander's cupola, rear stowage bin, fenders, tools, spare track links. **REPAINTED 2026-07-16 (visual-only) to the 2045 robot-faction scheme per the art-style guide (`src/world/palette.js`): dark gunmetal/grey-olive patches matching the robot frames + RED-emissive sensor lenses matching the robot eyes — no longer WWII dunkelgelb camo (the Tiger I shape stays).** The mortar tube (rear turret) FACES FORWARD. Uses only Lambert/Basic materials (already warmed) so spawning it needs no shader recompile. The muzzle anchors [`cannonMuzzle`/`mgMuzzle`/`mortarMuzzle`] are cosmetic spawn points — projectiles still fly at the player, so moving them with the new geometry is gameplay-neutral.** **HP = `CFG.campaign.boss.hp` (SAME as the old giant robot) and score = `boss.score`.** It **spawns at the CENTER of the alun-alun (2026-07-17 — `BOSS_POS = S4_END`), rolling in from the east side of the field during the `spawnRollSec` roll; the old east-wall smash is DISABLED by passing `wallX` far WEST of home (never crossed — `bossGate`/`onWallSmash` removed; the town-square GATE opens separately via `openGate` when the robots are cleared)**, then stays put and cycles **THREE attacks in turn** (`CFG.campaign.tank`): (1) **CANNON** — turret tracks the player then fires a shell that explodes on arrival (`cannonDamage` 50, blast radius = `CFG.grenade.killRadius` × `cannonBlastRatio` 0.30); (2) **MACHINE GUN** from the hull front — a burst of `mgBurst` (10) `enemyBullets`, each aimed at the player's CURRENT position (`mgDamage` 5/shot); (3) **MORTAR** from a rear turret tube that FACES FORWARD — a **BURST of `mortarBurst` (3) BALLISTIC PARABOLA lobs `mortarBurstGapSec` (0.5 s) apart** (2026-07-16: was a single lob; the burst reuses the MG-burst pattern via `tank.mortarLeft`/`mortarTimer`, handled in `updateTank` before the `blastPending` check), each lob (gravity `mortarGravity`; horizontal speed `mortarSpeed`, flight time = distance/speed) landing on the player's position AT THAT SHOT'S FIRE TIME (so the 3 impacts CHASE a moving player; 2026-07-15: was a homing projectile — now a parabolic arc), **dodge them by moving off the impact points or a TUMBLE** (dodge i-frames whiff the blast). `blastPending` is set at burst start so the `gapSec` countdown stays frozen through the whole burst + all flights, and only starts when the LAST lob explodes (`fireMortar` bumps `pendingId` per shot → the last-fired = the max, so `detonate`'s `id === pendingId` clears it on the final impact). **The mortar projectile is a REALISTIC MORTAR SHELL (2026-07-16, `mortarShell()` — replaced the old yellow ball): an olive-drab ogive HE body [cylinder + cone nose/tail], a brass nose fuze, and a steel tail boom with 4 cross fins; nose = local +Y, and `updateMortars` orients it along its velocity so it rises tilted then noses over point-first on descent. Geometry/materials are built once (shared, lazy) and are Lambert — no shader recompile.** (`mortarDamage` 50, radius = killRadius × `mortarBlastRatio` 0.35; `mortarTurnRadPerSec` is now dormant). **Between attacks there is a `gapSec` (5 s) pause that, for the cannon/mortar, starts only AFTER its projectile EXPLODES** (the tank fires → the shell/mortar flies → detonates → THEN the 5 s begins → next attack; while the projectile is in flight `blastPending` freezes the countdown; the MG's gap starts right after its burst); a `chargeK` telegraph glows the muzzle of the NEXT attack. Cannon/mortar player damage goes through `queueBoom(...,hurtPlayer:true,...)` (so armor/god-mode/dodge-i-frames reuse `processPendingBooms`); MG uses the shared `enemyBullets`. The tank has no player collision (can be walked through — cosmetic gap, not requested). **Between campaign stages there is a dedicated SHOP SCENE bracketed by two LOADING screens (2026-07-14, overhauled same day, `campaign/transition.js`):** reaching a stage's END stairs runs `beginStageTransition(nextScene)` → **LOADING #1** → **`campaignShopScene`** (its OWN scene `id:'campaign-shop'`, not an overlay on the stage — the shop modal shows over an OPAQUE backdrop [`#shopOverlay.campaignShop`], `shopActive()` always true, all gameplay hooks no-op) → player shops → **"Start Next Stage"** → **LOADING #2** → `setScene(nextScene)`. Both loadings hold to `MIN_LOADING_MS` (900 ms) so they're consistently visible (stage 1→2's world is pre-built in `stage1.enter`, otherwise its transition would flash by). The shop is the shared Survival modal in **campaign mode** (`openShop({mode:'campaign', …})`); the catalog HIDES only the Monas items (Heal/Strengthen Monas — no Monas in campaign) and **SELLS Radar, Shotgun, Assault Rifle, Grenade Launcher, and all weapon upgrades** alongside ammo/health/medkit/armor I-III/Vitality/Ammo Capacity; currency is the run **score** (campaign accrues score from kills, persists across stages). **Campaign now starts WITHOUT the radar** (`configurePlayer` sets `hasRadar=false` for ALL modes — buy it in the shop). `isPaused` stays true through both loadings so `updateGame` never simulates; the mid-frame `setScene(campaignShopScene)` from `playerCollide` is safe because the shop scene's frame hooks are all no-ops and `updateMode`/`checkWin` are guarded. Resume into the new stage is a **click-to-continue** on the blocker (pointer-lock needs a fresh gesture, lost across the async awaits — same as the initial game start). Stage 4's finish is `gameOver(true)`, no shop. Campaign robots hold position until seen/approached — while dormant they play a **class-specific IDLE SENTINEL animation** (2026-07-14, `animateRobotIdle`): head-sensor scanning, servo body-sway + weight-shift, and a weapon-appropriate ready stance (C claw guard, B/A rifles at low-ready that snap up to aim on random alert flickers) — no longer static mannequins.

> **Desktop/Steam port:** the plan to wrap this into a Windows `.exe` (Electron) and ship on Steam/itch lives in [STEAM-DESKTOP-PLAN.md](STEAM-DESKTOP-PLAN.md).
>
> **Gameplay roadmap:** the agreed feature backlog (robot behavior variants, stage-2 boss, survival shop, music, shotgun, etc.) with per-item implementation notes, pitfalls, and status lives in [IMPROVEMENT-PLAN.md](IMPROVEMENT-PLAN.md) — read it before starting any new gameplay feature, and update its status table when you finish one.

## Architecture (refactored 2026-07-05 into ES modules)

The old single-file `index.html` was split into **ES modules under `src/`** with a **scene system**. There is still **no build step, no npm dependencies, no framework** — plain static files; Three.js r128 + post-processing examples come from CDN `<script>` tags as the global `THREE` (modules do NOT import it). `package.json` is metadata-only (`"type": "module"` for Node tooling).

**→ The complete module catalog, every export, the scene interface contract, the config key table, and the how-to-add-a-stage recipe live in [MODULES.md](MODULES.md). Read it before hunting through files.**

Load-bearing rules (details in MODULES.md):

- **Scenes:** `menu` (DOM-only) → `survivalScene` or `campaign stage1Scene → stage2Scene`, managed by `core/sceneManager.js` (`setScene`/`activeScene`). All mode-specific behavior (collision walls, ground height, bullet blockers, grenade bounds, robot movement AI, radar landmarks, HUD status, drop clamping, win check, restart policy) is behind `activeScene.*` hooks — **shared systems contain zero mode if-else**. Adding a campaign stage = one new file in `src/scenes/campaign/` + wiring per the MODULES.md recipe.
- **The campaign stage worlds coexist in one THREE.Scene** far apart (stages 1–3 are indoor buildings at x≈30000 / 60000 / 90000; stage 4 is an outdoor parking-lot→highway→station level at x≈120000 — no shared survival park); `camera.far` 4000 + robot distance culling hide the far ones. Stage 1 & Stage 2 worlds are built ONCE at campaign start by `stage1.enter` (`buildStage2World()` then `buildWorld()`); the **Stage 3 & Stage 4 worlds are built lazily (guarded) in their own `enter`** on the respective transition (each is entered once per playthrough; restart always goes to stage 1). `placeStage2Robots()` (stage 2's `placeRobots`, which also places stage-2 supplies) + stage-1 `placeRobots`/`placeSupplies` run every stage-1 enter; stage-3 & stage-4 robots+supplies are placed in their own `enter`. Campaign restart (death) always re-enters stage 1 (arrays cleared by `resetGame` first). Every campaign robot carries `z.stage`; stage transition disposes the previous stage's robots silently.
- **All gameplay-tunable numbers live in `config/gameplay.json`**, loaded into `CFG` (`core/config.js`) before the game starts. Read `CFG.x.y` inside functions only (never at module top level). New tuning values go in the JSON, not as code constants. Visual-only values (FOV, animation amplitudes, colors) intentionally stay in code. (The reload keyframe auto-scaling note is historical — reload was removed with the magazine system 2026-07-11.)
- **Cross-module state = live ESM bindings**: the owning module exports `let` + setters (`isPaused`/`score` in `core/state.js`, `stamina`/`dodgeActive`/`dodgeProgress` in `entities/player.js`, `currentWeapon`/`isAiming`/`switchAnim` in `entities/weapons.js`, `scene`/`camera`/`renderer` in `core/renderer.js`). Circular imports are fine and used (weapons↔player, robots↔effects, stage1↔stage2, game↔input) **as long as bindings are only used inside functions**.
- **`updateGame()` block order in `core/game.js` is a contract** (matches the old `update()`): scene updateMode → weapon timers → player movement → weapon state → shooting → grenades → explosions → blood → drops → bullets → robots → win check. Bullets must move before the robot sweep hit-test.
- **The old `camera` is now a LOGIC PIVOT, not the renderer (top-down pivot 2026-07-11).** Its position = the player's eye-height point (feet + `eyeHCur`, same semantics as the FPS era) and its yaw = the AIM direction toward the cursor (set every frame by `updateTopdownAim` in input.js). Every gameplay system (robot targeting/claws, bullet/grenade/melee direction via `camera.getWorldDirection`, pickups, scene collision, radar, spawn points set by scenes) still reads `camera` and is UNCHANGED. Rendering is done by **`viewCam`** (renderer.js), a separate camera at fixed offset `CAM_OFF` (−70.7, 116, 70.7 — brought closer in steps 2026-07-11 [185/100 → 161/87 → 146/79 → 130/82]; then 130/82 → 116/100 LOWERED the angle a further ~15% while KEEPING the distance ~154 units [y down, z up] so only the pitch flattens, not the zoom: ~57.8° → ~49.2°; then **2026-07-16 the AZIMUTH rotated 45° from due-south to the SOUTHWEST** — the horizontal offset was split evenly x=−z [−70.7/+70.7], keeping horizontal distance ~100 & height 116 so pitch/zoom are unchanged, only the view direction turns) — Alien Shooter oblique angle. Because the camera now looks from the southwest, **screen-up = world NORTHEAST** (no longer −z); the ground-plane **screen basis** `SCREEN_UP`/`SCREEN_LEFT` (renderer.js, derived from `CAM_OFF`) is exported so **WASD stays screen-relative** — `updatePlayerMovement`/`tryDodge` map W→`SCREEN_UP`, A→`SCREEN_LEFT` (was hardcoded W=−z/A=−x). The cursor raycast is unaffected (reads the real viewCam matrix), and the **radar was re-aligned to the SCREEN** (2026-07-16, see the HUD radar bullet — its projection frame is now `SCREEN_UP`, so radar-up = screen-up and the "N" marker points to true world-north up-and-to-the-left). `followViewCam()` tracks a **dead-zone focus point** `camFocus` (not the pivot directly): it only shifts when the pivot leaves a tolerance box of world half-extents `DEAD_X`/`DEAD_Z` (both **16** since 2026-07-16 — a SQUARE dead-zone per user request; `DEAD_X` was reduced from 46 to match `DEAD_Z`), so small movement doesn't move the camera (anti-dizzy, per user); **when the player STOPS moving the focus eases back to the pivot so the character re-centers** (2026-07-16 — `followViewCam(dt)` detects "stopped" via per-frame pivot displacement < `MOVE_EPS2`, then exponentially lerps `camFocus`→pivot at `RECENTER_RATE` 3/s; smooth, NOT a snap, to avoid dizziness); y follows without tolerance; jumps > 400 units (spawn/scene-change/restart) snap-recenter. The cursor raycast (`updateTopdownAim`) reads the actual viewCam matrix so aim stays correct with the offset. A visible **player avatar** (`entities/playerAvatar.js`, a **characterful "resistance ranger"** — **detailed overhaul 2026-07-14 to shed the placeholder look**: combat helmet with side rails, a flipped-up NVG mount, chin strap and a headset with a curved mic boom; **amber tinted goggles** + a cloth face-wrap; a signature **amber shemagh scarf**; a full chest rig [3 mag pouches, admin/radio pouches with a vertical whip **antenna**, clipped grenade, an amber name-tape], diagonal harness straps; an assault pack with side pouches, top flap, compression straps, bedroll & a long antenna; utility belt with buckle/pouches/canteen; a **right-thigh holster** with a protruding pistol; cargo-pocket trousers, contoured knee pads, gaiters and soled boots; tactical **gloves** [hand groups] and **elbow pads** on the stretched arms — the geometry is purely additive, every rig pivot/anchor [`SHOULDER`, `GUN_OFF`/`GRIPS`/`TIPS`, `armorNodes` coords, leg hip/knee pivots, `avatarGunTip`] is UNCHANGED; hip AND knee leg pivots; the stretched-segment arms; **earlier overhauls 2026-07-12**) stands at the pivot's feet and (2026-07-12, **HUMANIZED — no more whole-body cursor spin**) is split into a LOWER body (root `avatarGroup` — legs+belt, yaw = MOVEMENT direction), an UPPER body (`upperG` — torso/arms/gun/sword, local yaw = waist twist toward the cursor clamped ±`MAX_TWIST` ~60°), and a `headG` that turns FIRST (fastest rate + extra ±35°) — a natural head→torso→legs aim chain. Legs face the travel direction so the walk cycle always reads correctly; moving >~110° away from the aim = BACKPEDAL (legs stay aim-side, the gait phase runs IN REVERSE so feet genuinely step backward; hysteresis 1.75/1.92 rad), lateral movement adds a hip-abduction side-shuffle (`hip.rotation.z`, both legs opening/closing together) plus a subtle upper-body bob, and when idle the feet stay planted until the waist twist exceeds ~53° — then they SHUFFLE around to realign (turn-in-place); melee/dodge rapidly straighten the whole body toward the aim. Walk amplitude splits into along-legs and sideways components by the move-vs-legs angle. **It holds a DISTINCT prop per selection (2026-07-12):** nine prop groups — five base (rifle/pistol/shotgun/launcher/medkit) plus **four LEVEL-3 variants (2026-07-12: reaching shop upgrade Lv3 swaps the held shape — `pistol3` = big chrome DESERT EAGLE, `shotgun3` = long combat shotgun [heat-shield ribs, shell saddle], `rifle3` = six-barrel GATLING GUN [rotor housing + ammo box], `launcher3` = olive AT4-style ROCKET LAUNCHER riding ON TOP of the right shoulder, both hands reaching up to its under-grips)** — built once inside the gun group and shown per `currentWeapon`/`medkitMode`/`player.weaponLvl` (cached `propKey`; each variant has its own `GRIPS` anchors, recoil config still read from the BASE weapon). **HANDS-ON-WEAPON (2026-07-12, replaced the FK `ARM_POSE` arms):** every prop has per-prop GRIP ANCHORS (`GRIPS` — right hand at the trigger/grip, left hand on the forend/pump/tube/medkit side); the palm meshes are placed EXACTLY on the active prop's anchors each frame and the arms are drawn as two STRETCHED segments (shoulder→fake-IK elbow→hand, elbow sags with slack) — so the weapon genuinely sits in both hands instead of floating at the chest. During the dodge the hands tuck to the chest (`TUCK` anchors); during melee the right hand follows the sword grip. **Visible RECOIL (2026-07-12):** each shot pitches the active weapon PROP up briefly (`prop.rotation.x = -gunRecoil × CFG.weapons.<w>.cameraKick × 6` — `gunRecoil` is now exported from weapons.js, 1 on fire → decays dt·6); the prop rotates about its rear so gunGrp/`avatarGunTip` stay fixed (calibrated bullet spawn + muzzle flash unaffected), and the GRIP ANCHOR POINTS are rotated by the same pitch so the HANDS (and stretched arms) ride the kick with the weapon — the front hand rises most, the trigger hand barely moves. `avatarGunTip` stays at its exact calibrated offset (gunGrp 0.65/7.5/1.2, tip 0/0.15/4.5 — bullet spawn point must not move) and is the bullet spawn point + muzzle flash parent for every weapon — with ONE deliberate exception (`TIPS` map, 2026-07-12): the Lv3 `launcher3` prop moves the tip to the shoulder tube's muzzle (0.95/2.45/2.6) so the rocket + flash visibly leave the tube; the 2D/xz hit test makes the height change gameplay-neutral, and every other prop keeps the default tip. **Melee (F) shows a SWORD slash (2026-07-12; made AGILE in a 2nd pass):** while `meleeT > 0` (weapons.js; `MELEE_TIME` exported) the gun prop hides and a sword on its own SHOULDER PIVOT (`swordPivot`, blade +Z, euler order 'YXZ') plays a **3-phase slash**: ANTICIPATION (0–18%, easeOut snap to a high-right cock + body counter-twist), STRIKE (18–52%, **cubic ease-in** so the ~245° arc EXPLODES, with a forward LUNGE `translateZ`, waist twist `rotateY`, a crouch dip, and a horizontal **swoosh trail fan** — `RingGeometry` sector at chest height whose `rotation.y` trails the blade and whose opacity tracks swing speed), then RECOVERY (52–100%, smoothstep settle). The right hand tracks the rotating grip point (manual euler math) so the stretched arm follows; the left hand counter-swings; legs take a lunging stance (`meleeDip` overrides hip/knee). All body dynamics are visual-only (logic position untouched). **Gameplay (2026-07-13; AUTO-TARGET 2026-07-16): the slash is an ARC SWEEP in the ~±70° FRONT CONE toward `meleeDir` — `doMeleeHit` (weapons.js, fires at 45% of the swing) damages every robot within `CFG.melee.range` + `bodyHitRadius`×scale AND inside that cone, by `CFG.melee.damage` (150 − robot armor: one slash kills every regular class C/B/A; the 1800-HP boss is only chipped + a coolant spurt). `meleeDir` is chosen when F is pressed (`pickMeleeDir` in `tryMelee`, exported `meleeDirX`/`meleeDirZ`): the direction to the NEAREST reachable robot (within melee range) if any is adjacent, else the cursor direction. So the player NO LONGER has to aim at a robot — press F and the character AUTO-FACES the nearest stuck robot and slashes it, even if it's behind the cursor (2026-07-16 user request). The avatar's body turns to `meleeDir` during the swing (`playerAvatar` overrides `aimYaw` to `atan2(meleeDirX, meleeDirZ)` while `meleeT>0`). The hit area stays a FRONT cone (not 360°). Gore/blood fly along `meleeDir`; each sword KILL = `killRobot(cause:'melee')` → the waist-bisection gore (see the GORE bullet).** The FPS viewmodel rigs are still BUILT (reload/switch/medkit logic reads their transforms) but live inside `fpsHolder`, a permanently invisible camera child — visible=false on the PARENT is immune to the `mesh.visible=true` writes of startSwitch/applyStartLoadout. **Staged IDLE AFK animation (2026-07-14):** when the player is TOTALLY still with no threat — reset instantly by any of movement / melee / dodge / medkit / a shot (`gunRecoil`) / weapon switch / **cursor (aim) movement** / any robot `chasing`/`jumping` / pause — the avatar's `afkT` accumulates and it plays: at **30 s** turn to the camera (`viewCam`) and **WAVE** the right hand ("Hey, are you there?", gun lowered into the left hand) then return to normal; at **60 s CROUCH** (body lowers, knees bend, periodically peeking the head/torso toward the camera to "check if you're back", gun held normally); at **90 s LIE DOWN** on the back (tips back about the feet pivot, hands behind the head, the weapon laid flat on the ground beside via `gunGrpRef`). Exiting AFK restores the gun to the hands (`GUN_OFF`) and decays the head tilt; `afkDebug()` exposes `{t, mode}` for tests. These are purely visual — logic position is untouched. Frame order in `animate()`: `updateTopdownAim` → `updateGame` → `followViewCam` + `updatePlayerAvatar` → decor → render. Decor animation (`world/decor.js` registry + weapon visuals) runs every frame **even while paused**.

## Running

**A static HTTP server is now mandatory** (ES modules + `fetch` of the JSON config are blocked on `file://`; the page shows an English error explaining this if opened directly):

```
python -m http.server 8000      # then open http://localhost:8000
```

Internet required for the Three.js CDN scripts. No build, no install. For syntax checking: `node --check src/<file>.js` (works thanks to `"type": "module"`). **The live headless test suite is [tools/smoke.mjs](tools/smoke.mjs)** (`node tools/smoke.mjs`, zero deps, a growing set of numbered `test()` sections — stubbed THREE/DOM driving the real `src/` modules; there is NO single-test filter (it's a flat sequential script — run the whole suite, or temporarily comment out sections to isolate one); the `localStorage` stub is a real in-memory Map so save/checkpoint is testable; coverage list at the end of MODULES.md). **Mandatory workflow for every gameplay change: add/adjust asserts for the new mechanic → run `node tools/smoke.mjs` until all pass → `node --check` every touched file → sync CLAUDE.md + MODULES.md.** Keep assertions CONFIG-DRIVEN (read `CFG`, never hardcode tuned numbers — the user hand-tunes gameplay.json between sessions; a test failing right after a pure config retune almost always means the test hardcoded a number). A missing stub method (fakeEl/THREE) is a harness gap, not a game bug — extend the stub.

## Key mechanics and conventions

- **Collisions are distance checks, not physics.** Robot-hit, pickup, and blast use `distanceTo` radii. **Bullet-vs-robot is a swept-segment check** (`segPointDist2`, prev→current position; spawn-frame segment starts at the player's EYE so point-blank never tunnels). Survival: Monas is a solid AABB resolved PER-AXIS (`resolveMonas` in world.js — `MONAS_HALF` 22, base 44×44; hug-and-slide, NOT a full revert, so the player never sticks — mirrors the campaign wall fix), solid trees/fountain cylinders (`resolveObstacles`), fence clamp. **`bulletBlocked` follows the real Monas silhouette height-by-height** (base 44 low → thin obelisk ~16 at eye height, nothing above y 64) so bullets pass through the empty air beside the obelisk and hit robots behind — do NOT revert it to a flat 44-wide column. Campaign: walkable-union tests (`stage1Walk` grid / `highwayWalk`) with per-axis `slideWalk` (hug walls, don't stick) + rotated-AABB `resolveBlockers` (medians/wrecks/furniture; standable tops are walkable). Robot bodies are solid to the player only (push-out at `CFG.robot.bodyBlockRadius` 7.5, deliberately < claw-stop 8.0 so an attacking robot doesn't shove the player every frame). **Robots also collide with EACH OTHER (2026-07-16, `separateRobots()` in robots.js, run once per frame at the end of `updateRobots` after all movement): any two robots closer than the sum of their separation radii [`CFG.robot.separationRadius` 4.0 × scale — kept ≤ half a grid cell's usable width so two robots still fit through a 1-cell corridor; was 5.5, lowered 2026-07-16] are pushed apart along the connecting line by `CFG.robot.separationRelax` (0.5) × overlap — a soft, converging push (no jitter), NOT a hard snap; `'idle'` [dormant campaign] robots are ANCHORS (they push others but don't move, preserving placement — two idles skip each other), `'jumping'` [fence vault] robots are skipped, bigger classes keep farther apart (radius × scale). This stops robots stacking on one point, and when the player is surrounded the robots that can't fit the front ring get pushed outward and WAIT behind it (out of claw reach) instead of piling in. Each pushed robot is then re-clamped to walkable space via the **`activeScene.clampRobot(z, oldX, oldZ)`** hook (2026-07-16 bugfix — without it the push shoved robots THROUGH walls and they got stuck, because each scene's per-frame robot wall-resolve reverts to the frame's start position, which was already inside the wall; `separateRobots` snapshots each robot's valid pre-separation position and passes it as the hug-slide fallback): survival = fence + `resolveMonas` + `resolveObstacles`, campaign = `campaignClampRobot` (`stage.resolve` furniture + per-axis `stage.walkable` slide, shared in common.js).**
- **Frame-rate independence:** `dt` (clamped 0.05) and `step = dt*60`. Multiply motion by `step`; decrement timers by `dt`. Fire-rate/reload use real `Date.now()` time.
- **Entity model:** six arrays in `core/state.js` — `bullets, enemyBullets, robots, grenades, explosions, drops` — iterated backwards for splicing (`enemyBullets` = robot ranged fire → player, added 2026-07-12). `explosions` doubles as a generic visual-decay pool (ground puffs ride it). "Blood" (GREEN COOLANT since 2026-07-12 — robots bleed coolant; `Blood*` function names kept) is a **fixed pool of 72 sprites** (`effects.js`, each with a velocity so it SPRAYS + falls — `spawnBloodBurst(x,y,z,dirx,dirz,n,power,spread)`; `spread` = cone width, `6.283` = 360° for explosions), explosion flashes a **fixed pool of 3 lights** — keep point-light counts constant (no mid-game shader recompiles). New entity type = mesh + array + update block + `resetGame` cleanup.
- **GORE (`entities/gore.js`, 2026-07-11; re-themed GREEN COOLANT 2026-07-12):** robots don't vanish on death — `killRobot(i, {cause,dirx,dirz})` **splices the robot from the live `robots` array FIRST** (so the wreck is inert: no AI/claw/body-block/bullet-hit and it doesn't hold up the survival wave-clear check), awards score, then **hands the robot's own mesh to `spawnCorpse`** (a topple animation about the feet → brief lie → fade [materials flipped `transparent`, no recompile] + slight sink → dispose ~1.7 s) and flings **gibs** (`gibRobot` hides limb pivots on the wreck and throws chunks colored per that limb's armor plate — machine scrap, default tone `0x3d444c`). Robots "bleed" **GREEN COOLANT**, the player bleeds **RED** (2026-07-12): the splat sprite texture (effects.js) is NEUTRAL white and tinted per-spawn via `material.color` — default `COOLANT_HEX` green, player-damage sites pass `PLAYER_BLOOD_HEX` red through the trailing `color` param of `spawnBlood`/`spawnBloodBurst`; the ground `spawnBloodDecal` puddle texture/tint (gore.js, default `0x2fbf66`) stays green (robot-only) — the function names kept their `Blood*` names, only the theme changed. **`cause: 'explosion'` = extra-gory dismemberment** (2× omnidirectional [360°] coolant bursts ~52 sprites, the 5 limb gibs PLUS ~10 extra metal-scrap chunks, and ~9 coolant puddles SCATTERED around the blast + one per gib landing); **`cause: 'melee'` (player sword kill, 2026-07-13) = the robot is CUT IN HALF at the waist**: `bisectCorpse` (gore.js) neutralizes the inner pose then reparents every `inner` child at local y ≥ 6.3 (torso/chest/core/pack/shoulders/neck/head/both arms) into a NEW top-half group whose origin sits exactly ON the cut plane (local y 6.35 — scale-independent, works for the boss too) — the top half is FLUNG tumbling (waist-pivot world-axis quaternion spin) toward aim + blade-travel-left, DRIPPING coolant every 0.06 s until it lands (+decal), while the legs+pelvis (the ORIGINAL group) keep STANDING 0.5–0.85 s with slowly buckling knees and a GUSHING stump, then topple like a normal corpse; both cross-sections get glowing orange CUT-EDGE discs (shared `CUT_GEO` circle + per-wreck Lambert-emissive clones = the robot-visor shader program, zero recompiles), both halves fade/sink together and the robot's materials (SHARED across the halves — collected uniquely from both trees) are disposed ONCE (internal `bisected` pool, cleaned by `resetGore`); no `gibRobot` limb-hiding — both halves keep all their limbs, only 2 small scrap chips fly from the cut (killRobot); bullet = a coolant spurt + occasional single limb. **Gibs and ground decals are FIXED POOLS** (`GIB_COUNT` 64 / `DECAL_COUNT` 44) created in `initGore(scene)` and added to the scene hidden — so preload's `renderer.compile` (which traverses hidden objects) compiles their shaders → no mid-game recompile/hitch. `updateGore(dt)` runs in `updateGame` after `updateRobots`; `resetGore()` in `resetGame` disposes wrecks + hides the pools. Gib/decal geometries + the decal texture are SHARED (never disposed); wreck materials are the robot's per-instance ones (disposed with the wreck). **PLAYER DEATH SEQUENCE (2026-07-12; revised same day to a NORMAL death — user asked "jangan meledak"):** player HP 0 does NOT cut straight to the overlay — all three damage sites in robots.js (claw / enemy bullet / boom) call `startPlayerDeath(dirx, dirz)` (game.js, direction = push of the killing hit) instead of `gameOver`: the avatar stays VISIBLE and **COLLAPSES** via `playAvatarDeath(dirx,dirz)` (playerAvatar.js — easeIn 90° topple about the feet toward the fall direction, weapon/sword props vanish from the hands, waist/head untwist, limbs settle into an asymmetric limp pose, arms drape at the sides; the death branch early-returns before the aim chain), with a moderate red `spawnBloodBurst` + red `spawnBloodDecal` puddles under the body and where the head lands — **NO gibs, NO explosion**; meanwhile **ALL ROBOTS CELEBRATE (2026-07-12)**: while `isPlayerDying()` **OR the scene's `activeScene.robotsCelebrate()` hook is true (survival: while the Monas is collapsing, 2026-07-13)**, `updateRobots` replaces scene-AI/attacks/`animateRobotRig` with `celebrateRobot(z, dt)` — each robot stops attacking (player AND Monas), smoothly turns to face the corpse, then cheers "HOORAY": both arms (B/A rifles included) thrust skyward pumping, joyful hops (half the cycle airborne, half planted), chest out + head tilted up shaking happily — with per-robot RANDOM wake-up delay/tempo/hop-height/style (synchronized pump vs alternating wave) so the cheer RIPPLES through the horde like a real crowd; dormant campaign `'idle'` robots stay put and in-flight player bullets still hit (fair to the last tick); `updateGame` keeps the WORLD simulating (blood/robots/bullets) while skipping every player-control block (weapon timers/move/shoot) plus `updateMode`/`checkWin`, then fires `gameOver(false)` after `CFG.player.deathDelaySec` (2 s). A dead player takes no further hits (`player.hp > 0` guards). Monas falling still ends the game instantly. `resetGame` calls `resetAvatarDeath()` (stand back up + re-evaluate the weapon prop).
- **Robots:** procedural **MACHINE frames** (`buildRobotMesh(cls)`, **overhauled 2026-07-12 from the stylized-human zombie bodies to actual robots**): tapered-cylinder frame torso + class-colored armor chest plate, ball hip/elbow joints, piston limbs, a sensor-drum head with a **glowing emissive visor (the "eyes") + power core** — the **VISOR/eyes are RED for ALL classes** (`EYE_RED` 0xff2020, user request 2026-07-14; the antenna tip too), while the **power core** stays the class color (`CLASS_LOOK.glow`: **C green / B yellow / A red / boss dark-purple**) so class is still readable via the chest core + armor plate (`CLASS_LOOK.armor`), a power backpack, and per-arm tips — **claw hands for melee (C/boss), a right ARM CANNON + head antenna for ranged (B/A)**. Shared `RG` geometries (built once via `geo.scale()` baking) + per-instance Lambert materials (emissive visor uses the same Lambert program — no new shader); never dispose `RG`. The old profession variants/skin tones and `tintRobot`/`applyClassTint` were deleted — class color is built in (boss too). `disposeRobot` still handles array materials defensively. **The animation rig contract is UNCHANGED** — same pivot groups (`inner`, hip/knee/shoulder, `headG`) at the same positions/heights, so `animateRobotRig` is byte-identical and gore.js reads the same rig. **Robot CLASSES** (2026-07-12, `CFG.robot.classes` — REPLACED the old runner/brute/exploder variants, which were deleted): every non-boss robot is one of three classes (`z.kind` = `'C'`/`'B'`/`'A'`), each stamping `hp`/`speed`/`armor`/`attack`/`ranged`/`scale` (+ `rangeMeters`/`fireDelaySec`/`bulletSpeed` for ranged) onto the robot at spawn — **C** (HP 60, slowest, melee claw, attack 5, **GREEN** armor), **B** (HP 90, slow, **RANGED**, attack 5, range **10 m** (70 u), `fireDelaySec` 1.0 s, `bulletSpeed` 7, **YELLOW** armor), **A** (HP 120, medium speed, **RANGED**, attack 10, range **12 m** (84 u), `fireDelaySec` 1.5 s, `bulletSpeed` 5 — slower but harder-hitting shots, **RED** armor). `armor` is flat damage-reduction applied to bullet & blast damage (`max(1, dmg−armor)`; 0 for all classes now — plumbing for future). **Ranged robots STOP AND SHOOT** (2026-07-12): the scene AI (survival `robotAI` / `campaignRobotAI`) halts a ranged robot at `0.95×z.range` **from its current target — the player AND (survival) Monas** — **when grid-LOS is clear** (`aim.direct`; blocked LOS = keep pathing closer) and faces it; the shared attack branch in robots.js fires via `fireRobotBullet(z, tx?, ty?, tz?, monasDmg?)` every `fireDelaySec`, gated by `z.losOK` (stamped from `aim.direct` each frame — no shooting through walls) + `res.chaseDist < z.range`. **B carries ONE rifle in the right hand; A carries TWO rifles (both arms) and fires them ALTERNATING left/right** (`z.fireSide` toggles per shot inside the same `fireDelaySec` cadence; enemy bullets spawn from the firing barrel's muzzle — `rig.muzzle` right / `rig.muzzleL` left, never from the body). **Shooting animation (2026-07-12): ranged arms HANG at the side while walking and RAISE to aim** (`z.aiming` set by the scene AI when stopped-in-range with clear LOS; lerped via `z.aimT` in `animateRobotRig` — B raises ONLY the right arm, the left keeps swinging low; A raises both), and each shot kicks the firing barrel UP briefly (`z.recoilT`/`z.recoilSide` recoil, replacing the old claw-swipe overlay that looked like a downward scratch — `fireRobotBullet` no longer sets `clawT`). **Enemy bullets** live in the `enemyBullets` array (state.js, small BLUE plasma `MAT.enemyBullet`, scale 1.05; a 6th entity type): `updateEnemyBullets(dt, step)` (called in `updateGame` right after `updateRobots`) moves them, swept-hit-tests the PLAYER (`segPointDist2`; dodge i-frames or an already-dead player = the bullet whiffs, while **god-mode shows the full hit feedback — RED blood + flash — and only skips the HP loss** [2026-07-12 fix: the blood/flash used to sit inside the god-mode guard, so bullet hits never bled] — **a hit sprays RED player blood via `spawnBloodBurst(..., PLAYER_BLOOD_HEX)`**, also at the claw & boom damage sites; player HP 0 → `startPlayerDeath()`, never an instant overlay), and kills them on walls/Monas via `activeScene.bulletBlocked` — **a bullet carrying `monasDmg` (fired AT Monas) that dies on a wall calls the optional scene hook `enemyBulletHitMonas(dmg, pos)`** (survival: ground-puff spark + `damageMonas`). Melee class C & the **boss** (`CFG.campaign.boss`, still melee: grenades deal the reduced `boss.grenadeDamage`, tested by `z.kind === 'boss'` in effects.js) claw as before. Survival rolls the class per spawn (`classBFromWave` 3 / `classAFromWave` 6, chances rising per wave, dijepit Max); Campaign grunts are class C (`spawnCampaignRobot(x,z,stage,cls='C')`; stages 2 & 3 mix in some B/A shooters at marked spots). The `pendingBooms`/`queueBoom` blast queue now serves ONLY the Grenade Launcher round (exploder removed, so its `hurtPlayer` branch is currently dead). Scaled robots MUST get `reachMul` from `reachForScale(scl)` — their scaled body-block radius otherwise pushes the player out of their own claw reach. Rig animated by direct `rotation.x` on pivots (`animateRobotRig`). **No damage feedback by design** (player must not see how hurt a robot is). **Damage is a numeric model (rebalanced 2026-07-07):** each bullet carries its weapon's `damage` (rifle 40 / pistol 25 / shotgun 10-per-pellet, × the shop upgrade level — see Weapons), robots have real HP (per class: C 60 / B 90 / A 120), grenades deal `grenade.damage` 100, player HP is 100. **No headshots (removed with the top-down pivot 2026-07-11):** every bullet deals its flat `damage` wherever it lands. **The bullet-vs-robot hit test is HORIZONTAL (2D, xz-plane) — the y dimension is intentionally dropped** because aiming is horizontal-only and all entities stand on the ground; a 3D sphere test let short hitboxes (historically the small scale-0.75 runner variant) slip UNDER the barrel-height (~7.65) bullet path and be unhittable point-blank. The hitbox is effectively a vertical cylinder of radius `bodyHitRadius` 5 × `z.scl` (class C 1.0 / B 1.1 / A 1.2 / boss 2.4); the swept segment (prev→current, projected to xz, spawn frame from the player center) still prevents point-blank/low-fps tunneling. Survival robots jump the fence (two-phase `jumping`→`chasing`, arc reused for the fountain vault); campaign robots start `'idle'` (a class-specific **IDLE** animation — `animateRobotIdle`, 2026-07-14, reworked same day to the user's spec: **BODY & LEGS stay STILL** (all damped to neutral — no hopping/rocking); only the HEAD and ARMS move. **All classes:** the head looks left-right (celingak-celinguk, scanning surroundings, `head.rotation.y`). **Per-class gesture** (periodic, randomized timing): **C** occasionally raises & lowers both claw-hands; **B** occasionally RUBS its rifle with the free left hand (reaches across + wipes back-and-forth); **A** occasionally JUGGLES its right rifle — tosses it up spinning and catches it (the exposed `rig.gunR` gun group arcs up via `position.y` + two `rotation.x` flips, returning exactly to the hand). Shootable, solid) and wake by distance (`activateMeters`×7) + stage-1 grid LOS, or when shot; on waking, the non-idle path damps out the residual head-scan (`head.rotation.y`) + lean (`inner.rotation.z`) and snaps a mid-juggle rifle back to the hand. **Chasing uses the pathfinder** (`utils/pathfind.js`, added 2026-07-06): each scene bakes a nav-grid once (survival: Monas+trees blocked, fountain deliberately walkable so the vault still triggers; stage 1: half-cell 7-unit grid — walls from the floor-plan grid PLUS furniture/stair blockers baked via resolve, so robots route around desks instead of shoving them; stages 2 & 3: same half-cell grid engine as stage 1, furniture/void baked via resolve). `navAim` checks grid-LOS **every frame** (cheap) and returns a shared `{x, z, direct}` — direct chase the instant the player is visible, waypoint steering the instant the line is blocked (no timer lag; only `findPath` is rate-limited by `CFG.robot.repathSec`, forced when stuck > `stuckSec`); waypoints also advance early when the next one is visible (runtime string-pull). Movement runs along a turn-rate-limited heading (`turnToward`, `CFG.robot.turnRadPerSec`) so robots curve smoothly instead of snapping between waypoints. Path failure/off-grid falls back to straight chase. Robots no longer wedge against walls/trees/Monas. Claw attack (melee classes) — **0.5 s WINDUP since 2026-07-13, damage is NOT instant on touch**: reaching claw range only STARTS an anticipation (`z.windT = CFG.robot.clawWindupSec`, `z.windTarget` 'player'/'monas'; `attackCd` is set at INITIATION so the 1-per-`clawCooldownSec` cadence is unchanged, just shifted); a central windup ticker in `updateRobots` (runs BEFORE the attack branch and keeps ticking even if the robot's target flips mid-windup) resolves it: swoosh SFX + strike anim start, then the player's distance is RE-CHECKED against `clawRange + clawStrikeGrace` (× reachMul) — backing off or dodging during the windup makes the claw WHIFF — while a Monas-targeted bite lands through the optional scene hook `monasGnawHit(z)` (survival → `damageMonas`; the commit/lock roll stays at initiation). The rig plays a **3-phase attack** (`animateRobotRig`, overhauled 2026-07-13, replacing the old one-arm sine swipe): ANTICIPATION (attacking arm cocks high behind with a strain TREMOR, other arm counters low, torso twists toward the arm + leans back, crouch coil, head tucks to aim — the readable telegraph), STRIKE (cubic ease-in explosion: ~183° diagonal slash across the body, torso whips forward with an `inner.position.z` LUNGE), RECOVERY (smoothstep settle; residual twist/lunge/head-tuck decays smoothly outside attacks — walk/standing never write `inner.rotation`; the dormant `'idle'` state early-returns to `animateRobotIdle` before this claw code). damage = the class `attack` stamped as `z.clawDmg` (C 5; ranged B/A claw only exists as a fallback since they shoot instead), `clawCooldownSec` 1 per robot (boss claws 60 — the windup telegraph applies to the boss too, Monas gnaw `monasClawDamage` 20; against the player's 100 HP); attacking robots stand still (walk cycle damps out).
- **Weapons:** FOUR weapons via the `WEAPON_DEF` table in weapons.js (all per-weapon visual constants live there): `'rifle'` (displayed **"Assault Rifle"**, modeled after the Pindad SS2-V2 — all-black, carry handle with ADS sight channel, ribbed handguard, skeleton folding stock), `'pistol'`, `'shotgun'` (pump-action, box mag), and **`'launcher'`** (displayed **"Grenade Launcher"**, added 2026-07-11 — a fat 40mm break-barrel; Survival-shop-exclusive, `launcherCost`). **The Grenade Launcher fires ONE explosive round per trigger pull (slow `fireDelayMs`, small `maxAmmo` pool) that EXPLODES ON IMPACT** — the round is a bullet with `b.explosive`/`b.explodeR` (slower `roundSpeed`, no tracer stretch; **the round MESH follows the shop upgrade level (2026-07-16): Lv1-2 = the Mk2 grenade via `buildGrenadeMesh`, Lv3 = a ROCKET via `buildRocketMesh` (grenades.js — olive tube, red warhead nose, cross fins, amber exhaust flame; nose = local +Z, lookAt-oriented along flight dir at spawn; shared lazy Lambert/Basic geo/mat, warmed in preload, `userData.rocket` for tests) matching the avatar's Lv3 shoulder rocket-launcher prop — visual only, damage/AoE/speed unchanged**); it detonates on hitting a robot (`robots.js` hit loop) or a wall/Monas (`bullets.js` `hitWall`) — **never on pure life-expiry** — by QUEUEING an `explodeAt` via `queueBoom(x,y,z,r,hurtPlayer,playerDmg,dmg)` (generalized `pendingBooms` in robots.js, processed after the robot loop = no reentrant splice; launcher booms are `hurtPlayer:false` = friendly **and pass the round's `b.damage` as `dmg` — the AoE damage scales with the shop upgrade level**). Blast radius = the old grenade's (`CFG.grenade.killRadius`+3.5); a null `dmg` falls back to `grenade.damage` 100. (The exploder variant that also used `queueBoom` was removed 2026-07-12 — the launcher round is now the queue's only user.) **Model overhaul 2026-07-10 (shapes only — every muzzle point, weapon position, and ADS sight height is at its old calibrated coordinate; do NOT move them):** rifle = split upper/lower receiver + curved banana mag + ejection-port/selector/trigger/buttpad details; pistol = composite slide with serrations and the SIGHTS RIDING THE SLIDE (same absolute offsets — they move with reload pulls now), full trigger-guard loop, hammer, grip panels; shotgun = sighting rib, barrel clamp, ribbed pump, comb+recoil-pad stock. **`pistolSlide`, `pistolMagMesh`, `sgPump`, `sgMagMesh` are now THREE.Groups** (were Meshes) — the reload rigs only write `.position`/`.rotation` so the animation contract is unchanged; keep it that way. **Hands:** `wrapFinger` helper builds fingers as tangent arc segments that genuinely wrap the handguard/pump/grips. **Rifle/shotgun left hand = OVERHAND grip matching the user's CS-style reference (2026-07-10): back of the hand faces the camera on the near side, four fingers drape OVER the top of the handguard/pump from near to far, thumb tucks along the near-bottom toward the muzzle, bare-skin wrist below — don't flip it back to an under-grip.** Fingerless-glove look: `wrapFinger`'s optional `tipMat` renders the last 2 finger segments in a shared `skin` Phong material (same shader program as the glove — warm-up unaffected); pistol support hand is thumbs-forward along the frame. The left forearm placeLimb endpoints in `updateWeaponVisuals` target the overhand wrist offset (−0.45, −0.75/−0.65, +0.8 from the hand group). (Shotgun firing: `pellets: 10` bullets per trigger pull with extra `pelletSpread`; the launcher fires 1 explosive round; rifle/pistol have no `pellets` key = single bullet). **Each weapon has a `damage` in `CFG.weapons.<w>` (rifle 40 / pistol 25 / shotgun 10-per-pellet / launcher 100 AoE); the value — × the weapon's Survival-shop UPGRADE level via `weaponDamage(w)` (weapons.js, 2026-07-12: `player.weaponLvl` 1..`maxWeaponLevel` 3, each level +`upgradeDamagePct` 25% of base → Lv2 125%, Lv3 150%; levels reset per run in `configurePlayer`) — is stamped onto every bullet at spawn (`b.damage`) so the hit-test in robots.js applies the right amount even after a weapon swap (fallback `CFG.weapons.bulletDamage`); the launcher round's leveled `b.damage` also rides `queueBoom → explodeAt(pos, r, dmg)` so its blast scales too (boss `grenadeDamage` override unaffected).** **MAGAZINE SYSTEM REMOVED (2026-07-11):** each weapon is ONE ammo pool — `player[w].ammo`, capped by `CFG.weapons.<w>.maxAmmo` (**rifle 500 / pistol 150 / shotgun 300**), starts FULL, refilled by ammo drops (`+CFG.weapons.<w>.ammoPickup` per owned weapon, the old 'mag' drop type/mesh reused) and the shop's Replenish All Ammo (sets to max). There is **NO reload**: R is unbound, `startReload()` is a kept-but-no-op export, `player.isReloading` is never true (the reload keyframe rigs, reload SFX cancel plumbing, and `reloadMul`/`reloadDurMs` fields are all dormant — don't re-wire them), no auto-reload, and an empty pool gives the dry-fire click. `magSize`/`startMags`/`maxMags`/`reloadMs` were removed from gameplay.json. **Any mention of mags/magazines/reload elsewhere in this file is historical.** Different fire delay/SFX/rig per weapon. **Top-down (2026-07-11): the FPS viewmodel rigs live hidden in `fpsHolder` (logic/timers still drive them — harmless); the VISIBLE gun is the avatar's compact rifle prop, `attachMuzzle` always points `muzzlePoint` at `avatarGunTip` (muzzle flash light + a flat, upward-facing flash sprite are parented there; per-weapon `muzzleScale` still applies), bullet spread is horizontal-only, and the FPS camera recoil kick was removed (aim yaw is cursor-driven).** **BULLET RANGE = THE CURSOR (2026-07-16): every player bullet is stamped `maxDist` (horizontal pivot→`aimPoint` distance at fire time) + its firing point `sx/sz`; `updateBullets` clamps the bullet EXACTLY at that boundary (overshoot backed off along `b.dir`) and marks it `b.ended` — it is removed the NEXT frame, so the final segment still gets the robot sweep (a robot standing right at the cursor is still hittable). On the `ended` death a launcher round EXPLODES at the bullet's actual end position (`queueBoom`), while a normal round — first pellet only, flagged `b.fx` — spawns `spawnBulletFloorHit` (effects.js) **at the bullet's ACTUAL clamped end position (revised 2026-07-16: it used to spawn at the frozen cursor point `fxX/fxZ`, which visually pinned every shot to one exact spot and hid the spread — don't revert)**: a small floor impact effect (amber spark + dust ground puffs riding the explosions pool, floor height via `activeScene.groundHeight`, no new materials/lights). Bullets can no longer fly past the cursor (the clamp is by DISTANCE from the firing point, so spread scatters impacts along an arc at cursor range).** **Inventory is 3 ORDERED weapon slots (`player.weapons`, max `CFG.weapons.maxWeapons` 3, distinct types) + medkit (grenade throw replaced by the launcher weapon 2026-07-11):** key **1** = slot 0's weapon, **2** = slot 1's, **3** = slot 2's, **4** = equip MEDKIT (`medkitMode` — then HOLD left-click `CFG.player.medkitUseSec` 2 s to use it for +70% HP; releasing cancels, pressing 4 again holsters; `equipMedkit`/`finishMedkit`/`holsterMedkit` in weapons.js, channel in `updateWeaponTimers`, progress via `#medkitBar`; **channel animation (2026-07-10): the kit is a tray with a hinged lid (`mkLid`) that opens during the channel revealing a bandage roll + vial, and a second right hand (`mkWorkHand`) arrives and rhythmically presses the contents — the box dips with each press; releasing smoothly closes/retracts everything;** **finishing OR holstering the medkit RAISES the weapon via `startSwitch` (the 0.5 s switch anim — fire is blocked until it completes; NOT an instant pop);** **while `medkitMode` the player moves at `CFG.player.medkitSlowMul` 0.75 [−25%] and CANNOT sprint — enforced in player.js**; max `CFG.player.maxMedkits` 2 held), **Q** = cycle to the next filled weapon slot; 0.5 s switch (model swap at midpoint; shooting blocked while `switchAnim >= 0`). **Exiting the medkit to a weapon ALWAYS plays the raise animation via `startSwitch(target)` — even when `target === currentWeapon`. The right-panel inventory (4 tiles: 3 weapons + medkit) highlights exactly ONE slot: a weapon slot is `.active` only when NOT in `medkitMode`.** (`grenadeMode` is a dormant always-false export kept only so old `if (grenadeMode || medkitMode)` guards still read correctly.) `player.owned` is DERIVED from `player.weapons` via `syncOwnedFromWeapons()` (state.js) — the ordered array is the source of truth for slot→key mapping, `owned` for "do I have this". **Loadout per mode (`configurePlayer`): Survival starts `['pistol']` only (buy the rest in the shop); Campaign starts `['rifle','pistol']` (shotgun AND Grenade Launcher are Survival-shop-exclusive).** Starting weapon (`applyStartLoadout`/`pickStartWeapon`) = `player.weapons[0]`; `lastWeapon` = another slot (bookkeeping). `trySwitchKey` ignores empty slots. After a shop buy/replace, `refreshOwnedWeapon()` re-points `currentWeapon`/`lastWeapon` if the active weapon was swapped out. Reload duration = `reloadMs × player.reloadMul` (shop upgrade), stored in `player.reloadDurMs` which the reload keyframe rigs read — timer and animation stay in sync. Accuracy: camera-space spread cone `(spreadBase + gunHeat·spreadBloom) × acc × movePen`; `acc` = ADS 0.75 (crouch removed 2026-07-11); movePen walk 1.25 (moving) / 1 (still) — sprint removed 2026-07-11. `gunRecoil` is visual-only. Ammo drops refill all **owned** weapons (+`ammoPickup` each, capped at `maxAmmo`).
- **Player ARMOR & character upgrades (2026-07-13, Survival shop):** ALL player damage (claw / enemy bullet / boom) now routes through **`damagePlayerHp(raw)`** (robots.js): with armor worn, HP takes `raw × (1 − tier.reduce)` (`CFG.armor.tiers` — 30/50/70%) while **durability takes the FULL raw damage** (user spec: no reduction on durability); at 0 the armor **SHATTERS** — `breakPlayerArmor`: steel-plate gibs + dust puff + `sfxMelee` crunch + red "ARMOR DESTROYED!" feed, `armorLvl` back to 0 (the avatar overlay strips next frame — its toggle runs BEFORE the death branch so a killing blow that breaks armor still strips the corpse) and all three armor cards unlock again. The shop sells **THREE SEPARATE armor cards** (`armor1..3`, revised 2026-07-13): any tier buyable directly (skipping allowed), buying equips that tier at full durability (replacing the worn one), re-buying the worn tier while damaged repairs it to full, an intact worn tier is rejected ("Worn" note), and tiers below the worn one are rejected (`ownedMsg`). God-mode leaves HP AND armor untouched; dodge i-frames whiff before armor is consulted. **Avatar armor overlay** (playerAvatar.js): 3 CUMULATIVE plate sets toggled by `player.armorLvl` (cached `armorKey`) — I: chest plate + shoulder caps + thigh plates; II: + big pauldron rims, backplate, belt plate, shin guards, helmet brow; III: + neck collar, red chest stripe & pauldron trim, helmet cheek plates, red crest — leg plates are parented to the hip/knee pivots (they walk with the body), all plain Phong (no new shader, preload-warmed). **Character upgrades:** `player.maxHp` is the EFFECTIVE max HP (Vitality raises it to `CFG.player.hpUpgrades` [150/200/250] + heals the increase) — every max-HP reader (HUD bar, medkit heal %, shop Replenish Health, medkit-channel gate) uses `player.maxHp`, NOT `CFG.player.maxHp`; **`maxAmmoFor(w)`** (state.js) is the EFFECTIVE ammo cap (Ammo Capacity tiers in `CFG.weapons.ammoUpgrades`) — drops refill, shop refills/weapon buys, and the HUD "/ max" all use it. HUD: steel-blue **ARMOR bar** above HEALTH (`#armorRow`, hidden while unarmored) and the ammo module's weapon name carries the upgrade-level suffix (" II"/" III").
- **Grenades (THROWN grenade REMOVED 2026-07-11 → became the Grenade Launcher weapon; see Weapons):** the thrown-grenade system in `grenades.js` (`spawnGrenade`/`updateGrenades`, ballistic + fuse, bounce/roll) is now DEAD (never spawned; `grenades` array stays empty; `updateGrenades` no-ops; the `grenadeCollide` scene hooks + `NADE_R` are dormant) — kept only because **`buildGrenadeMesh` (the shared Mk2 mesh) is reused for the Grenade Launcher's explosive round** (and warmed in preload). `explodeAt(pos, radius?, dmg?)` in effects.js still does the AoE (default `CFG.grenade.damage`/`killRadius`; the launcher round passes its own upgrade-leveled `b.damage` through the queue's `dmg` param), driven now solely by the launcher round via the `queueBoom` queue (exploder removed 2026-07-12). `CFG.grenade.damage`/`killRadius` are LIVE; the other `grenade.*` keys (throw speeds, fuse, max) are dormant. `player.grenades` is a dead field.
- **Stamina** (`CFG.stamina`): SPRINT REMOVED 2026-07-11 — stamina is now spent only by the **dodge** (`CFG.dodge.staminaCost` 20) and **melee** (15/swing); it just regens (12/s) otherwise. Draining to 0 sets `staExhausted` (blocks dodge/melee) until it recovers to `recoverThreshold` 20 (hysteresis). `drainStamina(n)` centralizes the drain+exhaust. (`adsDrainPerSec` is dormant — ADS removed.)
- **Dodge / evade** (`CFG.dodge`, Shift, 2026-07-11): a discrete roll (input.js keydown `!e.repeat` → `tryDodge()` in player.js), NOT a hold. Direction = the WASD screen-axis vector if any is held, else BACKWARD (opposite the aim facing = "tumble ke belakang"). During the `durationSec` (0.45 s) roll the player slides along the dodge dir at `speed` 2.2 (lowered from 4.2 on 2026-07-12 — total ~30 u ≈ 4 m, a short hop, not a launch) with a linear ease-out (fast→0), **overriding WASD/click-move** (walls & robot-body pushout still resolve — you can't roll through walls), and is **fully invincible** (`dodgeInvuln` in state.js, checked at the robot claw + boom sites AND vs enemy bullets in `updateEnemyBullets` — attacks whiff cleanly, no flash/damage; god-mode behavior unchanged). A `cooldownSec` (1.0 s, measured from dodge START) gates re-dodging (so max one dodge/second — anti-spam, on top of the 20 stamina cost). Firing & melee are blocked while dodging (`dodgeActive` guard in weapons.js). **The avatar animation is a real BACKFLIP (overhauled 2026-07-12, `playerAvatar.js`):** a full 360° rotation about the horizontal axis ⟂ the roll dir whose angle is **smoothstepped** (slow lift-off/landing, fast mid-air) and whose origin swings around a **waist-height pivot** (`PIV` 5.2 — the body flips about its center of mass, feet arc up to ~2× waist height, not dragged along the floor) plus a small hop; legs/arms **TUCK** mid-air (sine envelope, released before landing). 360° means it starts & ends upright facing the cursor (no pop).
- **Movement (top-down 2026-07-11):** WASD moves along SCREEN axes (W = `SCREEN_UP`, A = `SCREEN_LEFT` — the ground-plane screen basis from renderer.js, derived from the camera azimuth; since the SW-azimuth pivot 2026-07-16 that's world-NE/world-NW, not −z/−x), one normalized vector (diagonals not faster), **DIRECTIONAL speed relative to the cursor (2026-07-12):** running TOWARD the aim cursor = full speed; strafing sideways or backpedaling away from it = ×`CFG.movement.strafeWeight`/`backpedalWeight` (both 0.5 — you can't run backward/sideways at full sprint), smoothly blended via `dirSpeedMul` (dot of move-dir vs aim-dir, so forward-diagonal ≈ 0.75; applies to BOTH WASD and click-move; the ADS multiplier stays unused); SPRINT REMOVED 2026-07-11 (Shift is now the dodge — see the Dodge bullet). **RMB = move-to-point** (`setMoveTarget` in player.js): straight-line run to the cursor's ground point with normal wall sliding, a pulsing ring marker (playerAvatar.js), canceled by WASD / arrival (<3.5 u) / no progress for 1.2 s (wall-stuck guard) / `releaseInputs`. **Using a medkit (`medkitMode`) multiplies move speed by `CFG.player.medkitSlowMul` 0.75 and blocks the dodge (`tryDodge` early-returns).** Gravity/ground-height logic remains (falls, stage2 standable tops) but **jump and crouch — the keys AND all their code (`tryJump`/`jumpVelocity`, `toggleCrouch`/`clearCrouch`/`crouchedNow`/`crouchDrop`/`crouchMultiplier`/`crouchAccuracy`) — were fully REMOVED 2026-07-11**; `eyeHCur` is constant `eyeHeight`.
- **Coordinates are large-scale**: 1 m ≈ 7 units (`CAMP_M`); eye height 11.4; park half-extents 620×340; keep new geometry proportional.
- **HUD (restyled 2026-07-10 — "angled field-instrument" look: chamfered/skewed dark panels, amber accents, wide-tracked micro-labels; JS contracts preserved):** bottom-left `#barsHud` (label+number row ABOVE each bar; **ARMOR bar on top (2026-07-13): steel-blue `#armorRow`/`#armorFill`/`#armorNum` — durability remaining, shown only while wearing armor**; HEALTH bar — always red, color CSS-only, JS writes width + `#healthNum` + a `low` class at ≤25% of the EFFECTIVE `player.maxHp` for a CSS pulse; STAMINA bar — green→orange, red = exhausted, colors written by player.js; bar shells are skewed with a tick/gloss `::after` overlay so any JS fill color works). Bottom-right `#statsHud` = `#scoreBox` (static "SCORE" label + `#scoreText` number) and the **ammo module `#ammoBox`** (`#ammoWeapon` name, big `#ammoCount`, `#ammoMags` "/ N mags", `#ammoHint` line — grenade/medkit modes show that item's count + usage hint; reloading toggles a `reloading` class = dimmed blinking count). The old single `#ammoText` element is GONE. **`#inventoryHud`** (moved into the bottom-right `#statsHud` stack 2026-07-11, above the ammo box; was a right-edge vertical panel): a horizontal row of 4 ICON tiles built by `updateInventory()` in hud.js — each tile shows a hand-drawn **SVG icon** (NOT text/emoji) from the `ITEM_ICONS` map (`pistol`/`rifle`/`shotgun` per `player.weapons[0..1]`, `grenade`, `medkit`), a small hotkey number (`.invKey` 1-4), and a count badge (`.invCount`, grenade/medkit only). The active tile (held weapon / `grenadeMode` / `medkitMode`) gets `.active` (gold, pops up), empty/0 tiles get `.dim`. Icons rewrite only when the slot's item key changes (cached `_iconKey`) to avoid re-parsing SVG on every `updateUI`. Slot refs via `invSlots` (`{row, icon, count}`) in dom.js. **To add a new item icon, add an SVG string to `ITEM_ICONS` in hud.js** (viewBox 24×24, `currentColor` fill/stroke so it inherits the tile's active/dim color). **Radar is top-left** (moved 2026-07-05): canvas-drawn flat dark bg + range rings + FOV cone + N marker + glowing blips (gradient bg & rotating sweep removed by user request 2026-07-05 — don't re-add). **SCREEN-ALIGNED, view-locked (2026-07-16, user request — replaced the 2026-07-11 NORTH-LOCK):** the map's up = the SCREEN's up, so blips line up with what's on screen. Since the SW-azimuth camera makes world-north appear skewed on screen, `drawRadar` now calls `radarProject` with the FIXED frame `fx=SCREEN_UP.x, fz=SCREEN_UP.z` (the ground-plane screen-up from renderer.js) instead of `fx=0,fz=−1`; the **N marker moves to where TRUE world-north falls on the radar** (up-and-to-the-left with the SW camera) via the same `(−fx,fz)·(R−9)` formula (SCREEN_UP is unit). The map still does NOT spin with the cursor (the frame is the FIXED camera orientation, not the aim); only the player arrow + FOV cone rotate, now by the generalized `aimAngle = atan2(dir.x·−fz + dir.z·fx, dir.x·fx + dir.z·fz)` (reduces to `atan2(dir.x,−dir.z)` for the old north frame). `radarProject(dx,dz,fx,fz,R,range)` stays generic/testable; landmarks passed `clampEdge=true` pin to the rim as direction pointers when out of range. **Gated by `player.hasRadar`** (2026-07-08): Survival starts WITHOUT the radar and buys it in the shop (`Radar` item); `drawRadar` skips and `updateUI` hides the `#radar` canvas until owned. Campaign ALSO starts without it (2026-07-14 — `configurePlayer` sets `hasRadar=false` for all modes; bought in the campaign inter-stage shop). `#waveText` top-center via `activeScene.hudStatus()`. `#pickupFeed` mid-left (`showPickup`), full-item pickups are skipped with a grey "... already full" line (1.2 s rate limit). **`#crosshair` stays permanently hidden via CSS** (user request) — the element and all JS writes to it are intact so shooting/recoil stay byte-identical; don't "clean up" and don't re-show.
- **Loading screen + renderer warm-up (2026-07-10, `core/preload.js`):** `startGame` is now **async** — after mode pick it shows the opaque `#loadingScreen` (z-40, progress bar), runs the unchanged init sequence in chunks (`await loadingStep(pct, label)` = rAF + setTimeout(0) so the browser actually PAINTS between chunks), then `warmupAll()`: one sample of every later-spawned visual (bullet tracer, world grenade, mag/medkit drops, explosion trio + ground puff, one robot, a borrowed blood sprite) parented to **`viewCam`** (guaranteed in-frustum), `renderer.compile(scene, viewCam)` (traverses hidden objects too), and 3 real composer frames — then everything is restored/disposed (warmup-created materials only, never shared GEO/MAT). The player avatar warms with the world; the hidden FPS rigs never render so they need no warming. This kills the first-equip/first-spawn hitch (Three.js compiles a material's shader only on first render; `visible=false` objects never compile). Audio is warmed too: `preloadAllSFX()` both `load()`s every clip AND **primes** each one (a volume-0 play through the real `playSFX` pool, stopped ~400 ms later) — `load()` alone doesn't force decoding or start the OS audio pipeline, and the first-ever `sfxSwitch` play (= the first grenade equip) used to hitch on exactly that. New lazily-revealed meshes or first-use materials should be added to the warm-up list in preload.js.
- **Main menu (2026-07-15, `#mainMenu` z-32, `scenes/menu.js` `initMainMenu`):** the first screen at load — **Start Game** (reveals the existing `#modeSelect` mode+difficulty screen; `#modeBack` returns here), **Settings** (`#settingsPanel` — the GRAPHICS QUALITY row `#qualityRow` was MOVED here out of the `#instructions` blocker), **Credits** (`#creditsPanel`), **Exit Game** (`exitGame()` — confirm → `window.close()` with a goodbye-screen fallback). Settings/Credits are sub-panels (`.subview`/`.open` classes); each has a Back button.
- **Campaign save / checkpoint (2026-07-15, `core/saveGame.js`):** the current campaign stage is persisted to `localStorage['gibsCampaignStage']` (`saveCampaignStage(n)`) — written at the top of EVERY campaign stage `enter()` (stages 1–4 = the start-point checkpoint), so exiting mid-campaign (pause → EXIT GAME → reload) keeps the reached stage. Picking **Campaign** from `#modeSelect` calls `loadCampaignStage()` (returns 1–4, else 0); if a saved stage **> 1** exists it shows the **`#continuePrompt`** ("Continue?") — **Yes** → `startGame('campaign', {stage:saved})`, **No** → `clearCampaignSave()` + `startGame('campaign', {stage:1})` (fresh). `startGame(mode, opts)` builds via `setScene(stage1Scene)` (which builds the stage 1 & 2 worlds) then, when `opts.stage > 1`, `campaignJumpToStage(opts.stage)` reuses the cheat-jump path to land at the checkpoint stage's START (stage 3/4 worlds build lazily in their own `enter`; warmup then compiles the target stage). **Continue = a FRESH start at that stage** (default loadout, score 0 — only the stage number is saved, not inventory/score/in-stage progress). The save is CLEARED on **MISSION COMPLETE** (`gameOver(won=true)` → `clearCampaignSave()`) and on **New Game**.
- **Game-over prompt (2026-07-15):** the `#gameOver` screen now shows two buttons (`#goButtons`, wired lazily by `wireGameOverButtons()` in game.js) instead of only "Press SPACE": **RESTART STAGE** (survival label: "RESTART") → `resetGame(true)`, and **EXIT TO MAIN MENU** → `location.reload()`. `resetGame(atCurrentStage)` — when `true` AND campaign, restarts from the **START of the stage currently being played** (`campaignJumpToStage(loadCampaignStage()||1)` — e.g. die on stage 3 → replay stage 3, NOT stage 1); when `false` (pause-menu "RESTART GAME") it keeps the old `restartScene()` policy (survival in place, campaign stage 1). SPACE on game-over now also calls `resetGame(true)`. The worlds are already built during play, so jumping straight to the checkpoint stage is safe (stage 2's robots get the `campaignJumpToStage` special placement).
- **Graphics quality**: 5 tiers in the **main-menu Settings panel** (moved out of the start blocker 2026-07-15) — `initSettingsQuality()` (menu.js) only saves the pick to localStorage `gibsQuality` + highlights (renderer doesn't exist yet); `initQualityUI` (renderer.js) reads that value and APPLIES it during `startGame`. Hidden permanently after first pointer lock. Runtime-safe knobs (pixel ratio, shadow size, bloom, composer on/off); tier 4 disables shadows (one recompile, safe because start-screen-only). Don't call `applyQuality` mid-game.
- **Rendering:** sRGB + ACES (exposure 1.1 = brightness knob) + PCFSoft shadows following the player pivot (`updateShadowFollow(camera)`); composer **RenderPass(scene, `viewCam`)** → UnrealBloom → GammaCorrection → FXAA with null-fallback if CDN fails (fallback renders `renderer.render(scene, viewCam)`). The 2026-07-10 FPS viewmodel layer/pass was REMOVED in the top-down pivot (rigs are simply hidden inside `fpsHolder`; the visible gun is the avatar's). `viewCam` is a scene member (so preload's warm group parented to it renders); stage 1's ceiling mesh is `visible=false` so the camera sees into the building over its walls (grenade ceiling-bounce physics unaffected). hard-bloom materials set `toneMapped: false`; instanced meshes set `frustumCulled = false` (r128 doesn't merge instance bounds); city/facade recipes shared in `world/facades.js`; all textures procedural canvas (`makeTexture`/`makeNormalMap` — normal maps stay linear).
- **Input safety:** `requestLock()` swallows Chrome's cooldown rejection and calls `enterImmersive()` — fullscreen + **Keyboard Lock** (`LOCK_KEYS`, deliberately excluding Escape) so Ctrl+W/Ctrl+R can't kill the tab mid-game (Chromium; `beforeunload` guard as Firefox/Safari fallback). **Pointer Lock is KEPT in top-down mode:** the mouse moves a clamped VIRTUAL cursor (`#aimCursor`, shown while locked, recentered on lock) whose ground-plane raycast (`updateTopdownAim` via `viewCam`) yields `aimPoint` + the pivot's aim yaw; `aimDist` > 120 units picks the far grenade throw. `releaseInputs()` clears keys/aim/move-target on unlock/blur/reset. **ESC pause menu (any mode, 2026-07-09, `core/pauseMenu.js`):** pointer-unlock mid-game shows `#pauseMenu` inside `#blocker` — **RESTART GAME** (`resetGame`, restart from the beginning) and **EXIT GAME** (`location.reload()` → back to the `#mainMenu` [the first screen after reload]; `startGame` is one-shot so a reload is the clean path), each behind a **Yes/No** confirm, plus a **RESUME** button (2026-07-10: the ONLY way to resume from the pause menu — `#pauseResume` → `requestLock()`; blocker-background clicks are IGNORED while the menu is open via `isPauseMenuOpen()` in input.js, the old "click anywhere to resume" hint is removed — don't re-add it). Gated by a `hasStarted` flag in input.js (set on first lock) so the initial start screen doesn't show it; NOT shown during the survival shop (`shopActive`) or game-over (`isGameOver`). Menu clicks `stopPropagation` so they don't reach the blocker. `showPauseMenu` hides `#instructions`; `hidePauseMenu` (called on resume/lock) restores it. **Cheat console (any mode, backtick `` ` ``, 2026-07-09, `core/cheatConsole.js`):** backtick toggles `#cheatConsole` (only opens while pointer-locked = active play; pauses the game via `setPaused`). The command string is built MANUALLY from keydowns (`handleKey`, routed from input.js) — NOT a focused `<input>` — so it's robust against the Keyboard Lock on gameplay keys (G/D are locked). `god-mode` + Enter TOGGLES `godMode` (state.js) → player & Monas invincible (guards: robots.js claw + enemy-bullet hit + boom-queue `if (!godMode)` — each nested inside the dodge i-frame's `if (!dodgeInvuln)`/whiff check, survival `damageMonas` early-returns); **`more-money` + Enter = `addScore(100000)` + `updateUI()`** (Survival shop currency — buy anything; repeatable); **`skip-to-wave-N` + Enter** (e.g. `skip-to-wave-30`, regex `^skip-to-wave-\d+$`, N clamped ≥1) = jump straight to wave N via the `activeScene.cheatSkipToWave(N)` hook — **Survival-only** (`survivalScene` is the only scene with that hook; other modes reply "only works in Survival mode"); the hook silently disposes on-field robots (no score/gore, like `resetGame`), calls `endEvent()`+`closeShop()`, then `startWave(N)` so the wave's spawn count/concurrent cap/interval/fog chance all follow the escalation formula for N. **`skip-to-stage-N` + Enter** (2026-07-14, regex `^skip-to-stage-\d+$`, N = 1–4) = jump straight to a campaign stage via the `activeScene.cheatSkipToStage(N)` hook → `campaignJumpToStage(N)` (`campaign/transition.js`) — **Campaign-only** (all four campaign stage scenes carry the hook; Survival/other reply "only works in Campaign mode"); it disposes ALL robots + clears the transient entity arrays (bullets/enemyBullets/grenades/explosions/drops), then `setScene(stageN)` whose `enter()` builds the world (lazy for 3/4) + positions the player + places that stage's robots — **stage 2 is special** (its robots are normally placed by `stage1.enter`, so the helper calls `placeStage2Robots()` after entering); no shop/loading, invalid N returns null ("Invalid stage"). Closed on ESC-unlock via `forceHideCheatConsole` (so it doesn't overlap the pause menu). `godMode` persists across restart (a deliberate cheat toggle).
- **SFX:** always via `playSFX` (per-clip pool of 8; **returns the played node so a long clip [reload] can be stopped**). Robot footsteps: ONE global timer plays the nearest moving chaser's step (~0.55 s, ≤120 units) — never per-robot. Weapon audio in `weapons.js`: firing picks `sfxShoot` (rifle) / `sfxPistol` / `sfxShotgun` per `currentWeapon`; `startSwitch` plays `sfxSwitch`; a dry-fire `sfxEmpty` "click" plays once per trigger-pull (gated by module `emptyReady`, re-armed on mouse-up) when the trigger is pulled with the weapon's ammo pool at 0. (Reload SFX + the `cancelReload`/`reloadSfxNode` stop-the-sound plumbing are dormant — reload was removed with the magazine system 2026-07-11.) `robot-spawn.mp3`, `shotgung-cocking.mp3`, and `assets/visuals/robot.glb` exist on disk but are unused.
- **`resetGame()`** (core/game.js) clears every entity array (incl. `enemyBullets`) + pools (+ `resetRobotsFx()` launcher-boom queue + `resetStats()`), resets player/weapons/stamina/shop-upgrades via module reset fns, then re-enters the restart scene (`activeScene.restartScene()` — campaign always stage 1). Keep it in sync when adding entity types.
- **Survival is round-based (overhauled 2026-07-07):** `wave.phase` state machine in `survival/index.js` — `'fighting'` (spawn a FIXED per-wave budget `wave.toSpawn`, capped on-field by `maxConcurrent`) → wave cleared when `toSpawn===0 && robots.length===0` → `'cleared'` (show "WAVE CLEARED" + `shopCountdownSec` countdown in `hudStatus`) → `'shopping'` (auto-`openShop()`; SPACE = `startNextWave()` → `startWave(num+1)`). Weapon inventory: `player.weapons` ordered slots (survival starts `['pistol']`, max 3; `owned` derived via `syncOwnedFromWeapons`); the Shotgun, Assault Rifle & Grenade Launcher are bought in the shop, which appends to a free slot (full loadout) or, when both slots are full, prompts a REPLACE chooser. Field shop (`survival/shop.js`, **clickable mouse-driven MODAL** — game is PAUSED and the pointer released while it's open, the world still renders behind it; auto-opens between waves): **FOUR TABS** (2026-07-15 — `.shopTabs`/`.shopTab`, `itemTab()` classifier + `tabItems`/`visibleTabs`/`firstTabId`, tested via `shopTabDebug()`; **order revised 2026-07-17: General FIRST = the default/opening tab**): **General** (Replenish Ammo/Health, Medkit, Heal Monas, Radar), **Weapons** (ONE merged card per weapon — see the weapon-card note below; the `.rowStart` Buy+Upgrade pairing was removed with the merge), **Armor** (the 3 armor tiers), **Upgrades** (Ammo Capacity + Vitality + Strengthen Monas); empty tabs (e.g. the Monas items in campaign) are hidden. **Every tab uses ONE uniform `.shopGrid` (2 columns, FIXED 42vh height + inner scroll) and the description panel (`.shopDesc`) is a FIXED 104px height (+ inner scroll), so switching tabs NEVER resizes the shop panel** (it's a fixed modal, not a reflowing web page — the armor tab's long descriptions used to grow it). **NO Buy buttons (2026-07-15):** each `.shopCard` (built by `makeCard`) shows the item name + its price/status (`.shopCardPrice`), and **LEFT-CLICK the card buys it immediately, RIGHT-CLICK cancels the purchase you just made** (`shopUndoLast()` — a one-step undo that restores a `snapshotState()` snapshot of all purchase-mutable state [player fields + weapon ammo/levels + Monas hp/max/stage via `getMonasState`/`setMonasState` in index.js] and refunds the score; NOT a general sell-back, only the immediately-previous buy; `refreshOwnedWeapon` is re-run only if the weapon slots changed); hover previews the item in the full-width **description + price** panel (`.shopDesc`) below (clicking a card only previews — the per-card Buy button is the only purchase path); bottom-left = Score, bottom-right = **Start Next Wave** button. Items (data-driven `catalog()`; **buying is via the Buy button ONLY** — clicking a list row just selects/previews it, never buys — `shopPurchase(id)` does the transaction; currency = score): Replenish All Ammo (owned weapons only), Replenish Grenades, **Replenish Health** (`healthCost`; instant full heal to 100%), **Medkit** (`medkitCost`; a HELD medkit — max `CFG.player.maxMedkits` 2, `player.medkits`; equip with key 4 then HOLD left-click `medkitUseSec` 2 s to use for +70% HP, slowed −25% & no sprint/crouch while using), **Heal Monas** (+25% of Monas max HP), **Strengthen Monas** (raises Monas max HP to the next tier in `CFG.survival.strengthenMonasStages` [3000→4500→6000, last = ceiling] + heals by the increase; scene-local `monasStage`; shows "Maxed" at the ceiling; **the item description deliberately hides the HP numbers** per user request — it only says it reinforces the Monument), **Radar** (`radarCost`; sets `player.hasRadar` → the top-left minimap; Survival starts WITHOUT it, shows "Owned" once bought), and **ONE MERGED weapon card per weapon (2026-07-17, `weaponItem(w)` — replaced the separate Buy + "Upgrade <Weapon>" cards)**: while UNOWNED the card (id = the weapon key, e.g. `'shotgun'`) sells the weapon itself (append to a free slot + full ammo; **buying while all 3 slots are full opens the REPLACE chooser** — `shopPurchase` returns `'choose-replace'`, then a click on `shopReplaceWeapon(oldW)` swaps the new weapon into that slot and deducts the score, `shopCancelReplace` aborts); once OWNED the SAME card turns into the upgrade seller (name gains the sold tier's roman numeral, e.g. "Shotgun II"), selling Level 2 then Level 3 (max → "Maxed" note): each level adds +`upgradeDamagePct` 25% of the weapon's BASE damage (`player.weaponLvl` → `weaponDamage()` in weapons.js; the launcher's blast scales too), per-tier prices in `CFG.shop.upgradeCosts[w]`, levels reset each run and persist if a weapon is replaced away then re-bought. The PISTOL is never sold (starting loadout) — its card exists only while owned (upgrade variant), and disappears if the pistol is replaced away (matching the old upgrade-card gating). Buys are rejected (score kept) when full/owned/insufficient/maxed. Clicking **Start Next Wave** (or SPACE/Enter via scene `shopKey`) first shows an **"Are you ready? Yes / No"** confirm (`requestNextWave`/`confirmNext` in shop.js); confirming (click Yes, or a second SPACE) actually starts the wave. Buying is Buy-button-only. `input.js` keys off `activeScene.shopActive()`: while the shop is open it swallows all gameplay keys and, on the deliberate pointer-unlock, pauses WITHOUT showing the pause blocker (cursor freed for the menu); `startNextWave()` closes the shop then calls `requestLock()` to re-lock + resume. `monasHp`/`monasMaxHp` are scene-local (max re-seeded from `CFG.survival.monasMaxHp` on `enter()`, so Strengthen resets each run). Ammo drops refill only `player.owned` weapons (+`ammoPickup` each, cap `maxAmmo`). **Monas HP objective (priority target, reworked 2026-07-07):** every survival robot attacks Monas by default — per-frame `atkMonas = monasHp>0 && distToEye > aggroM × CAMP_M`, where the effective aggro radius `aggroM` is normally `CFG.survival.playerAggroMeters` (15 m → 105 units) but SHRINKS once a robot commits to Monas: on its FIRST bite of Monas the robot sets `z.monasCommitted` and rolls `z.monasLocked = Math.random() < CFG.survival.monasLockChance` (0.5) ONCE — committed-but-unlocked robots use the reduced `CFG.survival.monasLockAggroMeters` (5 m → 35 units, much harder to peel off), LOCKED robots use radius 0 (never chase the player, fully committed). It only chases the player when the player is within that (possibly shrunk) aggro radius, and returns to Monas once the player leaves it (no static `z.target` / `monasAttackerChance` anymore). Monas attackers return `{}` from `robotAI` so they never attack the player; **per class (2026-07-12): melee C gnaws the AABB edge (stop 6, `monasClawDamage` per bite), ranged B/A STOP at `0.95×z.range` and SHOOT Monas' center every `fireDelaySec`** — the bullet carries `monasDmg` (= `monasClawDamage`) and on hitting the Monas silhouette triggers the scene hook `enemyBulletHitMonas` → `damageMonas` (+ a spark puff); the commit/lock roll happens on the first SHOT too. **MONAS COLLAPSE cinematic (2026-07-13):** `damageMonas` reaching `monasHp ≤ 0` does NOT call `gameOver` immediately — it sets scene flag `monasFalling`, calls `startMonasCollapse()` (world.js), and shows "THE MONUMENT IS FALLING!"; further Monas damage is ignored. The obelisk was built with its **tower pieces (foot/obelisk/cup/flame/glow/light) parented into a hinge group `monasTop`** at `MONAS_HINGE` (x 6.5, y 8 = platform surface / base edge) while the base steps stay static, so the tower can **topple about its base edge**. `updateMonasCollapse(dt)` (driven each frame by `updateMode`, which skips the wave machine while `monasFalling`) runs a 3-phase animation from `CFG.survival.monasCollapse{Tremble,Topple,Settle}Sec`: TREMBLE (rising shake of `monasTop`, dust puffs from the base via `spawnGroundPuff`, flame dims, cam shake via renderer `addCamShake`), TOPPLE (gravity-accelerated `rotation.z` to ~−(π/2+0.14), dust dragged along the falling length, then a big IMPACT near flat = dust burst + marble **debris chunks** [shared box geo + the Monas stone/marble materials, gravity+spin+bounce] + heavy cam shake + flame extinguished), SETTLE (small bounce + dust settle); it returns `true` when done → `gameOver(false, 'THE MONUMENT HAS FALLEN')`. **While the tower falls ALL robots CELEBRATE** exactly as they do when the player dies — `updateRobots` gates `celebrateRobot` on `isPlayerDying() || activeScene.robotsCelebrate()`, and the survival scene's **`robotsCelebrate()` hook returns `monasFalling`** (so robots stop attacking, turn to the player, and cheer during the collapse). During the collapse the flame is taken off decor.js (`setFlameLight(null)`/`setFlameGlow(null)`) so it can be killed without the pulse overriding. `resetMonasCollapse()` (called in `enter()`, since the world persists across runs) stands the tower back up, restores the flame to decor, and disposes the debris/geo. **Scoring** (`CFG.robot.score`, applied in `killRobot(i, opts)`): class C kill 100 (`normalKill`), ranged class A/B kill 150 (`specialKill`) (boss = `boss.score`) — no headshot bonus (headshots removed 2026-07-11); death spawns gore (see the GORE bullet above). Wave events = the **Monas-fog event ONLY** (the old random every-5-waves `scene.fog` fog + blackout events were REMOVED 2026-07-11 — only the `EVT` state machine + `applyLightPreset(scene,'outdoor')` on enter remain). **`'monasfog'` is a THICK grey POSITION-BASED fog covering the whole arena EXCEPT a clear circle of `CFG.survival.monasFogClearMeters` (15 m) around Monas** — impossible with camera-distance `scene.fog`, so it's a translucent grey **overhead canopy disk** (`world.js` `createFogCanopy` — built ONCE at world-build so preload's `renderer.compile` warms it, no hitch; a 1024² radial canvas texture with `destination-out` punching a soft transparent hole at the disk center = Monas at world origin) floating at y 26, faded by the `EVT` envelope via `setFogCanopy(k)`/`driftFogCanopy(dt)` (slow spin about Monas keeps the clear hole locked on Monas); `endEvent()` calls `setFogCanopy(0)`. **Trigger:** eligible from wave `monasFogFromWave` (4 = "after wave 3"); `startWave` rolls `chance = min(monasFogChanceMax (0.75), monasFogChanceBase (0.20) + monasFogChancePerWave (0.03)·(n − fromWave))` (capped at 75%, reached ~wave 22). Duration `monasFogDurationSec` (60 s). Damage direction indicator: one reused `#hitDir` wedge rotated by `attackerAngle()`; end-of-run stats (`stats` in state.js, per-bullet accuracy) fill `#goStats` on the game-over screen.

## Editing guidance

- **ART STYLE = "GIBS 2045" (2026-07-16, single source of truth: [src/world/palette.js](src/world/palette.js))** — theme "Jakarta 2045, mildly futuristic, NOT cyberpunk". Rules: (1) warm dusty base materials (`PAL.gunmetal/ink/steel/panel/concrete` — no pure black, no cold blue-black); (2) ONE civic-tech accent, faded TEAL `PAL.tech` (all screens/strips/touchpads) — pure cyan `0x00ffff`, magenta `0xff00ff`, and neon underglow are BANNED (`FORBIDDEN_HEX`); (3) human/player accent = AMBER `PAL.amber` (matches the HUD) + sparse red-white `PAL.hazard`/`PAL.white`; (4) environment emissiveIntensity ≤ `EMISSIVE_MAX` 0.9 (combat FX exempt); (5) gameplay signal colors are reserved (robot eye red, class armor C/B/A, coolant green, player blood red, enemy plasma blue, EXIT green) — never use them for decor. All 14 `futuristic*` props, both stage-4 car palettes, and the boss tank paint follow it; **the smoke suite enforces the no-neon + emissive-cap rules by material sweep** — new visual assets MUST use PAL tokens.
- Keep the project a **static, buildless site**: no bundlers, frameworks, or npm dependencies (package.json stays metadata-only). It must run from a plain HTTP server.
- Follow the architecture rules at the top of [MODULES.md](MODULES.md) (scene hooks instead of mode if-else; CFG for tunables; live-binding state ownership; updateGame block order) and update MODULES.md whenever modules/exports/config keys change.
- Preserve existing controls (top-down since 2026-07-11: WASD screen-axis move, mouse = aim cursor, left-click fire/throw/use, **right-click = move-to-cursor-point [WASD cancels]**, **1/2/3 = the three weapon slots, Q = cycle weapons, 4 = equip medkit [then HOLD left-click 2 s to use]**, F melee, Shift dodge/evade, Space restart / Start-Next-Wave in the survival shop) and core mechanics unless the user asks for gameplay changes. Crouch/jump/ADS/reload/sprint and the thrown grenade do not exist anymore (sprint → dodge; grenade → Grenade Launcher weapon).
- If you edit the stage-1 room layout, re-verify grid connectivity (BFS from `S1_START` over floor cells) and that no furniture blocker parks in front of a doorway.
