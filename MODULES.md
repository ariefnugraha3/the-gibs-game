# MODULES.md — Katalog Modul & Kontrak Antarmuka

> Peta lengkap setiap ES module di `src/`, ekspor pentingnya, dan kontrak antar-modul.
> **Gunakan file ini sebagai referensi — tidak perlu membuka setiap file untuk tahu apa yang tersedia di mana.**
> Konvensi: komentar kode Bahasa Indonesia, seluruh teks UI **English**. `THREE` adalah global (CDN r128, bukan import).

## Peta folder

```
index.html            DOM overlay + CDN scripts + <script type="module" src="src/main.js">
css/style.css         seluruh CSS
config/gameplay.json  SEMUA konstanta mekanik yang bisa di-tuning (lihat tabel di bawah)
src/
  main.js             entry: boot() -> menu -> startGame(mode) -> animate() loop
  core/               engine & orkestrasi (bukan gameplay spesifik mode)
  utils/              helper murni yang bisa dipakai di mana saja
  world/              elemen dunia BERSAMA antar scene (cahaya, langit, dekorasi, fasad gedung)
  entities/           sistem gameplay bersama (player, senjata, peluru, granat, robot, drop, efek)
  scenes/             satu folder/file per scene; menambah stage = tambah file di scenes/campaign/
```

## Aturan arsitektur (WAJIB dipertahankan)

0. **PIVOT TOP-DOWN (2026-07-11, game beralih dari FPS ke top-down ala Alien Shooter):** objek `camera` (core/renderer.js) = **PIVOT LOGIKA player** — posisi = titik setinggi mata (kaki + `eyeHCur`), yaw = arah BIDIK kursor (`updateTopdownAim` di input.js). SEMUA sistem gameplay tetap membaca `camera.position`/`camera.getWorldDirection` seperti era FPS — JANGAN ganti ke objek lain. Render dilakukan `viewCam` (ofset tinggi `followViewCam`); visual player = avatar `entities/playerAvatar.js` (senapannya `avatarGunTip` = titik spawn peluru); rig viewmodel FPS tersembunyi permanen dalam `fpsHolder` (weapons.js) tapi tetap dibangun karena logika reload/switch/medkit menggerakkannya. Urutan animate(): `updateTopdownAim` → `updateGame` → `followViewCam`+`updatePlayerAvatar` → decor → render(viewCam).
1. **State bersama = live binding ESM.** Modul pemilik meng-export `let` + fungsi setter; modul lain hanya membaca (mis. `isPaused` milik `core/state.js`, `stamina` milik `entities/player.js`, `currentWeapon` milik `entities/weapons.js`). Jangan menulis state modul lain secara langsung — panggil setter/fungsinya.
2. **Semua angka mekanik dari `CFG`** (`core/config.js`, dimuat dari `config/gameplay.json` SEBELUM game mulai). Baca `CFG.<seksi>.<kunci>` **di dalam fungsi**, jangan di top-level modul (config belum termuat saat modul dievaluasi). Jangan hardcode angka tuning baru — tambahkan kuncinya ke JSON.
3. **Sistem bersama tidak boleh tahu mode.** Tidak ada lagi `GAME_MODE`/`CAMP_STAGE` if-else di sistem bersama — semuanya lewat hook `activeScene.*` (kontrak scene di bawah). Kalau butuh perilaku baru per scene, tambahkan hook baru ke SEMUA scene, jangan if-else mode.
4. **Import silang antar modul boleh melingkar** (weapons↔player, robots↔effects, stage1↔stage2, game↔input) **asal** binding hanya dipakai di dalam fungsi (call-time), tidak pernah di top-level.
5. **Urutan blok `updateGame()` di `core/game.js` = kontrak** (spawner → timer senjata → gerak player → recoil → tembak → granat → ledakan → darah → drop → peluru → robot → **gore [mayat/gib/genangan]** → cek menang). Jangan diubah tanpa alasan kuat.
6. Sisa aturan lama tetap berlaku: pool tetap (3 lampu ledakan, 14 sprite darah), material bersama jangan di-dispose, iterasi array entitas mundur, kalikan gerak dgn `step` & countdown dgn `dt`, dsb (lihat CLAUDE.md).

## KONTRAK ANTARMUKA SCENE

Scene = objek polos yang didaftarkan lewat `setScene(scene, opts)` (`core/sceneManager.js`).
Sistem bersama memanggil hook ini pada `activeScene` — **scene baru wajib mengimplementasikan semuanya**:

| Hook | Wajib | Dipanggil oleh | Tugas |
|---|---|---|---|
| `id` | ya | — | string unik, mis. `'campaign-3'` |
| `enter(opts)` | ya | sceneManager | bangun dunia (guard sekali), tempatkan entitas, posisi+arah kamera, preset cahaya |
| `exit()` | opsional | sceneManager | bersih-bersih saat meninggalkan scene |
| `updateMode(dt)` | opsional | game.updateGame | logika per-frame khas scene (wave spawner survival) |
| `playerCollide(pos, oldX, oldZ, feetY)` | ya | entities/player | dinding keras + penghalang pejal + (stage 1) trigger exit |
| `groundHeight(x, z, feetY)` | ya | player & grenades | ketinggian lantai yang bisa dipijak |
| `bulletBlocked(b)` → bool | ya | entities/bullets | peluru mati kena dinding dunia (Monas / dinding grid); juga dipakai peluru MUSUH (robots.updateEnemyBullets) |
| `enemyBulletHitMonas(dmg, pos)` | opsional | entities/robots.updateEnemyBullets | peluru robot BERTARGET Monas (`b.monasDmg`) mati di dinding → scene memotong HP Monas (hanya survival yang punya) |
| `grenadeCollide(g, oldGX, oldGZ)` | ya | entities/grenades | pantulan dinding/batas + resolve pejal + (stage 1) plafon |
| `robotAI(z, dt, step)` → `{skip?}` / `{chaseDist?}` | ya | entities/robots | gerak/aktivasi robot; `skip`=lewati rig+hit test (idle jauh); `chaseDist`=jarak saat cabang kejar berjalan (syarat serang: cakar melee / tembak ranged [scene juga men-set `z.losOK` + stop di `z.range`]) |
| `clampDropPos(x, z)` → `[x,z]` | ya | entities/drops | penjepit posisi drop |
| `hudStatus()` → string | ya | core/hud.updateUI | teks `#waveText` |
| `radarLandmarks(plot)` | ya | core/hud.drawRadar | `plot(dx, dz, color, size, clampEdge?)` relatif player; `clampEdge=true` = landmark di luar jangkauan dijepit ke tepi radar sbg penunjuk arah |
| `checkWin()` | opsional | game.updateGame | panggil `gameOver(true)` bila menang / transisi stage |
| `restartScene()` → scene | opsional | game.resetGame | scene tujuan saat restart setelah mati (campaign → selalu stage 1); tanpa ini restart di scene sendiri |
| `shopKey(key)` → bool | opsional | core/input keydown | shop MODAL survival: SPACE/Enter = Start Next Wave (pembelian item lewat KLIK, bukan keyboard); return true = tombol DIKONSUMSI |
| `shopActive()` → bool | opsional | core/input keydown + pointerlockchange | predikat "shop modal terbuka": input.js menelan semua tombol gameplay & saat pointer di-unlock, mem-pause TANPA menampilkan blocker (kursor untuk menu klik) |

### Cara menambah stage campaign baru (mis. Stage 4)

1. Buat `src/scenes/campaign/stage4.js` meniru pola `stage2.js` (dunia sendiri di offset jauh, mis. `x ≈ -30000`) ATAU pola `stage3.js` (memakai ulang dunia yang sudah ada + preset cahaya beda — jauh lebih murah): konfigurasi tata letak + `buildWorld()`/`ensure…()` + `placeRobots()` (pakai `spawnCampaignRobot(x, z, 4, cls?)`) + objek scene dgn semua hook (pakai `campaignRobotAI` dari `common.js` dgn `walkable`/`resolve`/`los?`/`nav` milik stage).
2. Dunia yang dibangun sejak awal campaign → panggil dari guard `built` di `stage1.enter()`; dunia yang dibangun lazy saat pertama masuk (pola stage 3) → guard di `enter()` sendiri, dan robot ditempatkan di `enter()` (resetGame membuang semua robot lebih dulu, jadi aman).
3. Arahkan transisi: scene sebelumnya men-trigger `setScene(stage4Scene, { transition: true })` (contoh nyata: `checkWin` stage 2 bertransisi ke stage 3 setelah boss tumbang). `enter()` stage baru membersihkan robot stage sebelumnya (`z.stage === N-1`) seperti `stage2Scene.enter`/`stage3Scene.enter`.
4. Beri `restartScene: () => stage1Scene` dan `hudStatus()` dgn `countStageRobots(4)`.
5. Stage TERAKHIR yang memakai `checkWin()` → `gameOver(true)` (saat ini: stage 3).

## Katalog modul

### src/main.js
`boot()` (muat config → menu), `startGame(mode)` — **async** (2026-07-10): `showLoading()` → init DIPENGGAL dgn `await loadingStep(pct, label)` (renderer → lights → qualityUI → effects → sky | setScene | **initPlayerAvatar** (SEBELUM initWeapons — muzzle flash nempel avatar) → embers → weapons → input → grain → **followViewCam()** (matrix viewCam valid utk raycast bidik frame pertama) | `preloadAllSFX()` | `warmupAll()`) → `hideLoading()` → UI → animate; try/catch → `showFatal`. Loop `animate()` **top-down 2026-07-11**: `updateTopdownAim()` (kursor→aim, SEBELUM updateGame) → `updateGame(dt, step, T)` → `followViewCam()` + `updatePlayerAvatar(dt)` (posisi pivot terbaru; jalan juga saat pause) → shadow/decor/weaponVisuals → embers+radar (saat main) → `composer.render()` (RenderPass = viewCam) / fallback `renderer.render(scene, viewCam)`. Auto-boot kecuali `__GIBS_TEST__`.

### src/core/
- **config.js** — `CFG` (objek berisi seluruh gameplay.json), `CFG_BASE` (salinan MURNI hasil muat — tak pernah dimutasi), `CAMP_M = 7` (unit per meter), `loadConfig()` (fetch `config/gameplay.json`; test hook `__GIBS_CONFIG__`; validasi seksi — seksi baru WAJIB didaftarkan di array `SECTIONS`), `applyDifficulty(name)` (mutasi CFG dari CFG_BASE dgn pengali `CFG.difficulty[name]` — idempoten; dipanggil menu sebelum startGame; menyentuh: hp robot survival/campaign/boss [dibulatkan], clawDamage semua varian+boss+monas, boomDamage, spawnInterval survival).
- **state.js** — status inti: `isPaused, isGameOver, score, mode, highScore, difficulty, godMode, dodgeInvuln` (+ setter `setPaused/setGameOver/setScore/addScore/setMode/setHighScore/setDifficulty/setGodMode/setDodgeInvuln` — **`godMode`** = cheat kebal player & Monas (toggle via konsol cheat; dibaca robots.js damage & survival `damageMonas`); **`dodgeInvuln`** = i-frame dodge (di-set player.js sepanjang animasi tumble; dibaca robots.js di kedua titik damage); high score per difficulty: kunci `gibsHighScore_<diff>`, kunci lama fallback normal); `stats` (kills/shots/hits — per PELURU; tanpa headshot sejak top-down) + `resetStats()`; `player` (hp/grenades/**medkits**/lastShot/speed/radius/vy/onGround; senjata rifle/pistol/shotgun = **`{ ammo }` saja — SISTEM MAGAZEN DIHAPUS 2026-07-11**: satu kolam peluru per senjata, kap `CFG.weapons.<w>.maxAmmo` [rifle 500/pistol 150/shotgun 300], mulai PENUH via `configurePlayer`, TANPA reload [`isReloading/reloadTimer/reloadDurMs` tinggal field dorman]; **`weapons:[]` = SLOT senjata berurutan, maks `CFG.weapons.maxWeapons` 3 [tombol 1/2/3], `owned:{pistol,rifle,shotgun,launcher}` DITURUNKAN dari `weapons` via `syncOwnedFromWeapons()`** — Survival mulai `['pistol']`, Campaign `['rifle','pistol']` (shotgun & **launcher** eksklusif shop Survival); senjata `rifle/pistol/shotgun/launcher = {ammo}`; `hasRadar` — Survival mulai TANPA radar [dibeli di shop], mode lain true; `medkits` 0 (item genggam maks 1); field dorman `dmgMul/reloadMul/upDmg/upReload` = 1/0; **`weaponLvl:{rifle,pistol,shotgun,launcher}` (2026-07-12) = level UPGRADE senjata shop Survival, 1..`CFG.weapons.maxWeaponLevel` [3] — dibaca `weaponDamage()` weapons.js [+25% damage base per level]; per-run, bertahan bila senjata diganti lalu dibeli lagi**) + `configurePlayer()` (stempel CFG; `weapons`/`owned`/`hasRadar`/`medkits`/`weaponLvl` [semua kembali 1] di-set per `mode`) + **`syncOwnedFromWeapons()`** (rebuild `owned` dari slot `weapons`); `keys, mouse`; array entitas `bullets, enemyBullets` (peluru robot ranged → player), `robots, grenades, explosions, drops`; resource bersama `GEO, MAT` (`MAT.enemyBullet` = bola plasma BIRU peluru robot); vektor scratch `_dir,_right,_tip,_v3,_sRight,_sUp,_kickEuler`; `clearArray(arr, scene)`.
- **dom.js** — semua ref elemen (`scoreText` [ANGKA skor saja — label "SCORE" statis di HTML], **modul amunisi 2026-07-10: `ammoWeapon, ammoCount, ammoMags, ammoHint, ammoBox`** [ganti `ammoText` lama], `healthFill, healthNum, waveText, blocker, gameOverScreen, finalScoreEl, bestScoreEl, gameOverTitle, crosshair, damageEl, staminaFill, stageMsgEl, radar, radarCtx`; **`invSlots`** = 4 tile inventori `{row, icon, count}` [1/2 senjata, 3 granat, 4 medkit; hud.js isi SVG ikon + hitungan]; **`medkitBar, medkitBarFill`** = bar channel medkit), `flashDamage()`, `showHitDir(relAngle)` (baji merah tepi layar diputar ke sudut penyerang — sudut dari `attackerAngle()` robots.js; SATU elemen `#hitDir` di-reuse), `showPickup(text,color)`, `showStageMsg(text,dur)`, `hideStageMsg()`, `initGrain()`, `showFatal(html)`.
- **renderer.js** — `scene, camera, viewCam, renderer, composer, bloomPass, fxaaPass, postFxOn, qualityTier, QUALITY[]`; `initRenderer()`, `onResize()` (aspek kedua kamera), `setFxaaRes()`, `applyQuality(t)`, `initQualityUI()`, `setQualityLightRef(dirLight)`. **PIVOT TOP-DOWN (2026-07-11): `camera` = PIVOT LOGIKA player (posisi = titik mata, yaw = arah bidik kursor — di-set `updateTopdownAim`), BUKAN kamera render.** `viewCam` = kamera render top-down (FOV 50, anggota scene supaya anak warmup preload ikut dirender); `followViewCam()` mengejar titik fokus **dead-zone** `camFocus` (bukan pivot langsung): fokus hanya bergeser saat pivot keluar kotak toleransi setengah-lebar `DEAD_X`/`DEAD_Z` (gerak kecil tak menggeser kamera, anti-pusing; `DEAD_Z` utara/selatan sengaja lebih kecil agar ruang pandang depan di widescreen tak sempit; y ikut penuh; lompat >400 unit snap-recenter), lalu posisi = fokus + `CAM_OFF` (0, 116, 100 — didekatkan bertahap 2026-07-11 [185/100 → 161/87 → 146/79 → 130/82]; lalu 130/82 → 116/100 = sudut diturunkan ~15% dengan JARAK dijaga ~154 unit tetap [y turun, z naik] → pitch ~57,8° → ~49,2°), lookAt fokus (pitch ~49° ala Alien Shooter, yaw tetap menghadap −z → atas layar = −z dunia) — dipanggil animate() SETELAH updateGame + sekali di startGame. Rantai composer: RenderPass(scene, **viewCam**) → UnrealBloom → Gamma → FXAA (pass viewmodel 2026-07-10 DIHAPUS bersama pivot).
- **sceneManager.js** — `activeScene`, `setScene(scene, opts)` (exit lama → enter baru).
- **input.js** — `initInput()` (pointer lock + KURSOR BIDIK VIRTUAL top-down + klik + keyboard + blur + beforeunload). **Top-down (2026-07-11):** mouse-look DIHAPUS — delta mouse menggerakkan kursor virtual `#aimCursor` (dijepit layar, dipusatkan ulang & ditampilkan tiap pointer-lock); `updateTopdownAim()` (dipanggil animate SEBELUM updateGame): kursor → NDC → ray `viewCam` → potong bidang kaki player = ekspor `aimPoint` (Vector3 live) + set YAW pivot `camera` menghadapnya (semua sistem lama yang membaca `camera.getWorldDirection` otomatis membidik kursor). KLIK KIRI = tembak (`mouse.isDown`) / lempar granat ke kursor (jarak kursor > 120 unit → 'far', else 'near') / channel medkit; KLIK KANAN = `setMoveTarget(aimPoint)` (gerak ke titik; WASD keydown memanggil `clearMoveTarget`); ADS, jongkok (C/Ctrl), dan lompat (SPACE) DIHAPUS; **Shift = `tryDodge()` (dodge/evade, aksi diskret `!e.repeat`; SPRINT DIHAPUS 2026-07-11)** — SPACE tersisa utk restart game-over (+ shop lewat `shopKey`); `releaseInputs()` juga `clearMoveTarget()`. (Catatan lama yang masih berlaku: **tombol 1/2 = slot senjata, 3 = equip granat, Q = tukar slot senjata, 4 = `equipMedkit()`**; **mode medkit** (`medkitMode`): klik kiri ditahan = channel (jalur normal `mouse.isDown`, tembak sudah di-blok di weapons.js); **shop MODAL survival**: bila `activeScene.shopActive?.()` true, keydown menelan SEMUA tombol gameplay [hanya SPACE/Enter → `shopKey` = Start Next Wave] dan `pointerlockchange` yang unlock mem-`setPaused(true)` TANPA menampilkan blocker [pointer sengaja dilepas agar kursor bisa klik menu]; game render tetap jalan di belakang), `requestLock()` (dipanggil ulang oleh `startNextWave` untuk resume), `enterImmersive()` (fullscreen + Keyboard Lock `LOCK_KEYS`, tanpa Escape), `releaseInputs()` (TIDAK menutup shop — shop modal ditutup hanya oleh Start Next Wave / enter ulang scene). **Menu jeda ESC**: flag `hasStarted` (true sejak lock pertama) → `pointerlockchange` unlock di tengah main (bukan shop/gameover) memanggil `showPauseMenu` (`pauseMenu.js`); lock/resume memanggil `hidePauseMenu`. **Konsol cheat**: `e.code==='Backquote'` toggle `cheatConsole` (buka hanya saat pointer ter-lock); saat terbuka, tombol non-backtick di-route ke `handleKey(e)` (ketikan perintah) & unlock/ESC memanggil `forceHideCheatConsole`.
- **hud.js** — `updateUI()` (skor [angka saja] + **modul amunisi** [nama senjata/item + hitungan besar `#ammoCount` + `/ maxAmmo` (tanpa magazen 2026-07-11) + baris petunjuk `#ammoHint`; grenadeMode/medkitMode menampilkan hitungan item itu; kelas CSS `reloading` tak pernah dipakai lagi] + health bar merah [JS menulis LEBAR + angka `#healthNum` + kelas `low` saat HP ≤ 25% → pulse CSS; WARNA tetap CSS-only] + `activeScene.hudStatus()`; juga sembunyikan/tampilkan kanvas `#radar` per `player.hasRadar`; memanggil **`updateInventory()`** = baris 4 tile IKON di tumpukan `#statsHud` kanan-bawah [`invSlots` = `{row, icon, count}`]: tiap tile isi SVG dari **`ITEM_ICONS`** [pistol/rifle/shotgun per `player.weapons`, grenade, medkit — buatan sendiri, BUKAN emoji/teks; `currentColor` ikut warna tile], nomor hotkey `.invKey`, badge hitungan `.invCount` [granat/medkit]; tile aktif `.active` [senjata terpegang / grenadeMode / medkitMode] emas & menonjol, kosong/0 `.dim`; ikon ditulis ulang hanya saat key berubah [cache `_iconKey`]); `radarProject(dx, dz, fx, fz, R, range)` (proyeksi generik ke frame (fx,fz); `drawRadar` memanggilnya dgn frame UTARA-TETAP `fx=0,fz=-1` → px=timur/kanan, py=selatan/bawah); `drawRadar()` (skip bila `!player.hasRadar`; radar kiri-atas **DIKUNCI UTARA-KE-ATAS sejak 2026-07-11** [permintaan user]: puncak = utara dunia −z [= atas layar top-down], peta TIDAK berputar mengikuti kursor; penanda N tetap di puncak; hanya panah player + kerucut FOV yang diputar `aimAngle = atan2(dir.x,-dir.z)` menampilkan arah bidik; latar gelap polos + cincin jarak + silang + blip glow; latar gradien & sweep berputar DIHAPUS 2026-07-05 — jangan ditambahkan lagi; landmark `clampEdge` dijepit ke tepi; robot & drops di-skip di luar jangkauan).
- **pauseMenu.js** — menu jeda overlay di dalam `#blocker` (2026-07-09, mode apa pun): `showPauseMenu()` (tampilkan `#pauseMenu` + sembunyikan `#instructions`) / `hidePauseMenu()` (sebaliknya, dipanggil saat resume) / `isPauseMenuOpen()` (dipakai input.js: selagi menu jeda terbuka, klik latar `#blocker` DIABAIKAN — resume HANYA via tombol **RESUME** → `requestLock()`; hint "click anywhere" dihapus 2026-07-10). Dua aksi berkonfirmasi **Yes/No**: **RESTART GAME** → `resetGame` (ulang dari awal), **EXIT GAME** → `location.reload()` (kembali ke `#modeSelect`; `startGame` sekali-jalan → reload = jalan paling bersih). Klik di dalam kotak menu `stopPropagation`. `wire()` lazy sekali; elemen `#pauseMain`/`#pauseConfirm`/`#pauseConfirmText`/`#pauseResume`/`#pauseRestart`/`#pauseExit`/`#pauseYes`/`#pauseNo`. Impor sirkular pauseMenu↔input (requestLock) aman — dipakai hanya di dalam handler.
- **cheatConsole.js** — konsol cheat overlay (`#cheatConsole`, tombol `` ` ``, 2026-07-09): `openCheatConsole()` (tampil + `setPaused(true)`) / `closeCheatConsole()` (sembunyi + resume) / `forceHideCheatConsole()` (sembunyi tanpa resume — dipakai input.js saat unlock/ESC) / `isCheatConsoleOpen()` / **`handleKey(e)`** (bangun string perintah dari keydown — Enter/Backspace/karakter; dipanggil input.js selama konsol terbuka, TAK andalkan fokus `<input>` supaya kebal Keyboard Lock). Perintah **`god-mode`** + Enter = TOGGLE `godMode` (state.js) → player & Monas kebal; **`more-money`** + Enter = `addScore(100000)` + `updateUI()` (mata uang shop Survival; bisa diulang); **`skip-to-wave-N`** + Enter (regex `^skip-to-wave-\d+$`) = LOMPAT ke wave N via `activeScene.cheatSkipToWave(N)` (hanya `survivalScene` yang punya hook itu → mode lain balas "only works in Survival mode"; N di-clamp ≥ 1) — hook membuang robot lapangan (dispose senyap, tanpa skor/gore), `endEvent()`+`closeShop()`, lalu `startWave(N)` sehingga jumlah/cap/interval/peluang kabut sesuai formula wave N. Buka HANYA saat pointer ter-lock (main aktif).
- **game.js** — `updateGame(dt, step, T)` (urutan kontrak; **selama sekuens kematian player, blok kendali player [timer senjata/gerak/tembak] + `updateMode` + `checkWin` DILEWATI — dunia lain (darah/gib/robot/peluru) tetap jalan**), **`startPlayerDeath(dirx?, dirz?)`/`isPlayerDying()`** (2026-07-12, revisi "mati biasa" — dipanggil KETIGA titik damage player di robots.js menggantikan `gameOver` langsung, arah = dorongan hit pembunuh: HP=0, `releaseInputs`+`hideMoveMarker`, avatar TETAP TAMPIL dan ROBOH via `playAvatarDeath(dx,dz)` [playerAvatar.js] + satu semburan darah merah sedang + genangan decal merah `0x8f1616` di titik jatuh & arah rebah — TANPA gib/ledakan/sfx ledak; hitung mundur `CFG.player.deathDelaySec` [2 dtk] di updateGame → baru `gameOver(false)`; keruntuhan Monas TETAP mengakhiri game seketika), `gameOver(won, title?)` (English: MISSION COMPLETE / GAME OVER / judul kustom scene mis. THE MONUMENT HAS FALLEN; mengisi `#goStats` dari `stats`), `resetGame()` (reset skor+stats+player/senjata/entitas + `resetRobotsFx()` + **batalkan sekuens kematian via `resetAvatarDeath()` (bangkit dari pose roboh)** → `restartScene()` → `requestLock`).
- **preload.js** — layar loading pra-game + pemanasan renderer (2026-07-10; disesuaikan pivot top-down 2026-07-11): `showLoading()`/`hideLoading()` (`#loadingScreen`, overlay OPAK z-40), `loadingStep(pct, label)` (bar `#loadingBarFill` + `#loadingText`, lalu `await` rAF+setTimeout(0) = jamin browser MELUKIS overlay sebelum kerja sinkron berikutnya), `warmupAll()` — Three.js mengompilasi shader material hanya saat objeknya PERTAMA dirender & mengunggah tekstur saat draw pertama. Warmup: grup anak-**VIEWCAM** (pasti masuk frustum) berisi 1 contoh tiap visual spawn-nanti (tracer `GEO.bullet`, granat dunia, magazen, medkit, trio ledakan + cincin debu [material per-instance ala `explodeAt`, di-dispose setelahnya], 1 robot kelas penembak [`buildRobotMesh('B')` → `disposeRobot`; lengan meriam + visor/inti emisif = jalur Lambert-emissive ikut hangat], sprite coolant pinjaman `borrowBloodSprite`) + `renderer.compile(scene, viewCam)` + 3 frame render nyata (jalur sama dgn animate → render target bloom/FXAA ikut hangat; avatar player & dunia ikut hangat — rig FPS tersembunyi tak pernah dirender jadi tak perlu), lalu SEMUA dikembalikan (sprite ke scene, dispose hanya material buatan warmup — resource bersama JANGAN). Dipanggil startGame SEBELUM `animate()`.

### src/utils/
- **math.js** — `rand(a,b)`, `clamp(v,lo,hi)`, `smooth01(u)`, `segPointDist2(ax..pz)` (sweep peluru).
- **textures.js** — `makeTexture(w,h,draw,repX,repY)` (sRGB), `speckle(g,w,h,colors,n,sMin,sMax)`, `makeNormalMap(w,h,drawHeight,strength)` (linear), `noiseHeight(base,jitter,n,sMin,sMax)`.
- **sfx.js** — semua klip (`sfxShoot, sfxShotgun, sfxEmpty, sfxSwitch, sfxPistol, sfxExplode, sfxReload, sfxHit, sfxPickup, sfxPurchase, sfxMelee, sfxThrow, sfxNadeRoll, sfxRobotBite, sfxFootstep, sfxRobotStep`) + `playSFX(sfx, vol)` (pool 8 node per klip — selalu lewat ini; **mengembalikan node yang diputar agar klip panjang [reload] bisa di-pause saat dibatalkan**). Suara senjata: `sfxShoot` rifle / `sfxPistol` pistol / **`sfxShotgun` shotgun** (dipilih di `updateShooting`), **`sfxSwitch` saat `startSwitch`** (ganti senjata), **`sfxEmpty` klik kosong** saat pelatuk ditarik & kolam peluru senjata habis (sekali per tarikan via `emptyReady`; `sfxReload` dorman — reload dihapus 2026-07-11), **`sfxPurchase` saat sukses beli item shop** (`shopPurchase`/`shopReplaceWeapon`). **`preloadAllSFX()`** (2026-07-10) — dipanggil layar loading pra-game, DUA tahap: `load()` semua klip (fetch) + **PRIME** = putar tiap klip sekali volume 0 lewat pool `playSFX` lalu hentikan ~400 ms kemudian (decoder + perangkat audio OS + node pool hidup SAAT loading; `load()` saja tidak memaksa decode — play perdana `sfxSwitch` di equip granat pertama dulunya masih tersendat).
- **collision.js** — `slideWalk(walkableFn, pos, oldX, oldZ, r)` (menyusur dinding per-sumbu), `resolveBlockers(pos, r, feetY, blockers)` (balok rotated-AABB `{x,z,hx,hz,axx,axz,azx,azz,rad,top,standable}`; return true bila kena balok standable), `blockersGroundHeight(x, z, feetY, blockers)`, `resolveCylinders(pos, r, cylinders)`.
- **pathfind.js** — pathfinder robot (2026-07-06): `makeNavGrid(x0, z0, cell, cols, rows, sample)` (bake Uint8Array, sample dipanggil di pusat sel), `gridLOS(grid, x1, z1, x2, z2, rad?)` (garis-pandang grid setebal badan robot), `findPath(grid, sx, sz, tx, tz)` (A* 8-arah + anti potong-sudut + string-pulling; null = tak tercapai/budget habis), `navAim(z, grid, tx, tz, dt, step)` (steering per-frame; return objek BERSAMA `{x, z, direct}` — jangan disimpan. LOS dicek TIAP frame: keputusan lurus-vs-path instan tanpa jeda timer, path dibuang begitu player terlihat; hanya findPath yang di-rate-limit `repathSec` + dipaksa saat macet `stuckSec`; maju waypoint juga saat waypoint berikutnya terlihat = string-pull runtime; status di `z.nav*`), `turnToward(z, desired, dt)` (heading laju-putar `turnRadPerSec` di `z.navHead` — gerak robot memakai sudut ini agar menikung mulus). Grid null = seluruhnya nonaktif.

### src/world/ (bersama antar scene)
- **lighting.js** — `ambLight, hemiLight, dirLight, rimLight`; `createBaseLights(scene)`; `LIGHT_PRESETS` (`outdoor`/`indoor`/`night` [stage 3]) + `applyLightPreset(scene, name)` (uniform saja, tanpa recompile); `updateShadowFollow(camera)`.
- **decor.js** — registri animasi dekoratif (jalan juga saat pause): `waterJets[], fireSprites[]`, setter `setFlameLight/setFlameGlow/setBurningMat/setWaterTex/setSkyDome/setS1FlickerLight`, `updateWorldDecor(dt, T, camera)`.
- **sky.js** — `createSky(scene)` (kubah+bulan+halo, ikut player via decor), `createEmbers(scene)`, `updateEmbers(dt, T, camera)` (partikel bara/abu di KOORDINAT DUNIA absolut; dibungkus toroidal ±350 sekitar player via modulo — TIDAK menempel kaku ke player, top-down fix 2026-07-11: dulu `pts.position=camera.position` tiap frame tampak "salju" mengikuti player).
- **facades.js** — resep gedung bersama (survival city & campaign): `CITY_PALETTE`, `makeFacadeTex()`, `makeLitTex()`, `makeCityMat(facade,lit)`, `makeBurningCityMat(facade)` (auto-daftar ke decor), `fillBuildingInstances(scene, list, mat)` (list `{x,z,w,d,h,ry,rz,color}`), `addFireSprites(scene, burnList)`.

### src/entities/ (sistem bersama)
- **player.js** — live: `stamina, staExhausted, dodgeActive, dodgeProgress, dodgeDirX, dodgeDirZ`, `eyeHCur` (konstan `eyeHeight`); `drainStamina(n)` (kuras + set `staExhausted` bila 0), **`tryDodge()`** (dipanggil input.js saat tekan Shift — DODGE/evade: gate cooldown/stamina/exhausted/medkit, arah = WASD bila ditekan else MUNDUR lawan hadap bidik, set i-frame `setDodgeInvuln(true)`), `setMoveTarget(x, z)`/`clearMoveTarget()` (**gerak klik-kanan top-down**: lurus ke titik + marker cincin playerAvatar; batal oleh WASD / tiba <3.5 u / macet 1.2 dtk / releaseInputs), `resetPlayerState()`, `updatePlayerMovement(dt, step)` (**top-down 2026-07-11: WASD = sumbu layar [W=−z, A=−x]; KECEPATAN DIREKSIONAL 2026-07-12: `dirSpeedMul` — penuh searah kursor, ×strafe/backpedalWeight (0.5) menyamping/mundur, blend halus via dot; berlaku utk WASD & klik-kanan; SPRINT DIHAPUS 2026-07-11**; **saat `dodgeActive`: guling ke `dodgeDir` speed meluruh, override WASD/klik-kanan, dodgeProgress 0..1 + i-frame mati di ujung**; stamina hanya regen+pulih, bar, dorongan badan robot ZBODY, `activeScene.playerCollide/groundHeight` pada POSISI PIVOT `camera`, gravitasi + jatuh dari tepian [**lompat & jongkok DIHAPUS 2026-07-11**], langkah kaki + langkah robot global; **saat `medkitMode`: gerak ×`CFG.player.medkitSlowMul`, dodge diblok**).
- **playerAvatar.js** — **OVERHAUL 2026-07-12**: `initPlayerAvatar(scene)` (tentara stylized DETAIL: helm kubah + pinggiran, rompi taktis + kantong dada + sabuk, ransel + gulungan matras, pelindung lutut; **BADAN TERBELAH DUA (2026-07-12): root `avatarGroup` = badan BAWAH [kaki+sabuk; yaw = arah GERAK] + `upperG` = badan ATAS [torso/lengan/senjata/pedang/swoosh; yaw lokal = puntiran pinggang ke kursor, dijepit ±`MAX_TWIST` 1.05 rad] + `headG` di dalamnya [toleh kepala ekstra ±`HEAD_TWIST` 0.62, menoleh LEBIH DULU] — konstanta koordinat lama tetap berlaku karena upperG/headG di origin root**; kaki ber-pivot pinggul+LUTUT [`hipL/R`, `kneeL/R`], lengan ber-pivot bahu+SIKU [`armL/R`, `elbowL/R`]; **`avatarGunTip` tetap di ofset kalibrasi — gunGrp (0.65,7.5,1.2), tip (0,0.15,4.5) — titik spawn peluru tak boleh bergeser**; dipanggil startGame SEBELUM `initWeapons` karena muzzle flash di-parent ke **`avatarGunTip`**). **PROP PER-SLOT (2026-07-12): lima grup prop berbeda — rifle (receiver+laras+magasin+popor), pistol (kecil, dipegang menjulur), shotgun (laras+POMPA+popor kayu), launcher (tabung 40mm GENDUT), medkit (kotak putih palang merah, dipegang rendah)** — dibangun sekali di gunGrp, ditampilkan per `currentWeapon`/`medkitMode` (cache `propKey`). **TANGAN-DI-SENJATA (2026-07-12, menggantikan lengan FK `ARM_POSE`): tiap prop punya ANCHOR GENGGAM (`GRIPS` — kanan di pelatuk/grip, kiri di forend/pump/tabung/sisi medkit); telapak DIPOSISIKAN TEPAT di anchor prop aktif tiap frame + lengan digambar 2 SEGMEN TERTARIK (`placeArm`/`placeSeg`: bahu→siku fake-IK [melorot sesuai kekenduran]→telapak) — senjata benar-benar dipegang, tak melayang di dada; dodge = tangan TUCK ke dada, melee = tangan kanan mengikuti gagang pedang; **RECOIL: prop aktif di-pitch naik `-gunRecoil×cameraKick×6` di pangkalnya (gunGrp/avatarGunTip TIDAK ikut — kalibrasi aman) + titik genggam DIPUTAR pitch yang sama → TANGAN & lengan ikut hentakan (tangan depan naik paling terasa)**. `updatePlayerAvatar(dt)` (dipanggil animate SETELAH updateGame: berdiri di kaki pivot; **RANTAI HADAP MANUSIAWI (2026-07-12, menggantikan `lookAt` seluruh badan): kaki menghadap ARAH GERAK [rate-limited `approachAngle`], puntiran pinggang `upperG` menutup sisa sudut ke kursor [±MAX_TWIST], kepala menoleh PALING DULU [rate tercepat + toleh ekstra]; gerak menjauhi bidikan >~110° = BACKPEDAL [kaki tetap di sisi bidikan, fase gait DIPUTAR MUNDUR = benar melangkah mundur, histeresis 1.75/1.92 rad]; diam + puntiran >0.92 rad = kaki MENYERET menyesuaikan hadap (turn-in-place ber-shuffle); GAIT TERARAH: komponen gerak sejajar-kaki = ayunan pinggul X, komponen menyamping = kedua pinggul buka-tutup `rotation.z` [side-shuffle nyata] + bob halus `upperG.position.y`; melee/dodge = seluruh badan cepat lurus ke bidikan**; ayunan kaki menurut kecepatan horizontal NYATA; **SALTO dodge 2026-07-12: rotasi 360° smoothstep di sekitar pivot PINGGANG [`PIV` 5.2 — origin kaki diayun mengelilingi pusat massa] + busur lompatan kecil + TUCK kaki/lengan di udara [envelope sinus, dilepas sebelum mendarat]**; **SABETAN PEDANG melee 2026-07-12 (dilincahkan): selama `meleeT > 0` [weapons.js, + `MELEE_TIME` diekspor] prop senjata disembunyikan & `swordPivot` [bahu kanan, bilah +Z, euler 'YXZ'] memainkan 3 FASE — ancang easeOut (badan memuntir kanan) → TEBAS easeIn-kubik busur ~245° (TERJANGAN maju `translateZ` + puntiran pinggang `rotateY` + merendah + kipas JEJAK swoosh `RingGeometry` yang membuntuti bilah, opacity ~ kecepatan) → recovery smoothstep; TANGAN KANAN mengikuti gagang (euler manual), kiri mengayun berlawanan, kaki kuda-kuda (`meleeDip`); dinamika badan visual-only**), `showMoveMarker(x, y, z)`/`hideMoveMarker()` (cincin denyut klik-kanan; dipakai player.js), **`playAvatarDeath(dirx, dirz)`/`resetAvatarDeath()`** (2026-07-12 — animasi MATI "biasa" dipicu `startPlayerDeath` game.js: tubuh ROBOH 90° easeIn di pivot kaki ke arah dorongan hit, prop senjata/pedang lenyap dari tangan, pinggang/kepala mengendur + kaki menekuk lemas asimetris + lengan terkulai di sisi badan [cabang mati early-return sebelum rantai hadap]; reset = bangkit + prop dievaluasi ulang). Material Phong polos = tanpa program shader baru. Impor sirkular playerAvatar↔player/input/**weapons** (baca `currentWeapon`/`medkitMode`) aman (dibaca di dalam fungsi).
- **weapons.js** — live: `currentWeapon, isAiming, switchAnim, meleeT, gunRecoil` (1 saat menembak → meluruh dt·6; dibaca playerAvatar utk hentakan laras naik) (+ konstanta **`MELEE_TIME`** 0.45 — dibaca playerAvatar utk animasi sabetan pedang)`, grenadeMode` (**DORMAN, selalu false sejak 2026-07-11 — granat lempar diganti weapon Grenade Launcher; disimpan agar guard lama `if (grenadeMode||medkitMode)` tetap benar**)`, medkitMode`; **`WEAPON_DEF`** (tabel definisi **EMPAT** senjata — rifle/pistol/shotgun/**launcher**: name/hipX/hipY/adsY/baseZ/kick/muzzle/muzzleScale/kfBase + ref mesh & muzzlePoint diisi initWeapons; senjata baru = tambah baris + model + WEAPON_DEF.<w>.mesh/muzzlePoint); `initWeapons()` (model+tangan, parent kamera, `scene.add(camera)`; rifle = Pindad SS2-V2; shotgun = pump-action; **launcher = laras gemuk 40mm + receiver + grip + popor** [model sederhana, rig FPS tersembunyi]), `attachMuzzle(wpn)` (def-driven), `startSwitch(t)`, **`trySwitchKey(k)`** (1/2/3 = slot senjata 0/1/2 [`player.weapons`, maks 3], **Q = putar ke slot terisi berikutnya**; slot kosong diabaikan; **dari medkit ke senjata SELALU via animasi angkat `startSwitch`**), **`refreshOwnedWeapon()`** (setelah beli/ganti di shop: pastikan `currentWeapon`/`lastWeapon` masih dimiliki), **`equipMedkit()`** (mode medkit tombol 4: pegang medkit [`medkitHandMesh`], tekan lagi = holster; TAHAN klik kiri `CFG.player.medkitUseSec` detik = channel -> `finishMedkit` sembuh 70% + `holsterMedkit`; lepas klik = batal; **holster/finish MENGANGKAT senjata via `startSwitch`**), `pickStartWeapon()/applyStartLoadout()` (senjata aktif awal = `player.weapons[0]`; dipakai initWeapons + resetWeapons), **`startReload()` = NO-OP (magazen+reload DIHAPUS 2026-07-11)**, `tryMelee()`, `toggleAim()` (dorman — ADS dihapus), `setAiming(v)`, `doMeleeHit()`, `updateWeaponTimers(dt)` (switch swap di tengah + **channel medkit**), `updateWeaponState(dt)`, `updateShooting()` (spread horizontal + spawn peluru; **damage per peluru distempel `weaponDamage(w)`** — export 2026-07-12: damage base CFG × level upgrade shop [`player.weaponLvl`, tiap level +`upgradeDamagePct` 25% dari base]; `pellets` [shotgun 10]; **`launcher` = 1 peluru MELEDAK-saat-kena: mesh Mk2 `buildGrenadeMesh(0.7)`, `roundSpeed`, flag `explosive`/`explodeR`**; `stats.shots` per peluru; segmen frame-1 dari MATA; **kolam peluru 0 -> klik-kosong `sfxEmpty`**), `updateWeaponVisuals(dt)` (ADS/FOV/bob; 3 cabang rig reload dorman; melee; **rig tangan medkit + bar `#medkitBar`**), `resetWeapons()`, `placeLimb(...)`. **TOP-DOWN (2026-07-11):** semua rig viewmodel di-parent ke **`fpsHolder`** (anak `camera` pivot, `visible=false` PERMANEN — kebal tulisan `mesh.visible=true` startSwitch/applyStartLoadout pada anak); `attachMuzzle(wpn)` selalu menunjuk `muzzlePoint` ke **`avatarGunTip`** (playerAvatar.js — kilat muzzle PointLight + sprite kilat REBAH menghadap atas permanen di sana; hanya `muzzleScale` per senjata yang diterapkan); sebar peluru HORIZONTAL saja (komponen vertikal dihapus — kipas pelet shotgun menyamping); tendangan kamera (`_kickEuler`) DIHAPUS; `setAllRigsVisible` DIHAPUS (rig tak pernah dirender). **Overhaul bentuk model 2026-07-10 (visual saja — SEMUA titik muzzle, posisi senjata, dan tinggi pisir ADS tetap di koordinat kalibrasi lama):** rifle = receiver atas/bawah terpisah + magazen KURVA + port ejeksi/selektor/pelatuk/bantalan popor; pistol = slide komposit (serrasi, port, PISIR IKUT SLIDE di ofset absolut sama), lingkar pelindung pelatuk utuh, hammer, panel grip; shotgun = rib bidik, klem laras, pump berrusuk, popor komb + bantalan. **`pistolSlide`/`pistolMagMesh`/`sgPump`/`sgMagMesh` kini THREE.Group** (rig hanya menulis `.position`/`.rotation` — kontrak animasi tak berubah, pertahankan). **Helper `wrapFinger`** (dalam initWeapons): jari = rangkaian ruas kotak tangensial busur yang benar-benar membungkus handguard/pump/grip; param opsional `tipMat` = material 2 ruas ujung (kulit — sarung tangan tanpa jari). **Tangan kiri rifle/shotgun = grip OVERHAND mengikuti referensi CS dari user (2026-07-10): punggung tangan menghadap kamera di sisi dekat, empat jari MENYAMPIR DI ATAS handguard/pump dekat→jauh, ibu jari menyelip sisi dekat-bawah ke arah muzzle, pergelangan kulit telanjang — jangan dikembalikan ke genggaman bawah.** Ujung placeLimb lengan kiri di updateWeaponVisuals mengikuti ofset pergelangan overhand (−0.45, −0.75/−0.65, +0.8 dari grup tangan); support hand pistol thumbs-forward. **Rig medkit**: baki + tutup berengsel `mkLid` (terbuka saat channel, isi: gulungan perban + vial merah dgn `MEDKIT_MAT` bersama) + tangan kanan penekan `mkWorkHand` yang memompa berirama selama channel (kotak ikut mengayun); di-reset saat `equipMedkit`.
- **bullets.js** — `updateBullets(step)` (maju, catat px/py/pz sweep, mati umur/`activeScene.bulletBlocked`; **peluru Grenade Launcher [`b.explosive`] yang menghantam dinding/Monas [`hitWall`] → `queueBoom(...,b.explodeR,false,0,b.damage)` [damage ber-level upgrade ikut ke boom] sebelum dibuang [ledakan diproses setelah loop robot]; habis-umur di ruang kosong hanya lenyap**). Impor `queueBoom` dari robots.js.
- **grenades.js** — `NADE_R`, `buildGrenadeMesh(scale)` (Mk2; bahan bersama — JANGAN dispose; **kini dipakai ulang sbg peluru Grenade Launcher**). **`spawnGrenade`/`updateGrenades`/`nudgeGrenade` = DORMAN sejak 2026-07-11** (granat lempar dihapus; `grenades` array selalu kosong, `updateGrenades` no-op, `grenadeCollide` scene hook tak terpanggil) — dipertahankan karena `buildGrenadeMesh` + `explodeAt` masih hidup.
- **robots.js** — `CLAW_TIME`, `ellipGeo(r,sx,sy,sz,ws,hs)` (elipsoid berbagi via `geo.scale`), **`buildRobotMesh(cls='C')`** (`{group, rig}`; **OVERHAUL MESIN 2026-07-12: rangka ROBOT [geometri bersama `RG`] menggantikan tubuh manusia — torso rangka + PELAT DADA armor warna kelas + panggul + inti daya & VISOR EMISIF senada + ransel daya + sendi bola pinggul/siku; ujung lengan = CAKAR (C/boss) atau **SENAPAN DIPEGANG (B = tangan KANAN saja; A = DUA senapan kiri+kanan, 2026-07-12** — grup senjata diputar searah lengan menodong: receiver+laras+moncong+magasin+popor; **`rig.muzzle`** = ujung laras kanan, **`rig.muzzleL`** = kiri [A saja] — titik spawn peluru musuh, null utk lengan cakar**)**; palet `CLASS_LOOK`: C HIJAU / B KUNING / A MERAH / boss ungu-gelap-visor-merah; rig pivot & tinggi TETAP jadi `animateRobotRig` byte-identik & gore.js membaca rig yang sama; Lambert emissive = program shader sama, tanpa recompile**; `tintRobot`/`applyClassTint`/`ROBOT_VARIANTS`/`ROBOT_SKIN_TONES` DIHAPUS — warna dibangun langsung per kelas, boss juga), `reachForScale(scl, base?)` (**PENTING**: reachMul utk robot berskala — badan pejal `bodyBlockRadius×scl` mendorong player; tanpa ini kelas besar A/boss mendorong player keluar jangkauan cakarnya sendiri; scl 1 → tepat 1.0), `attackerAngle(ax, az)` (sudut penyerang relatif LAYAR utk `showHitDir` — top-down: atas layar = −z dunia, murni `atan2(dx, -dz)`, bukan dari yaw pivot), `animateRobotRig(z, dt)` (**lengan PENEMBAK 2026-07-12: menggantung+mengayun saat jalan, TERANGKAT mengacungkan senapan saat `z.aiming` [di-set scene AI, lerp `z.aimT`] — B hanya lengan KANAN [kiri tetap di bawah], A KEDUA lengan; recoil `z.recoilT`/`z.recoilSide` = laras naik sesaat di lengan yang menembak; lengan melee tetap pose menjulur lama**), **`celebrateRobot(z, dt)` internal (2026-07-12 — SELEBRASI KEMENANGAN: dipanggil updateRobots selama `isPlayerDying()` MENGGANTIKAN AI scene+serangan+rig; robot berhenti, menoleh halus ke jasad player, kedua lengan [senapan B/A ikut] teracung memompa ke langit "HOREEE" + lompatan girang [separuh siklus di udara] + dada membusung + kepala mendongak; delay tersadar/tempo/tinggi lompat/gaya (pompa serempak vs melambai bergantian `z.celebAlt`) DIACAK per robot [field `z.celebT/celebRate/celebPhase/celebHop/celebAlt`, init malas] → sorak MENJALAR di kerumunan; robot campaign 'idle' tetap dorman; pose via pendekatan eksponensial = transisi mulus dari pose apa pun; visual murni)**, `disposeRobot(z)`, **`killRobot(i, {cause,dirx,dirz}?)`** (**GORE 2026-07-11, tema COOLANT 2026-07-12**: SPLICE dari `robots` DULU [bangkai inert] + skor via `robotScore` [kelas C 100 / kelas ranged A·B 150 / boss `boss.score`] + `stats.kills`, LALU serahkan mesh ke gore.js: semburan COOLANT hijau `spawnBloodBurst` + `gibRobot` + `spawnCorpse` + genangan coolant `spawnBloodDecal`; `cause:'explosion'` = HANCUR ekstra-gore (2× semburan coolant 360° ~52 percikan + 5 anggota + ~10 serpihan logam ekstra [tone `0x3d444c`] + ~9 genangan TERCECER), peluru/melee = muncrat + kadang satu anggota; **tanpa headshot**), **`queueBoom(x,y,z,r,hurtPlayer?,playerDmg?,dmg?)`** (antre ledakan `explodeAt` diproses SETELAH loop robot [anti splice reentrant] — kini hanya peluru Grenade Launcher [bullets.js/robots.js, friendly, **meneruskan `b.damage`-nya lewat param `dmg` → AoE ikut level upgrade shop**; `dmg` null → default `CFG.grenade.damage`]; exploder dihapus 2026-07-12 → cabang `hurtPlayer` dorman), `resetRobotsFx()` (kosongkan antrean ledakan — dipanggil resetGame), **`fireRobotBullet(z, tx?, ty?, tz?, monasDmg?)`** (robot ranged tembak 1 peluru → `enemyBullets`, mesh `GEO.bullet`+`MAT.enemyBullet`; **kelas A BERGANTIAN kiri/kanan per tembakan** [toggle `z.fireSide`, delay antar tembakan tetap `fireDelaySec`] — **spawn dari UJUNG LARAS yang menembak** [`rig.muzzle` kanan / `rig.muzzleL` kiri, fallback dada]; target default player, override (tx,tz) utk sasaran lain [survival: pusat Monas]; `monasDmg` distempel ke peluru; **set `z.recoilT`/`z.recoilSide` = hentakan laras NAIK di lengan penembak — TIDAK lagi men-set `clawT`** [dulu tampak mencakar ke bawah]), **`PLAYER_BLOOD_HEX`** (0xb51a1a — warna darah MERAH player), **`updateEnemyBullets(dt, step)`** (gerak+hit peluru MUSUH→player: sweep segmen `segPointDist2`; dodge i-frame / player sudah tumbang = MELESET, **god-mode = darah+flash tetap tampil dan hanya HP-nya yang utuh** [fix 2026-07-12 — dulu seluruh efek di dalam guard godMode sehingga kena peluru tak berdarah]; **kena player → semburan DARAH MERAH `spawnBloodBurst(..., PLAYER_BLOOD_HEX)`** [juga di titik cakar & boom hurtPlayer]; **HP habis → `startPlayerDeath()` game.js, BUKAN gameOver langsung** [berlaku di ketiga titik damage; guard `player.hp > 0` menghentikan damage lanjutan pada yang sudah tumbang]; diblok dinding `activeScene.bulletBlocked` — **peluru ber-`monasDmg` yang terblokir memanggil hook scene `enemyBulletHitMonas(dmg, pos)`**; dipanggil `updateGame` setelah `updateRobots`), `updateRobots(dt, step)` (loop bersama: **selama `isPlayerDying()` → cabang SELEBRASI `celebrateRobot` menggantikan AI+serangan+rig [semua robot berhenti menyerang player & Monas; peluru player yang melayang tetap diuji]**; **peluru `b.explosive` kena robot → `queueBoom` + buang peluru [ledakan AoE menyusul]**; `activeScene.robotAI` → **serang bila `chaseDist` ada**: kelas MELEE (C/boss) cakar [damage `z.clawDmg`, jangkauan × `z.reachMul`, indikator arah]; kelas RANGED (`z.ranged`, B/A) **STOP-AND-SHOOT (2026-07-12)** — scene AI menghentikannya di `0.95×z.range` saat LOS bebas, lalu `fireRobotBullet` tiap `z.fireDelaySec` digerbang `z.losOK !== false && chaseDist < z.range` [range per kelas: B 10 m = 70 u, A 12 m = 84 u — tak menembak tembus dinding] → rig → sweep hit peluru [**HORIZONTAL/2D bidang xz — y diabaikan** supaya hitbox pendek (robot skala kecil) tak lolos di bawah lintasan peluru; radius silinder `bodyHitRadius`×`z.scl`; damage = `max(1, b.damage×player.dmgMul − z.armor)`; `stats.hits`] → `processPendingBooms` [ledakan Grenade Launcher DI LUAR loop — anti bug splice reentrant; ledakan berantai iteratif]). Field per-robot KELAS: `kind` (='C'/'B'/'A'/'boss'), `scl, armor, attack, clawDmg, ranged, fireDelaySec, bulletSpeed, range` (unit, = `rangeMeters×CAMP_M`, distempel scene spawner), `fireCd, losOK` (di-set scene AI tiap frame = `aim.direct`), `aiming`/`aimT` (stance membidik — scene AI/lerp rig), `fireSide` (A: giliran laras ±1), `recoilT`/`recoilSide` (hentakan laras naik), `reachMul` (target Monas-vs-player kini per-frame berbasis jarak di survival robotAI — bukan field statis).
- **drops.js** — `MEDKIT_MAT`, `MAG_GEO`, `MAG_MAT` (bersama, jangan dispose), `buildMedkitMesh()`, `buildMagMesh()` (Group magazen kurva: 3 segmen miring + alas + bibir + peluru brass — dipakai spawnDrop & supply stage 1; ganti balok kuning lama), `spawnDrop(pos)` (peluang CFG.drops + `activeScene.clampDropPos`; **drop granat DIHAPUS 2026-07-11 — cuma paket peluru 'mag'**), `updateDrops(dt, T)` (bob, pickup dgn aturan full-item + feed "already full" cd 1.2s, kedaluwarsa; **drop 'mag' = PAKET PELURU: +`CFG.weapons.<w>.ammoPickup` per senjata DIMILIKI [termasuk launcher], cap `maxAmmo`**; **medkit pickup = tambah 1 medkit genggam [maks `CFG.player.maxMedkits`] — dipakai via tombol 4**).
- **effects.js** — `initEffects(scene)` (pool 3 lampu + **72** sprite percikan cairan — **tekstur splat NETRAL putih, DIWARNAI per-spawn via material.color (2026-07-12): default `COOLANT_HEX` hijau [robot], darah MERAH player lewat param warna [`PLAYER_BLOOD_HEX` robots.js]; nama fungsi `Blood*` dipertahankan**), `explodeAt(pos, radius?, dmg?)` (visual + DAMAGE robot dlm radius: `max(1, d − z.armor)` dgn d = param `dmg` [peluru launcher meneruskan `b.damage`-nya yang sudah ber-level upgrade] atau default `CFG.grenade.damage` [boss `z.kind==='boss'` tetap `boss.grenadeDamage` khusus — tak terpengaruh upgrade] — robot ber-HP tinggi bisa selamat di pinggir radius; **mati oleh ledakan → `killRobot(i,{cause:'explosion',dir…})` = dismember**), `spawnGroundPuff(x, z, color, scale, y)`, **`spawnBlood(x, y, z, vx?, vy?, vz?, color?)`** (percikan pool tetap; kecepatan opsional = MUNCRAT + jatuh; `color` default `COOLANT_HEX`), **`spawnBloodBurst(x, y, z, dirx, dirz, n, power?, spread?, color?)`** (semburan `n` percikan kerucut `spread` rad searah damage; `spread`=6.283 → 360° utk ledakan — dipakai saat kena tembak & saat mati; `color` = hijau robot / merah player), `updateExplosions(dt)`, `updateBloodPool(dt)` (integrasi kecepatan+gravitasi percikan), `resetBloodPool()`, **`borrowBloodSprite()`** (warmup preload). Top-down: `spawnBlood` menggeser sprite ke arah **viewCam**. (Bangkai/gib/genangan coolant lantai = modul terpisah **gore.js**.)
- **gore.js** — **GORE (2026-07-11; re-tema COOLANT 2026-07-12)**: `initGore(scene)` (pool TETAP gib `GIB_COUNT` 64 [serpihan logam, warna default `0x3d444c`] + genangan coolant `DECAL_COUNT` 44 [tekstur blob HIJAU, tint default `0x2fbf66`], dibuat sekali & disembunyikan di scene → warmup preload mengompilasi shader-nya, nol recompile), `spawnCorpse(group, rig, {dirx,dirz,dur?,fast?})` (jadikan mesh robot BANGKAI: pose lemas → toppling `rotateX` di atas kaki → memudar [material di-`transparent`] + ambles → dispose), `gibRobot(rig, group, level, dirx, dirz, restY)` (`'heavy'`=ledakan: lengan/paha/kepala lepas; `'light'`=peluru/melee: kadang satu — SEMBUNYIKAN pivot di bangkai + lempar gib sewarna pelat anggota itu [`limbTone`]), `spawnGibs(x,y,z,count,dirx,dirz,power,tone,restY)` (serpihan balistik + spin + mantul + genangan coolant saat mendarat), `spawnBloodDecal(x,z,s,tone?)` (genangan coolant pipih di tanah), `updateGore(dt)` (dipanggil updateGame SETELAH updateRobots), `resetGore()` (dipanggil resetGame — dispose bangkai, sembunyikan pool). Geometri/material gib & tekstur decal DIBAGI (jangan dispose); material bangkai = per-instance robot (dispose bersama bangkai).

### src/scenes/
- **menu.js** — `initMenu(onPick)`: kartu `#modeSelect` (`data-mode`) + baris `#diffRow` difficulty (localStorage `gibsDifficulty`; saat mode diklik → `applyDifficulty(diff)` + `setDifficulty(diff)` SEBELUM `onPick`) + cutscene 4 slide (hanya survival; campaign lompat ke blocker).
- **survival/world.js** — `PARK, FENCE_H, ROAD_W, FOUNTAIN, treeColliders, MONAS_HALF` (22 = half dasar Monas 44×44); `buildSurvivalWorld()` (ground/jalan/pagar/props/Monas/city); `buildSurvivalNav(fountainWalkable=true)` (nav-grid: Monas+pohon penghalang; bak walkable=vault survival / bak PEJAL utk stage 3 tanpa vault); **dunia taman DIBAGI dua scene**: `ensureParkWorld()` (bangun sekali), `getSurvivalNav()` (grid bak-walkable), `getParkNavSolidFountain()` (grid bak-pejal); **`resolveMonas(pos, oldX, oldZ, radius)`** (AABB Monas dorong keluar PER-SUMBU = menyusur, TIDAK menempel/macet — seperti slideWalk campaign), `resolveObstacles(pos, r, feetY)` (pohon + bak; return true bila bak menghalangi = pemicu vault), `segmentHitsFountain(...)`, `groundHeightAt(x, z, feetY)`. **Kanopi kabut event** (`createFogCanopy()` dipanggil dari `buildSurvivalWorld` = dibangun sekali, ikut warm-up preload): piringan overhead tembus-pandang di y 26, tekstur radial dengan LUBANG bening `CFG.survival.monasFogClearMeters` di pusat=Monas; kendali `setFogCanopy(k)` (opacity 0..1, sembunyi saat k~0) + `driftFogCanopy(dt)` (putar pelan; lubang tetap terkunci di Monas).
- **survival/index.js** — `survivalScene` (+hook `shopKey`/`shopActive`). **Wave berbasis "clear" (overhaul 2026-07-07)** via `wave.phase` (`'fighting'`→`'cleared'`→`'shopping'`): `startWave(n)` (jatah `robotsPerWaveBase + (n-1)·Step`, cadence & cap dari `CFG.survival`, picu event bila `n % eventEveryWaves`), `updateMode` (spawn sampai jatah habis dibatasi `maxConcurrent`; bersih [`toSpawn===0 && robots.length===0`] → 'cleared' + "WAVE CLEARED" + hitung mundur `shopCountdownSec` → `openShop()` + 'shopping'; catatan: fase shopping mem-PAUSE game [shop modal] jadi updateMode diam sampai Next Wave), `startNextWave()` (export; tutup shop + `startWave(num+1)` + **`requestLock()` untuk resume** — dipanggil dari gesture klik/SPACE), `shopKey(key)` (HANYA SPACE/Enter → Next Wave; pembelian item lewat KLIK di shop.js). `spawnRobot()` (lompat pagar dua fase + roll KELAS C/B/A per wave; stempel `z.range` = rangeMeters×CAMP_M), **`monasHp`/`monasMaxHp` scene-local** (max di-reset dari `CFG.survival.monasMaxHp` di `enter()`; objektif PRIORITAS; 0 → `gameOver(false,'THE MONUMENT HAS FALLEN')`) + item shop Monas `healMonas()` (export; +25% max HP, tolak bila penuh), `strengthenMonas()` (export; naikkan max ke TINGKAT berikutnya di `CFG.survival.strengthenMonasStages` mis. 3000→4500→6000 + heal sebesar kenaikan; `monasStage` scene-local, plafon di tier terakhir → tolak), getter `getMonasHp/getMonasMaxHp` + `isMonasFullyStrengthened()` (shop tandai 'Maxed'), `EVT` event wave = **kabut Monas SAJA** (fog jarak + blackout lama DIBUANG 2026-07-11): `'monasfog'` = kanopi kabut berbasis-posisi via `setFogCanopy`/`driftFogCanopy` (world.js), lubang bersih `monasFogClearMeters` di Monas; di-roll di `startWave` (eligible ≥ `monasFogFromWave`, peluang `min(monasFogChanceMax, monasFogChanceBase + monasFogChancePerWave·(n−fromWave))`), `robotAI` (jumping→chasing via `navAim`; **target per-frame: `atkMonas = monasHp>0 && distToEye > playerAggroMeters·CAMP_M` — serang Monas default, kejar player hanya dalam radius aggro**; **serangan Monas per kelas (2026-07-12): MELEE menggerogoti tepi AABB [stop 6, `damageMonas(monasClawDamage)` per cakaran], PENEMBAK B/A BERHENTI di `0.95×z.range` lalu `fireRobotBullet(z, 0,0,0, monasClawDamage)` ke PUSAT Monas tiap `fireDelaySec` — peluru membawa `monasDmg`, terblokir siluet Monas → hook `enemyBulletHitMonas(dmg,pos)` [scene ini] = `spawnGroundPuff` + `damageMonas`; komit/lock Monas di-roll pada TEMBAKAN pertama juga**; penyerang Monas return `{}`). `hudStatus`: fighting `Wave N — X left · Monas P%`, cleared hitung mundur, shopping "Field Shop open".
- **survival/shop.js** — shop antar-gelombang **MENU KLIK modal** (2026-07-08; game DI-PAUSE + pointer dilepas oleh input.js selama `shopActive()`; dibuka OTOMATIS oleh scene setelah wave selesai). Katalog data-driven `catalog()` → item `{id,name,desc,cost,weapon?,apply()}`: ammo (senjata DIMILIKI saja, termasuk launcher), **health** (Replenish Health — sembuh instan 100%), **medkit** (beli 1 medkit genggam → `player.medkits`; dipakai via tombol 4), **healMonas**, **strengthenMonas**, **radar** (set `player.hasRadar`), shotgun, rifle, **launcher** (Grenade Launcher, `launcherCost`), **+4 item UPGRADE senjata `up_<w>` (2026-07-12, helper `upgradeItem(w)`): kartu hanya MUNCUL utk senjata yang sedang DIMILIKI** (pistol/shotgun/rifle/launcher) — nama "Upgrade [Weapon] II/III", menaikkan `player.weaponLvl[w]` Lv2 lalu Lv3 (= `maxWeaponLevel`, maks), tiap level +`upgradeDamagePct` (25%) dari damage BASE (efek via `weaponDamage()` weapons.js; boom launcher ikut), harga per tingkat `CFG.shop.upgradeCosts[w]`, level maks → `ownedNote` 'Maxed' + pesan tolak per-item `maxedMsg` (`apply()` → null sukses / string alasan; Monas via impor `healMonas`/`strengthenMonas`/`isMonasFullyStrengthened` dari index.js — circular; Strengthen bertingkat, plafon → 'Maxed'; deskripsi SENGAJA tanpa angka max HP). **Beli senjata:** slot kosong → `buyWeapon` append + `syncOwnedFromWeapons` + `refreshOwnedWeapon`; **slot penuh (`maxWeapons` 3)** → `shopPurchase` return `'choose-replace'` + `pendingWeapon`, UI `renderReplace` (tombol per senjata), lalu **`shopReplaceWeapon(oldW)`** (ganti di slot itu + potong skor) / **`shopCancelReplace()`**; `isReplacePending()`/`pendingWeaponName()`. `shopPurchase(id)` (dipakai handler KLIK DOM DAN test headless: return null/alasan/`'choose-replace'`; potong skor via `addScore(-cost)` hanya bila sukses; **BELI hanya lewat tombol Buy** — klik baris daftar cuma memilih/preview), **`requestNextWave()`/`isConfirmOpen()`** (Start Next Wave dua langkah: panggil pertama → prompt "Are you ready?" Yes/No [`confirmNext`], panggil kedua / klik Yes → `startNextWave`), `openShop/closeShop/isShopOpen`, `render()` (createElement → handler klik/hover nyata; **GRID kartu** `.shopGrid`→`.shopCard` [nama + tombol `.shopCardBuy` per kartu = satu-satunya jalur beli; klik kartu hanya PILIH/preview] + panel `.shopDesc` lebar-penuh di bawah [deskripsi+harga via `showDesc`, diperbarui saat hover], atau `.shopReplace*` saat pendingWeapon, atau `.shopConfirm*` saat confirmNext; bawah `.shopScore` + `.shopNext` → `requestNextWave`). Impor `WEAPON_DEF`/`refreshOwnedWeapon` dari weapons.js + `syncOwnedFromWeapons` dari state.js. Next Wave dari SPACE (scene `shopKey` → `requestNextWave`). Semua teks English.
- **campaign/common.js** — `spawnCampaignRobot(x, z, stage, cls='C')` (idle; cls: C/B/A [CFG.robot.classes; B/A = penembak ranged, `z.range` distempel dari rangeMeters×CAMP_M] / **boss** [CFG.campaign.boss — langsung chasing, melee, frame gelap raksasa dari `buildRobotMesh('boss')`, granat luka berkurang, skor khusus]; hp/speed/attack/scale per kelas dibangun `buildRobotMesh(cls)`; `reachMul` via `reachForScale`), `campaignRobotAI(z, dt, step, {walkable, resolve, los?, nav?})` (culling `cullDistance`, aktivasi `activateMeters`×CAMP_M + LOS opsional/tembak; kejar via `navAim(nav)` + slide per-sumbu; **stop: ranged berhenti di `0.95×z.range` saat `aim.direct` [stop-and-shoot] else stop-range × `z.reachMul`; `z.losOK = aim.direct` + `z.aiming = !z.moving && aim.direct` tiap frame [stance senapan terangkat]**), `countStageRobots(n)`.
- **campaign/stage1.js** — `stage1Scene` (id `campaign-1`) + `S1, s1grid, s1Cell, S1_START, S1_EXIT, s1Wall, stage1Walk, s1LOS, s1SegHitsWall, resolve, s1Nav (nav-grid SETENGAH sel = 7 unit: dinding dari grid denah + furnitur/undakan di-bake via resolve — dibangun di AKHIR buildWorld setelah blockers terisi), buildWorld, placeRobots, placeSupplies`. Grid = satu-satunya sumber dinding (visual+kolisi+LOS+peluru). **Denah dirombak total 2026-07-11 ke floor-plan referensi user** (Lantai 2): START = tangga TURUN kiri-atas (`S1_START` c3,r4; player turun dari Lt.3) → koridor sempit → Office/Conference/Supply Room → Main Hall pusat → Security/Break/Restroom/Storage → END = tangga TURUN kanan-bawah (`S1_EXIT` c24-28,r23-27; ke lantai bawah = stage 2). 9 spot robot `S1_ROBOTS` `[c,r,jumlah]`; supply di Supply Room + bonus tersebar. **Konektivitas BFS + bebas-pintu + supply-di-lantai sudah diverifikasi** (skrip di scratchpad final.js) — jika mengubah denah/furnitur, jalankan ulang tes itu (BFS dari start meliputi semua sel lantai, tak ada furnitur menutup sel pintu, tiap posisi supply walkable & di luar furnitur). **Top-down 2026-07-11: mesh plafon `visible=false`** (kamera di atas — interior terlihat melewati dinding; fisika pantulan granat di plafon TIDAK berubah). `enter()` = orkestrator campaign: build kedua stage (guard) + tempatkan SEMUA (stage2 robots, stage1 robots, supplies) + preset indoor + posisi start. Trigger `S1_EXIT` di `playerCollide` → `setScene(stage2Scene, {transition:true})`.
- **campaign/stage2.js** — `stage2Scene` (id `campaign-2`) + `CAMP, CAMP_DIR, CAMP_PERP, CAMP_START, highwayWalk, resolve, buildWorld, placeRobots`. Internal `navGrid` (bake di akhir buildWorld: koridor jalan+cincin+serong; median/mobil di-bake lewat resolve — path lewat celah median; lengan barat/timur di luar grid = fallback lurus) + **boss** (`bossSpawned/bossRef`: `checkWin` men-spawn boss saat sisa ≤ `spawnWhenRemaining` — TERMASUK lompatan langsung ke 0; HP bar boss di `hudStatus` [blok █░], `updateMode` me-refresh UI 0.2s selama boss hidup). `enter()` = pembersihan robot stage 1 + reset boss + preset outdoor + teleport + stage msg. `checkWin`: boss tumbang & sisa 0 → `setScene(stage3Scene, {transition:true})`.
- **campaign/stage3.js** — `stage3Scene` (id `campaign-3`, FINAL) — "Taman Monas, Malam Hari": MEMAKAI ULANG dunia taman survival (`ensureParkWorld` + nav `getParkNavSolidFountain` — bak pejal, campaignRobotAI tak punya vault) + preset `night`. Robot (campuran varian, `CFG.campaign.stage3Robots`) ditempatkan di `enter()` (bukan orkestrator stage 1 — resetGame membuang robot dulu, aman). Hook player/granat/peluru = pola survival (pagar clamp, AABB Monas, resolveObstacles). `checkWin`: `countStageRobots(3) === 0` → `gameOver(true)`. `restartScene` → stage1.

## config/gameplay.json — kunci tuning

| Seksi | Kunci |
|---|---|
| `player` | maxHp, speed, radius, eyeHeight, gravity (jatuh dari tepian; lompat dihapus), **medkitHealPct** (0.7 = +70% HP), **maxMedkits** (2), **medkitUseSec** (2 = detik tahan klik utk pakai medkit), **medkitSlowMul** (0.75 = gerak −25% saat memakai medkit), **deathDelaySec** (2 = jeda sekuens gore kematian player sebelum layar GAME OVER, 2026-07-12) |
| `stamina` | max, adsDrainPerSec, meleeCost, regenPerSec, recoverThreshold (sprintDrainPerSec DIHAPUS 2026-07-11; adsDrainPerSec dorman) |
| `dodge` | staminaCost (20), cooldownSec (1.0), durationSec (0.45), speed (2.2 — diturunkan dari 4.2 2026-07-12: salto pendek ~4 m) — dodge/evade Shift (2026-07-11) |
| `movement` | adsMultiplier (dorman), **backpedalWeight (0.5) & strafeWeight (0.5) — HIDUP LAGI 2026-07-12: pengali kecepatan mundur/menyamping relatif KURSOR (dirSpeedMul di player.js; maju searah kursor = penuh)**, walkSpreadPenalty (sprintMultiplier & sprintSpreadPenalty DIHAPUS 2026-07-11) |
| `weapons` | **maxWeapons** (3 = slot senjata maks sejak 2026-07-11), **upgradeDamagePct** (0.25 = +25% damage base per level upgrade shop, 2026-07-12), **maxWeaponLevel** (3 = plafon level upgrade), bulletSpeed, bulletLife, bulletDamage (fallback), spreadBase, spreadBloom, heatPerShot, heatCoolPerSec, adsAccuracy (dorman — ADS dihapus; crouchAccuracy DIBUANG 2026-07-11); per senjata (`rifle`/`pistol`/`shotgun`/**`launcher`**): **maxAmmo** (kap kolam peluru — rifle 500 / pistol 150 / shotgun 300 / **launcher 25**), **ammoPickup** (isi ulang per drop peluru — 80/30/50/**8**), fireDelayMs, cameraKick (recoil visual prop avatar), **damage** (dibawa peluru, DIKALI level upgrade via `weaponDamage()`; rifle 40/pistol 25/shotgun 10-per-pelet/**launcher 100 AoE**) (+ shotgun: pellets, pelletSpread; + **launcher: roundSpeed** — peluru MELEDAK saat kena, radius = `grenade.killRadius`+3.5) |
| `melee` | range, cooldownSec |
| `grenade` | **killRadius + damage (100) = LIVE** (dipakai `explodeAt` + peluru Grenade Launcher); max/start/fuseSec/throwSpeed/throwUpward/throwNearSpeed/throwNearUpward/pushSpeed = DORMAN (granat lempar dihapus 2026-07-11) |
| `robot` | bodyHitRadius (5, radius hit badan — hit-test HORIZONTAL/2D, tanpa headshot), bodyBlockRadius (7.5, dorong-keluar fisik), clawCooldownSec (1), clawRange, stopRange, repathSec (rate-limit findPath), stuckSec (macet → repath paksa), turnRadPerSec (laju putar heading), **rangedBulletLife** (120, umur peluru robot; `rangedRange` global DIHAPUS — range kini per kelas), score.{normalKill 100, specialKill 150} (special = kelas ranged A/B), **classes.{C,B,A}.{hp,speed,armor,attack,ranged,scale}** (+ranged **rangeMeters**,fireDelaySec,bulletSpeed) — C: 60/0.55/melee/atk5/HIJAU; B: 90/0.75/RANGED/atk5/**range 10 m**/delay1.0/bspd7/KUNING; A: 120/0.95/RANGED/atk10/**range 12 m**/delay1.5/bspd5/MERAH (menggantikan variants runner/brute/exploder yg DIHAPUS 2026-07-12) |
| `survival` | robotsPerWaveBase/Step (jatah robot per wave), shopCountdownSec (jeda "Wave Cleared"→shop), spawnIntervalBase/Step/Min, maxRobotsBase/Step/Cap (cap robot di lapangan), **robotSpeedWaveMin/Step/Max** (faktor ramp kecepatan per-wave dikali speed KELAS: wave 1 = Min [0.72], +Step [0.02] tiap wave, dijepit Max [1.0] → penuh ~wave 15; HP/speed dasar kini dari `CFG.robot.classes`, bukan lagi robotHp*/robotSpeed* yg DIHAPUS), **classBFromWave/classBChance/classBChancePerWave/classBChanceMax** (peluang spawn kelas B NAIK per wave sejak FromWave 3, dijepit Max), **classAFromWave/classAChance/classAChancePerWave/classAChanceMax** (kelas A sejak wave 6), monasMaxHp, **strengthenMonasStages** (tangga max-HP Monas item shop Strengthen — mis. [3000,4500,6000], tier terakhir = plafon), playerAggroMeters (radius aggro player, m; robot serang Monas di luar radius ini, kejar player di dalamnya), **monasLockAggroMeters** (radius aggro yang menyusut setelah robot mulai memukul Monas — mis. 5 m; jauh lebih sulit dialihkan), **monasLockChance** (peluang robot yang sudah memukul Monas TERKUNCI penuh = radius 0, tak pernah mengejar player — mis. 0.5), monasClawDamage, **monasFogFromWave** (wave pertama eligible event kabut = "setelah wave 3", mis. 4), **monasFogChanceBase** (peluang kabut di wave eligible pertama, mis. 0.20), **monasFogChancePerWave** (kenaikan peluang tiap wave, mis. 0.03), **monasFogChanceMax** (plafon peluang, mis. 0.75), **monasFogClearMeters** (radius zona bersih di sekitar Monas saat kabut, mis. 15 m), **monasFogDurationSec** (mis. 60) |
| `campaign` | robotSpeedScale (kali speed kelas), activateMeters, cullDistance, stage3Robots (grunt kelas C default; stage 3 campur ~60/30/10 C/B/A), boss.{hp,speed,scale,clawDamage,reachMul,spawnWhenRemaining,score,grenadeDamage} (boss = melee) |
| `drops` | magChance, grenadeChance (dorman — drop granat dihapus 2026-07-11), lifetimeSec |
| `shop` | ammoCost, **healthCost** (Replenish Health instan 100%), **medkitCost** (item Medkit genggam), healMonasCost, strengthenMonasCost, radarCost (harga item Radar; Survival mulai tanpa radar), shotgunCost, rifleCost, **launcherCost** (harga Grenade Launcher), **upgradeCosts.{pistol,shotgun,rifle,launcher}** (2026-07-12: `[hargaLv2, hargaLv3]` per senjata utk item Upgrade) |
| `difficulty` | easy/normal/hard.{robotHpMul, robotDamageMul, spawnIntervalMul} — diterapkan `applyDifficulty()` dari CFG_BASE |

Catatan: nilai visual murni (FOV, amplitudo animasi, warna) sengaja BUKAN di JSON. (Sistem magazen + reload dihapus 2026-07-11 — rig animasi reload di weapons.js dorman.)

## Menjalankan & menguji

- **Wajib HTTP server** (ES modules + fetch config tidak jalan di `file://`): `python -m http.server 8000` → `http://localhost:8000`. `index.html` menampilkan pesan error yang jelas bila dibuka via file://.
- `package.json` hanya metadata `{"type":"module"}` untuk tooling Node (`node --check src/**/*.js`) — tetap TANPA dependensi/build.
- Test headless (Node + stub THREE/DOM, menjalankan modul asli): harness ada di scratchpad sesi pengembangan (`rt/stubs.mjs`, `driver.mjs`, `test-survival.mjs`, `test-campaign.mjs`, `test-config.mjs`) — 61 assert meliputi gerak/pagar/spawner/tembak/cakar/stamina/granat/melee/pickup/lompat/BFS grid/aktivasi idle/wall-slide/peluru-vs-dinding/transisi stage/menang/restart/tuning config. Bila hilang, pola pembuatannya: stub `THREE` (Vector3/Quaternion/Euler matematika nyata, sisanya no-op), stub DOM/Audio/localStorage, set `__GIBS_TEST__` + `__GIBS_CONFIG__`, import `src/main.js`, panggil `boot()` + `startGame(mode)`, drive `updateGame(dt, step, T)` manual dgn `Date.now()` dipalsukan.
