# MODULES.md — Katalog Modul & Kontrak Antarmuka

> Peta lengkap setiap ES module di `src/`, ekspor pentingnya, dan kontrak antar-modul.
> **Gunakan file ini sebagai referensi — tidak perlu membuka setiap file untuk tahu apa yang tersedia di mana.**
> Konvensi: komentar kode Bahasa Indonesia, seluruh teks UI **English**. `THREE` adalah global (CDN r128, bukan import).

## Peta folder

```
index.html            DOM overlay + CDN scripts + <script type="module" src="src/main.js">
css/style.css         seluruh CSS
config/gameplay.json  SEMUA konstanta mekanik yang bisa di-tuning (lihat tabel di bawah)
src/
  main.js             entry: boot() -> menu -> startGame(mode) -> animate() loop
  core/               engine & orkestrasi (bukan gameplay spesifik mode)
  utils/              helper murni yang bisa dipakai di mana saja
  world/              elemen dunia BERSAMA antar scene (cahaya, langit, dekorasi, fasad gedung)
  entities/           sistem gameplay bersama (player, senjata, peluru, granat, robot, drop, efek)
  scenes/             satu folder/file per scene; menambah stage = tambah file di scenes/campaign/
```

## Aturan arsitektur (WAJIB dipertahankan)

0. **PIVOT TOP-DOWN (2026-07-11, game beralih dari FPS ke top-down ala Alien Shooter):** objek `camera` (core/renderer.js) = **PIVOT LOGIKA player** — posisi = titik setinggi mata (kaki + `eyeHCur`), yaw = arah BIDIK kursor (`updateTopdownAim` di input.js). SEMUA sistem gameplay tetap membaca `camera.position`/`camera.getWorldDirection` seperti era FPS — JANGAN ganti ke objek lain. Render dilakukan `viewCam` (ofset tinggi `followViewCam`); visual player = avatar `entities/playerAvatar.js` (senapannya `avatarGunTip` = titik spawn peluru); rig viewmodel FPS tersembunyi permanen dalam `fpsHolder` (weapons.js) tapi tetap dibangun karena logika reload/switch/medkit menggerakkannya. Urutan animate(): `updateTopdownAim` → `updateGame` → `followViewCam`+`updatePlayerAvatar` → decor → render(viewCam).
1. **State bersama = live binding ESM.** Modul pemilik meng-export `let` + fungsi setter; modul lain hanya membaca (mis. `isPaused` milik `core/state.js`, `stamina` milik `entities/player.js`, `currentWeapon` milik `entities/weapons.js`). Jangan menulis state modul lain secara langsung — panggil setter/fungsinya.
2. **Semua angka mekanik dari `CFG`** (`core/config.js`, dimuat dari `config/gameplay.json` SEBELUM game mulai). Baca `CFG.<seksi>.<kunci>` **di dalam fungsi**, jangan di top-level modul (config belum termuat saat modul dievaluasi). Jangan hardcode angka tuning baru — tambahkan kuncinya ke JSON.
3. **Sistem bersama tidak boleh tahu mode.** Tidak ada lagi `GAME_MODE`/`CAMP_STAGE` if-else di sistem bersama — semuanya lewat hook `activeScene.*` (kontrak scene di bawah). Kalau butuh perilaku baru per scene, tambahkan hook baru ke SEMUA scene, jangan if-else mode.
4. **Import silang antar modul boleh melingkar** (weapons↔player, robots↔effects, stage1↔stage2, game↔input) **asal** binding hanya dipakai di dalam fungsi (call-time), tidak pernah di top-level.
5. **Urutan blok `updateGame()` di `core/game.js` = kontrak** (spawner → timer senjata → gerak player → recoil → tembak → granat → ledakan → darah → drop → peluru → robot → **gore [mayat/gib/genangan]** → cek menang). Jangan diubah tanpa alasan kuat.
6. Sisa aturan lama tetap berlaku: pool tetap (3 lampu ledakan, 14 sprite darah), material bersama jangan di-dispose, iterasi array entitas mundur, kalikan gerak dgn `step` & countdown dgn `dt`, dsb (lihat CLAUDE.md).

## KONTRAK ANTARMUKA SCENE

Scene = objek polos yang didaftarkan lewat `setScene(scene, opts)` (`core/sceneManager.js`).
Sistem bersama memanggil hook ini pada `activeScene` — **scene baru wajib mengimplementasikan semuanya**:

| Hook | Wajib | Dipanggil oleh | Tugas |
|---|---|---|---|
| `id` | ya | — | string unik, mis. `'campaign-3'` |
| `enter(opts)` | ya | sceneManager | bangun dunia (guard sekali), tempatkan entitas, posisi+arah kamera, preset cahaya |
| `exit()` | opsional | sceneManager | bersih-bersih saat meninggalkan scene |
| `updateMode(dt)` | opsional | game.updateGame | logika per-frame khas scene (wave spawner survival) |
| `playerCollide(pos, oldX, oldZ, feetY)` | ya | entities/player | dinding keras + penghalang pejal + (stage 1) trigger exit |
| `groundHeight(x, z, feetY)` | ya | player & grenades | ketinggian lantai yang bisa dipijak |
| `bulletBlocked(b)` → bool | ya | entities/bullets | peluru mati kena dinding dunia (Monas / dinding grid); juga dipakai peluru MUSUH (robots.updateEnemyBullets) |
| `enemyBulletHitMonas(dmg, pos)` | opsional | entities/robots.updateEnemyBullets | peluru robot BERTARGET Monas (`b.monasDmg`) mati di dinding → scene memotong HP Monas (hanya survival yang punya) |
| `monasGnawHit(z)` | opsional | entities/robots.updateRobots (ticker windup) | sabetan cakar bertarget Monas MENDARAT (2026-07-13 — `z.windTarget==='monas'` setelah `clawWindupSec`) → scene memotong HP Monas (`damageMonas`); hanya survival |
| `grenadeCollide(g, oldGX, oldGZ)` | ya | entities/grenades | pantulan dinding/batas + resolve pejal + (stage 1) plafon |
| `robotAI(z, dt, step)` → `{skip?}` / `{chaseDist?}` | ya | entities/robots | gerak/aktivasi robot; `skip`=lewati rig+hit test (idle jauh); `chaseDist`=jarak saat cabang kejar berjalan (syarat serang: cakar melee / tembak ranged [scene juga men-set `z.losOK` + stop di `z.range`]) |
| `clampRobot(z, oldX, oldZ)` | opsional | entities/robots.separateRobots | jepit robot ke area sah SETELAH dorongan separasi (bugfix 2026-07-16 — cegah nyangkut dinding); survival = pagar+Monas+pohon, campaign = `campaignClampRobot` (furnitur + slide per-sumbu `walkable`); `(oldX,oldZ)` = posisi valid pra-separasi |
| `clampDropPos(x, z)` → `[x,z]` | ya | entities/drops | penjepit posisi drop |
| `hudStatus()` → string | ya | core/hud.updateUI | teks `#waveText` |
| `radarLandmarks(plot)` | ya | core/hud.drawRadar | `plot(dx, dz, color, size, clampEdge?)` relatif player; `clampEdge=true` = landmark di luar jangkauan dijepit ke tepi radar sbg penunjuk arah |
| `checkWin()` | opsional | game.updateGame | panggil `gameOver(true)` bila menang / transisi stage |
| `restartScene()` → scene | opsional | game.resetGame | scene tujuan saat restart setelah mati (campaign → selalu stage 1); tanpa ini restart di scene sendiri |
| `shopKey(key)` → bool | opsional | core/input keydown | shop MODAL survival: SPACE/Enter = Start Next Wave (pembelian item lewat KLIK, bukan keyboard); return true = tombol DIKONSUMSI |
| `shopActive()` → bool | opsional | core/input keydown + pointerlockchange | predikat "shop modal terbuka": input.js menelan semua tombol gameplay & saat pointer di-unlock, mem-pause TANPA menampilkan blocker (kursor untuk menu klik) |

### Cara menambah stage campaign baru (mis. Stage 4)

1. Buat `src/scenes/campaign/stage4.js` meniru pola gedung indoor `stage1.js`/`stage2.js`/`stage3.js` (grid sel 2 m + dinding/collision/LOS/hit-peluru dari grid yang sama, dunia sendiri di offset jauh mis. `x ≈ 120000`) ATAU memakai ulang dunia yang sudah ada + preset cahaya beda (lebih murah): konfigurasi tata letak/grid + `buildWorld()`/`ensure…()` + `placeRobots()` (pakai `spawnCampaignRobot(x, z, 4, cls?)`) + objek scene dgn semua hook (pakai `campaignRobotAI` dari `common.js` dgn `walkable`/`resolve`/`los?`/`nav` milik stage). **Verifikasi konektivitas grid dgn BFS** (scratchpad + assert smoke) sebelum selesai.
2. **SEMUA dunia campaign di-pre-build di awal (2026-07-16)** — stage baru meng-export `ensureWorld()` (guard `built` di dalamnya) + `worldBuilt()` dan dipanggil dari guard `built` di `stage1.enter()` (loading antar-stage jadi konsisten ~`MIN_LOADING_MS`, tak ada build+compile lazy di LOADING #2); `enter()` stage itu tetap memanggil `ensureWorld()` sebagai jaring pengaman, dan robot ditempatkan di `enter()` (resetGame membuang semua robot lebih dulu, jadi aman).
3. Arahkan transisi: scene sebelumnya men-trigger `setScene(stage4Scene, { transition: true })` (contoh nyata: `checkWin` stage 2 bertransisi ke stage 3 setelah boss tumbang). `enter()` stage baru membersihkan robot stage sebelumnya (`z.stage === N-1`) seperti `stage2Scene.enter`/`stage3Scene.enter`.
4. Beri `restartScene: () => stage1Scene` dan `hudStatus()` dgn `countStageRobots(4)`.
5. Stage TERAKHIR yang memakai `checkWin()` → `gameOver(true)` (saat ini: stage 3).

## Katalog modul

### src/main.js
`boot()` (muat config → menu), `startGame(mode)` — **async** (2026-07-10): `showLoading()` → init DIPENGGAL dgn `await loadingStep(pct, label)` (renderer → lights → qualityUI → effects → sky | setScene | **initPlayerAvatar** (SEBELUM initWeapons — muzzle flash nempel avatar) → embers → weapons → input → grain → **followViewCam()** (matrix viewCam valid utk raycast bidik frame pertama) | `preloadAllSFX()` | `warmupAll()`) → `hideLoading()` → UI → animate; try/catch → `showFatal`. Loop `animate()` **top-down 2026-07-11**: `updateTopdownAim()` (kursor→aim, SEBELUM updateGame) → `updateGame(dt, step, T)` → `followViewCam()` + `updatePlayerAvatar(dt)` (posisi pivot terbaru; jalan juga saat pause) → shadow/decor/weaponVisuals → embers+radar (saat main) → `composer.render()` (RenderPass = viewCam) / fallback `renderer.render(scene, viewCam)`. Auto-boot kecuali `__GIBS_TEST__`.

### src/core/
- **config.js** — `CFG` (objek berisi seluruh gameplay.json), `CFG_BASE` (salinan MURNI hasil muat — tak pernah dimutasi), `CAMP_M = 7` (unit per meter), `loadConfig()` (fetch `config/gameplay.json`; test hook `__GIBS_CONFIG__`; validasi seksi — seksi baru WAJIB didaftarkan di array `SECTIONS`), `applyDifficulty(name)` (mutasi CFG dari CFG_BASE dgn pengali `CFG.difficulty[name]` — idempoten; dipanggil menu sebelum startGame; menyentuh: hp robot survival/campaign/boss [dibulatkan], clawDamage semua varian+boss+monas, boomDamage, spawnInterval survival).
- **state.js** — status inti: `isPaused, isGameOver, score, mode, highScore, difficulty, godMode, dodgeInvuln` (+ setter `setPaused/setGameOver/setScore/addScore/setMode/setHighScore/setDifficulty/setGodMode/setDodgeInvuln` — **`godMode`** = cheat kebal player & Monas (toggle via konsol cheat; dibaca robots.js damage & survival `damageMonas`); **`dodgeInvuln`** = i-frame dodge (di-set player.js sepanjang animasi tumble; dibaca robots.js di kedua titik damage); high score per difficulty: kunci `gibsHighScore_<diff>`, kunci lama fallback normal); `stats` (kills/shots/hits — per PELURU; tanpa headshot sejak top-down) + `resetStats()`; `player` (hp/grenades/**medkits**/lastShot/speed/radius/vy/onGround; senjata rifle/pistol/shotgun = **`{ ammo }` saja — SISTEM MAGAZEN DIHAPUS 2026-07-11**: satu kolam peluru per senjata, kap `CFG.weapons.<w>.maxAmmo` [rifle 500/pistol 150/shotgun 300], mulai PENUH via `configurePlayer`, TANPA reload [`isReloading/reloadTimer/reloadDurMs` tinggal field dorman]; **`weapons:[]` = SLOT senjata berurutan, maks `CFG.weapons.maxWeapons` 3 [tombol 1/2/3], `owned:{pistol,rifle,shotgun,launcher}` DITURUNKAN dari `weapons` via `syncOwnedFromWeapons()`** — Survival mulai `['pistol']`, Campaign `['rifle','pistol']` (shotgun & **launcher** eksklusif shop Survival); senjata `rifle/pistol/shotgun/launcher = {ammo}`; `hasRadar` — Survival mulai TANPA radar [dibeli di shop], mode lain true; `medkits` 0 (item genggam maks 1); field dorman `dmgMul/reloadMul/upDmg/upReload` = 1/0; **`weaponLvl:{rifle,pistol,shotgun,launcher}` (2026-07-12) = level UPGRADE senjata shop Survival, 1..`CFG.weapons.maxWeaponLevel` [3] — dibaca `weaponDamage()` weapons.js [+25% damage base per level]; per-run, bertahan bila senjata diganti lalu dibeli lagi**; **KARAKTER 2026-07-13: `maxHp` EFEKTIF + `hpLvl` [item Vitality — SEMUA pembaca max HP memakai `player.maxHp`], `ammoLvl` [tier kap peluru — kap efektif via export **`maxAmmoFor(w)`**, dipakai drops/shop/hud], `armorLvl`/`armor`/`armorMax` [armor dikenakan: tier 0..3 + durability; damage player lewat `damagePlayerHp` robots.js]**) + `configurePlayer()` (stempel CFG; `weapons`/`owned`/`hasRadar`/`medkits`/`weaponLvl` [semua kembali 1] di-set per `mode`) + **`syncOwnedFromWeapons()`** (rebuild `owned` dari slot `weapons`); `keys, mouse`; array entitas `bullets, enemyBullets` (peluru robot ranged → player), `robots, grenades, explosions, drops`; resource bersama `GEO, MAT` (`MAT.enemyBullet` = bola plasma BIRU peluru robot); vektor scratch `_dir,_right,_tip,_v3,_sRight,_sUp,_kickEuler`; `clearArray(arr, scene)`.
- **dom.js** — semua ref elemen (`scoreText` [ANGKA skor saja — label "SCORE" statis di HTML], **modul amunisi 2026-07-10: `ammoWeapon, ammoCount, ammoMags, ammoHint, ammoBox`** [ganti `ammoText` lama], `healthFill, healthNum`, **bar ARMOR 2026-07-13: `armorRow, armorFill, armorNum`** (tampil hanya saat memakai armor), `waveText, blocker, gameOverScreen, finalScoreEl, bestScoreEl, gameOverTitle, crosshair, damageEl, staminaFill, stageMsgEl, radar, radarCtx`; **`invSlots`** = 4 tile inventori `{row, icon, count}` [1/2 senjata, 3 granat, 4 medkit; hud.js isi SVG ikon + hitungan]; **`medkitBar, medkitBarFill`** = bar channel medkit), `flashDamage()`, `showHitDir(relAngle)` (baji merah tepi layar diputar ke sudut penyerang — sudut dari `attackerAngle()` robots.js; SATU elemen `#hitDir` di-reuse), `showPickup(text,color)`, `showStageMsg(text,dur)`, `hideStageMsg()`, `initGrain()`, `showFatal(html)`.
- **renderer.js** — `scene, camera, viewCam, renderer, composer, bloomPass, fxaaPass, postFxOn, qualityTier, QUALITY[]`, **`SCREEN_UP`/`SCREEN_LEFT`** (basis layar bidang-tanah utk WASD relatif-layar, 2026-07-16), **`camFocusPos()`** (getter fokus kamera, uji recenter); `initRenderer()`, `onResize()` (aspek kedua kamera), `setFxaaRes()`, `applyQuality(t)`, `initQualityUI()`, `setQualityLightRef(dirLight)`, **`addCamShake(a)`/`resetCamShake()`** (guncangan kamera sinematik 2026-07-13: jitter posisi viewCam yang meluruh di `followViewCam`; dipakai runtuhnya Monas). **PIVOT TOP-DOWN (2026-07-11): `camera` = PIVOT LOGIKA player (posisi = titik mata, yaw = arah bidik kursor — di-set `updateTopdownAim`), BUKAN kamera render.** `viewCam` = kamera render top-down (FOV 50, anggota scene supaya anak warmup preload ikut dirender); `followViewCam(dt)` mengejar titik fokus **dead-zone** `camFocus` (bukan pivot langsung): fokus hanya bergeser saat pivot keluar kotak toleransi setengah-lebar `DEAD_X`/`DEAD_Z` (gerak kecil tak menggeser kamera, anti-pusing; **keduanya 16 sejak 2026-07-16 = kotak PERSEGI**, DEAD_X diturunkan dari 46 agar sama dgn DEAD_Z per permintaan user; **saat player BERHENTI [perpindahan pivot/frame < `MOVE_EPS2`] fokus di-EASE balik ke pivot → karakter kembali ke TENGAH, smoothing eksponensial `RECENTER_RATE` 3/dtk, halus bukan snap**, 2026-07-16; getter uji `camFocusPos()`; y ikut penuh; lompat >400 unit snap-recenter), lalu posisi = fokus + `CAM_OFF` (−70.7, 116, 70.7 — didekatkan bertahap 2026-07-11 [185/100 → 161/87 → 146/79 → 130/82]; lalu 130/82 → 116/100 = sudut diturunkan ~15% dengan JARAK dijaga ~154 unit tetap [y turun, z naik] → pitch ~57,8° → ~49,2°; lalu **2026-07-16 AZIMUTH diputar 45° dari selatan ke BARAT DAYA** — ofset horizontal dibagi rata x=−z, jarak horizontal ~100 & tinggi 116 dijaga → pitch/zoom tetap, hanya arah pandang berputar), lookAt fokus (pitch ~49° ala Alien Shooter). **Karena kamera kini memandang dari barat daya, atas layar = TIMUR LAUT dunia (bukan −z).** Basis layar bidang-tanah **`SCREEN_UP`/`SCREEN_LEFT`** (diekspor, diturunkan dari `CAM_OFF`) dipakai player.js agar WASD/dodge tetap RELATIF LAYAR (W=SCREEN_UP, A=SCREEN_LEFT). Radar tetap NORTH-locked (kompas). Dipanggil animate() SETELAH updateGame + sekali di startGame. Rantai composer: RenderPass(scene, **viewCam**) → UnrealBloom → Gamma → FXAA (pass viewmodel 2026-07-10 DIHAPUS bersama pivot).
- **sceneManager.js** — `activeScene`, `setScene(scene, opts)` (exit lama → enter baru).
- **saveGame.js** — save/checkpoint Campaign (2026-07-15): `saveCampaignStage(n)` (tulis nomor stage 1..4 ke `localStorage['gibsCampaignStage']`; dipanggil di AWAL `enter()` tiap stage campaign = checkpoint start-point), `loadCampaignStage()` (kembalikan 1..4 valid atau 0), `clearCampaignSave()` (New Game / MISSION COMPLETE). try/catch di setiap operasi (storage diblokir → abaikan, tak crash). HANYA nomor stage disimpan — continue = mulai SEGAR di START stage (loadout default, skor 0). Dibaca menu.js (prompt Continue) & startGame `opts.stage`; dihapus game.js `gameOver(won)`.
- **input.js** — `initInput()` (pointer lock + KURSOR BIDIK VIRTUAL top-down + klik + keyboard + blur + beforeunload). **Top-down (2026-07-11):** mouse-look DIHAPUS — delta mouse menggerakkan kursor virtual `#aimCursor` (dijepit layar, dipusatkan ulang & ditampilkan tiap pointer-lock); `updateTopdownAim()` (dipanggil animate SEBELUM updateGame): kursor → NDC → ray `viewCam` → potong bidang kaki player = ekspor `aimPoint` (Vector3 live) + set YAW pivot `camera` menghadapnya (semua sistem lama yang membaca `camera.getWorldDirection` otomatis membidik kursor). KLIK KIRI = tembak (`mouse.isDown`) / lempar granat ke kursor (jarak kursor > 120 unit → 'far', else 'near') / channel medkit; KLIK KANAN = `setMoveTarget(aimPoint)` (gerak ke titik; WASD keydown memanggil `clearMoveTarget`); ADS, jongkok (C/Ctrl), dan lompat (SPACE) DIHAPUS; **Shift = `tryDodge()` (dodge/evade, aksi diskret `!e.repeat`; SPRINT DIHAPUS 2026-07-11)** — SPACE tersisa utk restart game-over (+ shop lewat `shopKey`); `releaseInputs()` juga `clearMoveTarget()`. (Catatan lama yang masih berlaku: **tombol 1/2 = slot senjata, 3 = equip granat, Q = tukar slot senjata, 4 = `equipMedkit()`**; **mode medkit** (`medkitMode`): klik kiri ditahan = channel (jalur normal `mouse.isDown`, tembak sudah di-blok di weapons.js); **shop MODAL survival**: bila `activeScene.shopActive?.()` true, keydown menelan SEMUA tombol gameplay [hanya SPACE/Enter → `shopKey` = Start Next Wave] dan `pointerlockchange` yang unlock mem-`setPaused(true)` TANPA menampilkan blocker [pointer sengaja dilepas agar kursor bisa klik menu]; game render tetap jalan di belakang), `requestLock()` (dipanggil ulang oleh `startNextWave` untuk resume), `enterImmersive()` (fullscreen + Keyboard Lock `LOCK_KEYS`, tanpa Escape), `releaseInputs()` (TIDAK menutup shop — shop modal ditutup hanya oleh Start Next Wave / enter ulang scene). **Menu jeda ESC**: flag `hasStarted` (true sejak lock pertama) → `pointerlockchange` unlock di tengah main (bukan shop/gameover) memanggil `showPauseMenu` (`pauseMenu.js`); lock/resume memanggil `hidePauseMenu`. **Konsol cheat**: `e.code==='Backquote'` toggle `cheatConsole` (buka hanya saat pointer ter-lock); saat terbuka, tombol non-backtick di-route ke `handleKey(e)` (ketikan perintah) & unlock/ESC memanggil `forceHideCheatConsole`.
- **hud.js** — `updateUI()` (skor [angka saja] + **modul amunisi** [nama senjata/item **+ suffix LEVEL upgrade " II"/" III" (2026-07-13; Lv1 polos)** + hitungan besar `#ammoCount` + `/ maxAmmo` **EFEKTIF via `maxAmmoFor` (tier Ammo Capacity)** + baris petunjuk `#ammoHint`; grenadeMode/medkitMode menampilkan hitungan item itu; kelas CSS `reloading` tak pernah dipakai lagi] + health bar merah [JS menulis LEBAR + angka `#healthNum` + kelas `low` saat HP ≤ 25% → pulse CSS; WARNA tetap CSS-only; **maks = `player.maxHp` EFEKTIF (item Vitality), bukan CFG**] + **bar ARMOR (2026-07-13: `#armorRow`/`#armorFill`/`#armorNum` — tampil hanya saat `player.armorLvl > 0`, lebar = durability/armorMax, warna baja kebiruan CSS)** + `activeScene.hudStatus()`; juga sembunyikan/tampilkan kanvas `#radar` per `player.hasRadar`; memanggil **`updateInventory()`** = baris 4 tile IKON di tumpukan `#statsHud` kanan-bawah [`invSlots` = `{row, icon, count}`]: tiap tile isi SVG dari **`ITEM_ICONS`** [pistol/rifle/shotgun per `player.weapons`, grenade, medkit — buatan sendiri, BUKAN emoji/teks; `currentColor` ikut warna tile], nomor hotkey `.invKey`, badge hitungan `.invCount` [granat/medkit]; tile aktif `.active` [senjata terpegang / grenadeMode / medkitMode] emas & menonjol, kosong/0 `.dim`; ikon ditulis ulang hanya saat key berubah [cache `_iconKey`]); `radarProject(dx, dz, fx, fz, R, range)` (proyeksi generik ke frame (fx,fz) = arah "atas" radar; sejak 2026-07-16 `drawRadar` memanggilnya dgn frame **SEJAJAR LAYAR** `fx,fz = SCREEN_UP` [dulu utara-tetap `fx=0,fz=-1`]); `drawRadar()` (skip bila `!player.hasRadar`; radar kiri-atas **SEJAJAR LAYAR sejak 2026-07-16** [permintaan user, gantikan north-lock 2026-07-11]: atas radar = atas layar [karena kamera barat daya, utara dunia tampil serong] → blip sebidang dgn tampilan; peta TETAP tak berputar mengikuti kursor [frame = orientasi kamera tetap]; penanda N bergeser ke arah utara SEBENARNYA [serong kiri-atas] via rumus `(-fx,fz)·(R-9)`; hanya panah player + kerucut FOV yang diputar `aimAngle = atan2(dir.x·-fz + dir.z·fx, dir.x·fx + dir.z·fz)` [umum thd frame] menampilkan arah bidik; latar gelap polos + cincin jarak + silang + blip glow; latar gradien & sweep berputar DIHAPUS 2026-07-05 — jangan ditambahkan lagi; landmark `clampEdge` dijepit ke tepi; robot & drops di-skip di luar jangkauan).
- **pauseMenu.js** — menu jeda overlay di dalam `#blocker` (2026-07-09, mode apa pun): `showPauseMenu()` (tampilkan `#pauseMenu` + sembunyikan `#instructions`) / `hidePauseMenu()` (sebaliknya, dipanggil saat resume) / `isPauseMenuOpen()` (dipakai input.js: selagi menu jeda terbuka, klik latar `#blocker` DIABAIKAN — resume HANYA via tombol **RESUME** → `requestLock()`; hint "click anywhere" dihapus 2026-07-10). Dua aksi berkonfirmasi **Yes/No**: **RESTART GAME** → `resetGame` (ulang dari awal), **EXIT GAME** → `location.reload()` (kembali ke `#mainMenu` — layar pertama pasca-reload; `startGame` sekali-jalan → reload = jalan paling bersih). **TUTORIAL** (2026-07-15) → `showTutorial()` tampilkan panel `#pauseTutorial-panel` (How to Play = key-mapping `.keyGrid` yang sama dengan kartu Start), Back (`#pauseTutBack`) → `showMain`. `showMain()` menyembunyikan panel konfirmasi DAN tutorial. Klik di dalam kotak menu `stopPropagation`. `wire()` lazy sekali; elemen `#pauseMain`/`#pauseConfirm`/`#pauseConfirmText`/`#pauseTutorial`(tombol)/`#pauseTutorial-panel`/`#pauseTutBack`/`#pauseResume`/`#pauseRestart`/`#pauseExit`/`#pauseYes`/`#pauseNo`. Impor sirkular pauseMenu↔input (requestLock) aman — dipakai hanya di dalam handler.
- **cheatConsole.js** — konsol cheat overlay (`#cheatConsole`, tombol `` ` ``, 2026-07-09): `openCheatConsole()` (tampil + `setPaused(true)`) / `closeCheatConsole()` (sembunyi + resume) / `forceHideCheatConsole()` (sembunyi tanpa resume — dipakai input.js saat unlock/ESC) / `isCheatConsoleOpen()` / **`handleKey(e)`** (bangun string perintah dari keydown — Enter/Backspace/karakter; dipanggil input.js selama konsol terbuka, TAK andalkan fokus `<input>` supaya kebal Keyboard Lock). Perintah **`god-mode`** + Enter = TOGGLE `godMode` (state.js) → player & Monas kebal; **`more-money`** + Enter = `addScore(100000)` + `updateUI()` (mata uang shop Survival; bisa diulang); **`skip-to-wave-N`** + Enter (regex `^skip-to-wave-\d+$`) = LOMPAT ke wave N via `activeScene.cheatSkipToWave(N)` (hanya `survivalScene` yang punya hook itu → mode lain balas "only works in Survival mode"; N di-clamp ≥ 1) — hook membuang robot lapangan (dispose senyap, tanpa skor/gore), `endEvent()`+`closeShop()`, lalu `startWave(N)` sehingga jumlah/cap/interval/peluang kabut sesuai formula wave N; **`skip-to-stage-N`** + Enter (2026-07-14, regex `^skip-to-stage-\d+$`, N=1–4) = LOMPAT ke stage campaign via `activeScene.cheatSkipToStage(N)` (keempat stage campaign punya hook → mode lain balas "only works in Campaign mode") → `campaignJumpToStage(N)` (transition.js) buang SEMUA robot + entitas transien lalu `setScene(stageN)` (enter bangun dunia + tempat robot + posisi player; stage 2 spesial → helper panggil `placeStage2Robots`); invalid → null. Buka HANYA saat pointer ter-lock (main aktif).
- **game.js** — `updateGame(dt, step, T)` (urutan kontrak; **selama sekuens kematian player, blok kendali player [timer senjata/gerak/tembak] + `updateMode` + `checkWin` DILEWATI — dunia lain (darah/gib/robot/peluru) tetap jalan**), **`startPlayerDeath(dirx?, dirz?)`/`isPlayerDying()`** (2026-07-12, revisi "mati biasa" — dipanggil KETIGA titik damage player di robots.js menggantikan `gameOver` langsung, arah = dorongan hit pembunuh: HP=0, `releaseInputs`+`hideMoveMarker`, avatar TETAP TAMPIL dan ROBOH via `playAvatarDeath(dx,dz)` [playerAvatar.js] + satu semburan darah merah sedang + genangan decal merah `0x8f1616` di titik jatuh & arah rebah — TANPA gib/ledakan/sfx ledak; hitung mundur `CFG.player.deathDelaySec` [2 dtk] di updateGame → baru `gameOver(false)`; keruntuhan Monas TETAP mengakhiri game seketika), `gameOver(won, title?)` (English: MISSION COMPLETE / GAME OVER / judul kustom scene mis. THE MONUMENT HAS FALLEN; mengisi `#goStats` dari `stats`; **`won` (MISSION COMPLETE) → `clearCampaignSave()`** = campaign tamat, mulai baru; **PROMPT game-over 2026-07-15: `wireGameOverButtons()` (lazy) merangkai `#goRestart` → `resetGame(true)` [label "RESTART STAGE" campaign / "RESTART" survival] & `#goExit` → `location.reload()` [ke `#mainMenu`]**), `resetGame(atCurrentStage=false)` (reset skor+stats+player/senjata/entitas + `resetRobotsFx()` + **batalkan sekuens kematian via `resetAvatarDeath()` (bangkit dari pose roboh)** → titik restart → `requestLock`; **`atCurrentStage` [prompt/SPACE game-over]: campaign ULANG dari AWAL stage CHECKPOINT via `campaignJumpToStage(loadCampaignStage()||1)` — dunia sudah terbangun selama main, ia setScene+tempatkan robot stage itu [stage 2 khusus]; selain itu [pause "RESTART GAME"] pakai `restartScene()`: survival di tempat, campaign stage 1**).
- **preload.js** — layar loading pra-game + pemanasan renderer (2026-07-10; disesuaikan pivot top-down 2026-07-11): `showLoading()`/`hideLoading()` (`#loadingScreen`, overlay OPAK z-40), `loadingStep(pct, label)` (bar `#loadingBarFill` + `#loadingText`, lalu `await` rAF+setTimeout(0) = jamin browser MELUKIS overlay sebelum kerja sinkron berikutnya), `warmupAll()` — Three.js mengompilasi shader material hanya saat objeknya PERTAMA dirender & mengunggah tekstur saat draw pertama. Warmup: grup anak-**VIEWCAM** (pasti masuk frustum) berisi 1 contoh tiap visual spawn-nanti (tracer `GEO.bullet`, granat dunia, magazen, medkit, trio ledakan + cincin debu [material per-instance ala `explodeAt`, di-dispose setelahnya], 1 robot kelas penembak [`buildRobotMesh('B')` → `disposeRobot`; lengan meriam + visor/inti emisif = jalur Lambert-emissive ikut hangat], sprite coolant pinjaman `borrowBloodSprite`) + `renderer.compile(scene, viewCam)` + 3 frame render nyata (jalur sama dgn animate → render target bloom/FXAA ikut hangat; avatar player & dunia ikut hangat — rig FPS tersembunyi tak pernah dirender jadi tak perlu), lalu SEMUA dikembalikan (sprite ke scene, dispose hanya material buatan warmup — resource bersama JANGAN). Dipanggil startGame SEBELUM `animate()`.

### src/utils/
- **math.js** — `rand(a,b)`, `clamp(v,lo,hi)`, `smooth01(u)`, `segPointDist2(ax..pz)` (sweep peluru).
- **textures.js** — `makeTexture(w,h,draw,repX,repY)` (sRGB), `speckle(g,w,h,colors,n,sMin,sMax)`, `makeNormalMap(w,h,drawHeight,strength)` (linear), `noiseHeight(base,jitter,n,sMin,sMax)`.
- **sfx.js** — semua klip (`sfxShoot, sfxShotgun, sfxEmpty, sfxSwitch, sfxPistol, sfxExplode, sfxReload, sfxHit, sfxPickup, sfxPurchase, sfxMelee, sfxThrow, sfxNadeRoll, sfxRobotBite, sfxFootstep, sfxRobotStep`) + `playSFX(sfx, vol)` (pool 8 node per klip — selalu lewat ini; **mengembalikan node yang diputar agar klip panjang [reload] bisa di-pause saat dibatalkan**). Suara senjata: `sfxShoot` rifle / `sfxPistol` pistol / **`sfxShotgun` shotgun** (dipilih di `updateShooting`), **`sfxSwitch` saat `startSwitch`** (ganti senjata), **`sfxEmpty` klik kosong** saat pelatuk ditarik & kolam peluru senjata habis (sekali per tarikan via `emptyReady`; `sfxReload` dorman — reload dihapus 2026-07-11), **`sfxPurchase` saat sukses beli item shop** (`shopPurchase`/`shopReplaceWeapon`). **`preloadAllSFX()`** (2026-07-10) — dipanggil layar loading pra-game, DUA tahap: `load()` semua klip (fetch) + **PRIME** = putar tiap klip sekali volume 0 lewat pool `playSFX` lalu hentikan ~400 ms kemudian (decoder + perangkat audio OS + node pool hidup SAAT loading; `load()` saja tidak memaksa decode — play perdana `sfxSwitch` di equip granat pertama dulunya masih tersendat).
- **collision.js** — `slideWalk(walkableFn, pos, oldX, oldZ, r)` (menyusur dinding per-sumbu), `resolveBlockers(pos, r, feetY, blockers)` (balok rotated-AABB `{x,z,hx,hz,axx,axz,azx,azz,rad,top,standable}`; return true bila kena balok standable), `blockersGroundHeight(x, z, feetY, blockers)`, `resolveCylinders(pos, r, cylinders)`.
- **pathfind.js** — pathfinder robot (2026-07-06): `makeNavGrid(x0, z0, cell, cols, rows, sample)` (bake Uint8Array, sample dipanggil di pusat sel), `gridLOS(grid, x1, z1, x2, z2, rad?)` (garis-pandang grid setebal badan robot), `findPath(grid, sx, sz, tx, tz)` (A* 8-arah + anti potong-sudut + string-pulling; null = tak tercapai/budget habis), `navAim(z, grid, tx, tz, dt, step)` (steering per-frame; return objek BERSAMA `{x, z, direct}` — jangan disimpan. LOS dicek TIAP frame: keputusan lurus-vs-path instan tanpa jeda timer, path dibuang begitu player terlihat; hanya findPath yang di-rate-limit `repathSec` + dipaksa saat macet `stuckSec`; maju waypoint juga saat waypoint berikutnya terlihat = string-pull runtime; status di `z.nav*`), `turnToward(z, desired, dt)` (heading laju-putar `turnRadPerSec` di `z.navHead` — gerak robot memakai sudut ini agar menikung mulus). Grid null = seluruhnya nonaktif.

### src/world/ (bersama antar scene)
- **palette.js** — **PANDUAN GAYA VISUAL "GIBS 2045" (2026-07-16, patokan tunggal art-style: "Jakarta 2045, agak futuristis tapi tidak terlalu")**: ekspor `PAL` (token warna — dasar `ink/gunmetal/steel/panel/concrete/rubber/wood/leaf`, aksen teknologi sipil `tech/techDim/screenBg` [teal pudar — SATU-SATUNYA emissive lingkungan], aksen manusia `amber/amberDim/hazard/white` [amber senada HUD; merah-bata+putih = sentuhan merah-putih]), `EMISSIVE_MAX` (0.9 — plafon emissive lingkungan; efek tempur bebas), `RESERVED_HEX` (warna sinyal gameplay yang dilarang utk dekorasi: mata robot merah, EXIT hijau — plus armor kelas C/B/A, coolant, darah player, plasma musuh), `FORBIDDEN_HEX` (neon terlarang: cyan `0x00ffff` & magenta `0xff00ff` — juga dilarang: hitam murni, biru-hitam dingin, neon underglow). Murni konstanta (tanpa THREE) — aman diimpor modul mana pun; aturan lengkap di header file. **Ditegakkan smoke test** (sweep material semua prop futuristik + kendaraan). Semua aset visual baru WAJIB memakai token PAL.
- **lighting.js** — `ambLight, hemiLight, dirLight, rimLight`; `createBaseLights(scene)`; `LIGHT_PRESETS` (`outdoor`/`indoor`/`night` [stage 3]) + `applyLightPreset(scene, name)` (uniform saja, tanpa recompile); `updateShadowFollow(camera)`.
- **decor.js** — registri animasi dekoratif (jalan juga saat pause): `waterJets[], fireSprites[]`, setter `setFlameLight/setFlameGlow/setBurningMat/setWaterTex/setSkyDome/setS1FlickerLight`, `updateWorldDecor(dt, T, camera)`.
- **sky.js** — `createSky(scene)` (kubah+bulan+halo, ikut player via decor), `createEmbers(scene)`, `updateEmbers(dt, T, camera)` (partikel bara/abu di KOORDINAT DUNIA absolut; dibungkus toroidal ±350 sekitar player via modulo — TIDAK menempel kaku ke player, top-down fix 2026-07-11: dulu `pts.position=camera.position` tiap frame tampak "salju" mengikuti player).
- **facades.js** — resep gedung bersama (survival city & campaign): `CITY_PALETTE`, `makeFacadeTex()`, `makeLitTex()`, `makeCityMat(facade,lit)`, `makeBurningCityMat(facade)` (auto-daftar ke decor), `fillBuildingInstances(scene, list, mat)` (list `{x,z,w,d,h,ry,rz,color}`), `addFireSprites(scene, burnList)`.

### src/entities/ (sistem bersama)
- **player.js** — live: `stamina, staExhausted, dodgeActive, dodgeProgress, dodgeDirX, dodgeDirZ`, `eyeHCur` (konstan `eyeHeight`); `drainStamina(n)` (kuras + set `staExhausted` bila 0), **`tryDodge()`** (dipanggil input.js saat tekan Shift — DODGE/evade: gate cooldown/stamina/exhausted/medkit, arah = WASD bila ditekan else MUNDUR lawan hadap bidik, set i-frame `setDodgeInvuln(true)`), `setMoveTarget(x, z)`/`clearMoveTarget()` (**gerak klik-kanan top-down**: lurus ke titik + marker cincin playerAvatar; batal oleh WASD / tiba <3.5 u / macet 1.2 dtk / releaseInputs), `resetPlayerState()`, `updatePlayerMovement(dt, step)` (**top-down 2026-07-11: WASD = sumbu layar [W=−z, A=−x]; KECEPATAN DIREKSIONAL 2026-07-12: `dirSpeedMul` — penuh searah kursor, ×strafe/backpedalWeight (0.5) menyamping/mundur, blend halus via dot; berlaku utk WASD & klik-kanan; SPRINT DIHAPUS 2026-07-11**; **saat `dodgeActive`: guling ke `dodgeDir` speed meluruh, override WASD/klik-kanan, dodgeProgress 0..1 + i-frame mati di ujung**; stamina hanya regen+pulih, bar, dorongan badan robot ZBODY, `activeScene.playerCollide/groundHeight` pada POSISI PIVOT `camera`, gravitasi + jatuh dari tepian [**lompat & jongkok DIHAPUS 2026-07-11**], langkah kaki + langkah robot global; **saat `medkitMode`: gerak ×`CFG.player.medkitSlowMul`, dodge diblok**).
- **playerAvatar.js** — **OVERHAUL 2026-07-12; OVERHAUL BENTUK KARAKTER 2026-07-14**: `initPlayerAvatar(scene)` (**"resistance ranger" berkarakter — dirombak 2026-07-14 agar tak lagi seperti placeholder** [geometri PURELY ADDITIVE — semua pivot/anchor `SHOULDER`/`GUN_OFF`/`GRIPS`/`TIPS`/`armorNodes`/pivot kaki/`avatarGunTip` TETAP]: helm + rel samping + dudukan NVG dahi + strap dagu + HEADSET dgn boom mic melengkung; GOGGLE lensa amber + masker kain wajah; SYAL/shemagh amber khas [identitas, senada aksen HUD]; CHEST RIG penuh [3 kantong magasin, pouch admin/radio + ANTENA whip vertikal, granat, name-tape amber] + tali harness diagonal; ransel + kantong sisi + tutup + tali kompresi + matras + antena panjang; sabuk utilitas [gesper/pouch/kantin]; HOLSTER paha kanan berpistol menyembul; celana kargo + pelindung lutut berkontur + gaiter + boot bersol; SARUNG TANGAN taktis [grup `handL/R`] + BANTALAN SIKU [`elbowL/R`, ditempatkan ke titik siku fake-IK di `placeArm`]; material identitas: `accent`/`accentDk` amber, `glove`/`rubber`/`metal`/`glass`[lensa]/`cloth`; **BADAN TERBELAH DUA (2026-07-12): root `avatarGroup` = badan BAWAH [kaki+sabuk; yaw = arah GERAK] + `upperG` = badan ATAS [torso/lengan/senjata/pedang/swoosh; yaw lokal = puntiran pinggang ke kursor, dijepit ±`MAX_TWIST` 1.05 rad] + `headG` di dalamnya [toleh kepala ekstra ±`HEAD_TWIST` 0.62, menoleh LEBIH DULU] — konstanta koordinat lama tetap berlaku karena upperG/headG di origin root**; kaki ber-pivot pinggul+LUTUT [`hipL/R`, `kneeL/R`], lengan ber-pivot bahu+SIKU [`armL/R`, `elbowL/R`]; **`avatarGunTip` tetap di ofset kalibrasi — gunGrp (0.65,7.5,1.2), tip (0,0.15,4.5) — titik spawn peluru tak boleh bergeser** (SATU pengecualian sengaja 2026-07-12: peta `TIPS` — prop `launcher3` memindah tip ke moncong tabung bahu (0.95,2.45,2.6) supaya roket & kilat keluar dari tabungnya; hit test 2D/xz → netral gameplay, prop lain tetap default); dipanggil startGame SEBELUM `initWeapons` karena muzzle flash di-parent ke **`avatarGunTip`**). **PROP PER-SLOT (2026-07-12): SEMBILAN grup prop — lima dasar: rifle (receiver+laras+magasin+popor), pistol (kecil, dipegang menjulur), shotgun (laras+POMPA+popor kayu), launcher (tabung 40mm GENDUT), medkit (kotak putih palang merah, dipegang rendah) + EMPAT VARIAN LEVEL 3 (2026-07-12, tampil saat `player.weaponLvl[w] >= 3`): `pistol3` DESERT EAGLE krom besar (slide slab + celah ventilasi moncong), `shotgun3` combat shotgun panjang (rusuk pelindung panas + sadel peluru merah), `rifle3` GATLING 6-laras (rumah rotor + kotak amunisi + foregrip vertikal), `launcher3` ROCKET LAUNCHER olive gaya AT4 DI ATAS BAHU KANAN (pita kuning + corong exhaust; kedua tangan menggapai grip bawah tabung) — anchor genggam sendiri di `GRIPS.<w>3`, config recoil tetap dibaca dari senjata DASAR** — dibangun sekali di gunGrp, ditampilkan per `currentWeapon`/`medkitMode`/level (cache `propKey`). **OVERLAY ARMOR (2026-07-13): tiga set pelat KUMULATIF mengikuti `player.armorLvl` per frame (cache `armorKey`, diperiksa SEBELUM cabang mati supaya armor yang pecah pada pukulan mematikan tetap lenyap dari jasad) — Set 1: pelat dada + tutup bahu + pelindung paha; Set 2: bibir pauldron besar + pelat punggung + pelat sabuk + pelindung tulang kering + alis helm; Set 3: kerah leher + jalur dada & trim pauldron MERAH + pelat pipi helm + JAMBUL crest merah; pelat kaki di-parent pivot pinggul/lutut (ikut melangkah), semua Phong (tanpa shader baru, ikut warmup); pecah → pecahan pelat via gib (breakPlayerArmor robots.js).** **TANGAN-DI-SENJATA (2026-07-12, menggantikan lengan FK `ARM_POSE`): tiap prop punya ANCHOR GENGGAM (`GRIPS` — kanan di pelatuk/grip, kiri di forend/pump/tabung/sisi medkit); telapak DIPOSISIKAN TEPAT di anchor prop aktif tiap frame + lengan digambar 2 SEGMEN TERTARIK (`placeArm`/`placeSeg`: bahu→siku fake-IK [melorot sesuai kekenduran]→telapak) — senjata benar-benar dipegang, tak melayang di dada; dodge = tangan TUCK ke dada, melee = tangan kanan mengikuti gagang pedang; **RECOIL: prop aktif di-pitch naik `-gunRecoil×cameraKick×6` di pangkalnya (gunGrp/avatarGunTip TIDAK ikut — kalibrasi aman) + titik genggam DIPUTAR pitch yang sama → TANGAN & lengan ikut hentakan (tangan depan naik paling terasa)**. `updatePlayerAvatar(dt)` (dipanggil animate SETELAH updateGame: berdiri di kaki pivot; **RANTAI HADAP MANUSIAWI (2026-07-12, menggantikan `lookAt` seluruh badan): kaki menghadap ARAH GERAK [rate-limited `approachAngle`], puntiran pinggang `upperG` menutup sisa sudut ke kursor [±MAX_TWIST], kepala menoleh PALING DULU [rate tercepat + toleh ekstra]; gerak menjauhi bidikan >~110° = BACKPEDAL [kaki tetap di sisi bidikan, fase gait DIPUTAR MUNDUR = benar melangkah mundur, histeresis 1.75/1.92 rad]; diam + puntiran >0.92 rad = kaki MENYERET menyesuaikan hadap (turn-in-place ber-shuffle); GAIT TERARAH: komponen gerak sejajar-kaki = ayunan pinggul X, komponen menyamping = kedua pinggul buka-tutup `rotation.z` [side-shuffle nyata] + bob halus `upperG.position.y`; melee/dodge = seluruh badan cepat lurus ke bidikan**; ayunan kaki menurut kecepatan horizontal NYATA; **SALTO dodge 2026-07-12: rotasi 360° smoothstep di sekitar pivot PINGGANG [`PIV` 5.2 — origin kaki diayun mengelilingi pusat massa] + busur lompatan kecil + TUCK kaki/lengan di udara [envelope sinus, dilepas sebelum mendarat]**; **SABETAN PEDANG melee 2026-07-12 (dilincahkan): selama `meleeT > 0` [weapons.js, + `MELEE_TIME` diekspor] prop senjata disembunyikan & `swordPivot` [bahu kanan, bilah +Z, euler 'YXZ'] memainkan 3 FASE — ancang easeOut (badan memuntir kanan) → TEBAS easeIn-kubik busur ~245° (TERJANGAN maju `translateZ` + puntiran pinggang `rotateY` + merendah + kipas JEJAK swoosh `RingGeometry` yang membuntuti bilah, opacity ~ kecepatan) → recovery smoothstep; TANGAN KANAN mengikuti gagang (euler manual), kiri mengayun berlawanan, kaki kuda-kuda (`meleeDip`); dinamika badan visual-only**), **IDLE AFK BERTAHAP (2026-07-14): player DIAM TOTAL & tak ada ancaman (semua reset bila `moving`/`inMelee`/`dodgeActive`/`medkitMode`/`gunRecoil>0.05`/`switchAnim>=0`/kursor `aimPoint` bergerak/ada robot `state==='chasing'||'jumping'`/`isPaused`/tanpa `aimPoint`) -> `afkT` bertambah; ambang `AFK_WAVE` 30 / `AFK_CROUCH` 60 / `AFK_LIE` 90 dtk: WAVE (berbalik ke `viewCam` sambil tangan kanan MELAMBAI, senjata diturunkan di tangan kiri; sekali ~5 dtk lalu normal) -> CROUCH (badan merendah, lutut menekuk, sesekali MENGINTIP kamera ~tiap 6.5 dtk, senjata dipegang normal) -> LIE (telentang tumbang di pivot kaki, tangan di belakang kepala, SENJATA digeletakkan rata di samping via `gunGrpRef`); keluar AFK = kembalikan `gunGrp` ke `GUN_OFF` + luruhkan tunduk kepala; state via `afkDebug()` (utk smoke). **PENTING — tunduk/dongak kepala (WAVE mendongak, CROUCH mengintip, LIE dagu ke dada) WAJIB lewat `lerpHeadPitch()`, BUKAN set `headG.rotation.x` langsung: origin `headG` ada di kaki (y≈0) sedang mesh kepala di y≈10.6, jadi rotasi X mentah melempar kepala ~5 unit ke belakang — `lerpHeadPitch` mengompensasi `headG.position` agar berporos di leher (`HEAD_PIVOT_Y` 9.9)**; LIE = TERLENTANG (tumbang mundur `avatarGroup.rotation.x`, wajah ke atas, tangan di belakang kepala `placeArm` lokal (±1.05, 10.95, −0.9)). **PENTING — RANTAI HADAP (aim chain: legYaw/twist/head) berjalan SETELAH blok AFK (bila AFK aktif ia `return` duluan): kalau aim chain jalan DULU, ia menarik `legYaw` ke kursor & "berkelahi" dgn AFK -> badan terlentang miring salah + kaki bergoyang seperti gagal menata; jangan kembalikan urutannya**)**, `showMoveMarker(x, y, z)`/`hideMoveMarker()` (cincin denyut klik-kanan; dipakai player.js), **`playAvatarDeath(dirx, dirz)`/`resetAvatarDeath()`** (reset juga membatalkan AFK + kembalikan senjata) (2026-07-12 — animasi MATI "biasa" dipicu `startPlayerDeath` game.js: tubuh ROBOH 90° easeIn di pivot kaki ke arah dorongan hit, prop senjata/pedang lenyap dari tangan, pinggang/kepala mengendur + kaki menekuk lemas asimetris + lengan terkulai di sisi badan [cabang mati early-return sebelum rantai hadap]; reset = bangkit + prop dievaluasi ulang). Material Phong polos = tanpa program shader baru. Impor sirkular playerAvatar↔player/input/**weapons** (baca `currentWeapon`/`medkitMode`) aman (dibaca di dalam fungsi).
- **weapons.js** — live: `currentWeapon, isAiming, switchAnim, meleeT, gunRecoil` (1 saat menembak → meluruh dt·6; dibaca playerAvatar utk hentakan laras naik) (+ konstanta **`MELEE_TIME`** 0.45 — dibaca playerAvatar utk animasi sabetan pedang)`, grenadeMode` (**DORMAN, selalu false sejak 2026-07-11 — granat lempar diganti weapon Grenade Launcher; disimpan agar guard lama `if (grenadeMode||medkitMode)` tetap benar**)`, medkitMode`; **`WEAPON_DEF`** (tabel definisi **EMPAT** senjata — rifle/pistol/shotgun/**launcher**: name/hipX/hipY/adsY/baseZ/kick/muzzle/muzzleScale/kfBase + ref mesh & muzzlePoint diisi initWeapons; senjata baru = tambah baris + model + WEAPON_DEF.<w>.mesh/muzzlePoint); `initWeapons()` (model+tangan, parent kamera, `scene.add(camera)`; rifle = Pindad SS2-V2; shotgun = pump-action; **launcher = laras gemuk 40mm + receiver + grip + popor** [model sederhana, rig FPS tersembunyi]), `attachMuzzle(wpn)` (def-driven), `startSwitch(t)`, **`trySwitchKey(k)`** (1/2/3 = slot senjata 0/1/2 [`player.weapons`, maks 3], **Q = putar ke slot terisi berikutnya**; slot kosong diabaikan; **dari medkit ke senjata SELALU via animasi angkat `startSwitch`**), **`refreshOwnedWeapon()`** (setelah beli/ganti di shop: pastikan `currentWeapon`/`lastWeapon` masih dimiliki), **`equipMedkit()`** (mode medkit tombol 4: pegang medkit [`medkitHandMesh`], tekan lagi = holster; TAHAN klik kiri `CFG.player.medkitUseSec` detik = channel -> `finishMedkit` sembuh 70% + `holsterMedkit`; lepas klik = batal; **holster/finish MENGANGKAT senjata via `startSwitch`**), `pickStartWeapon()/applyStartLoadout()` (senjata aktif awal = `player.weapons[0]`; dipakai initWeapons + resetWeapons), **`startReload()` = NO-OP (magazen+reload DIHAPUS 2026-07-11)**, `tryMelee()`, `toggleAim()` (dorman — ADS dihapus), `setAiming(v)`, `doMeleeHit()` (**SAPUAN BUSUR kerucut depan ~±70° searah `meleeDir`: robot dalam `melee.range` + `bodyHitRadius×scl` DAN di dalam kerucut kena `CFG.melee.damage` [150] − armor; mati → `killRobot(cause:'melee')` = bangkai TERBELAH DUA + spawnDrop; selamat [boss] → muncrat coolant + terbangun bila idle; gore/darah searah `meleeDir`; loop mundur aman splice**), **`meleeDirX`/`meleeDirZ`** (live, arah tebasan) + `pickMeleeDir()` internal (2026-07-16: `tryMelee` AUTO-pilih arah ke robot terjangkau TERDEKAT [character otomatis MENGHADAP robot yg menempel walau kursor di arah lain] atau ke kursor bila tak ada; playerAvatar override `aimYaw` ke `meleeDir` saat `meleeT>0` → badan berputar menebas), `updateWeaponTimers(dt)` (switch swap di tengah + **channel medkit**), `updateWeaponState(dt)`, `updateShooting()` (spread horizontal + spawn peluru; **damage per peluru distempel `weaponDamage(w)`** — export 2026-07-12: damage base CFG × level upgrade shop [`player.weaponLvl`, tiap level +`upgradeDamagePct` 25% dari base]; `pellets` [shotgun 10]; **`launcher` = 1 peluru MELEDAK-saat-kena: mesh Mk2 `buildGrenadeMesh(0.7)`, `roundSpeed`, flag `explosive`/`explodeR`**; `stats.shots` per peluru; segmen frame-1 dari MATA; **kolam peluru 0 -> klik-kosong `sfxEmpty`**), `updateWeaponVisuals(dt)` (ADS/FOV/bob; 3 cabang rig reload dorman; melee; **rig tangan medkit + bar `#medkitBar`**), `resetWeapons()`, `placeLimb(...)`. **TOP-DOWN (2026-07-11):** semua rig viewmodel di-parent ke **`fpsHolder`** (anak `camera` pivot, `visible=false` PERMANEN — kebal tulisan `mesh.visible=true` startSwitch/applyStartLoadout pada anak); `attachMuzzle(wpn)` selalu menunjuk `muzzlePoint` ke **`avatarGunTip`** (playerAvatar.js — kilat muzzle PointLight + sprite kilat REBAH menghadap atas permanen di sana; hanya `muzzleScale` per senjata yang diterapkan); sebar peluru HORIZONTAL saja (komponen vertikal dihapus — kipas pelet shotgun menyamping); tendangan kamera (`_kickEuler`) DIHAPUS; `setAllRigsVisible` DIHAPUS (rig tak pernah dirender). **Overhaul bentuk model 2026-07-10 (visual saja — SEMUA titik muzzle, posisi senjata, dan tinggi pisir ADS tetap di koordinat kalibrasi lama):** rifle = receiver atas/bawah terpisah + magazen KURVA + port ejeksi/selektor/pelatuk/bantalan popor; pistol = slide komposit (serrasi, port, PISIR IKUT SLIDE di ofset absolut sama), lingkar pelindung pelatuk utuh, hammer, panel grip; shotgun = rib bidik, klem laras, pump berrusuk, popor komb + bantalan. **`pistolSlide`/`pistolMagMesh`/`sgPump`/`sgMagMesh` kini THREE.Group** (rig hanya menulis `.position`/`.rotation` — kontrak animasi tak berubah, pertahankan). **Helper `wrapFinger`** (dalam initWeapons): jari = rangkaian ruas kotak tangensial busur yang benar-benar membungkus handguard/pump/grip; param opsional `tipMat` = material 2 ruas ujung (kulit — sarung tangan tanpa jari). **Tangan kiri rifle/shotgun = grip OVERHAND mengikuti referensi CS dari user (2026-07-10): punggung tangan menghadap kamera di sisi dekat, empat jari MENYAMPIR DI ATAS handguard/pump dekat→jauh, ibu jari menyelip sisi dekat-bawah ke arah muzzle, pergelangan kulit telanjang — jangan dikembalikan ke genggaman bawah.** Ujung placeLimb lengan kiri di updateWeaponVisuals mengikuti ofset pergelangan overhand (−0.45, −0.75/−0.65, +0.8 dari grup tangan); support hand pistol thumbs-forward. **Rig medkit**: baki + tutup berengsel `mkLid` (terbuka saat channel, isi: gulungan perban + vial merah dgn `MEDKIT_MAT` bersama) + tangan kanan penekan `mkWorkHand` yang memompa berirama selama channel (kotak ikut mengayun); di-reset saat `equipMedkit`.
- **bullets.js** — `updateBullets(step)` (maju, catat px/py/pz sweep, mati umur/`activeScene.bulletBlocked`; **peluru Grenade Launcher [`b.explosive`] yang menghantam dinding/Monas [`hitWall`] → `queueBoom(...,b.explodeR,false,0,b.damage)` [damage ber-level upgrade ikut ke boom] sebelum dibuang [ledakan diproses setelah loop robot]; habis-umur di ruang kosong hanya lenyap**). **BATAS KURSOR (2026-07-16): tiap peluru player membawa `maxDist` (jarak horizontal pivot→`aimPoint` saat menembak) + titik tembak `sx/sz` (distempel weapons.js); saat jarak dari sx/sz ≥ maxDist posisi DIJEPIT tepat di batas (overshoot dimundurkan searah `b.dir`) dan ditandai `b.ended` — mati frame BERIKUTNYA supaya segmen terakhir masih dapat giliran sweep robot (robot yang berdiri persis di kursor tetap tertembak). Kematian `ended`: launcher → `queueBoom` meledak DI POSISI AKHIR peluru; peluru biasa ber-flag `b.fx` (pelet PERTAMA saja) → `spawnBulletFloorHit` DI POSISI AKHIR peluru (revisi 2026-07-16: dulu di titik kursor beku `fxX/fxZ` — sebar arah jadi tak terlihat, semua tembakan tampak mendarat di satu titik yang sama persis; jangan dikembalikan). Klem berbasis JARAK dari titik tembak → sebar arah menghambur di busur sejauh kursor.** Impor `queueBoom` dari robots.js, `spawnBulletFloorHit` dari effects.js.
- **grenades.js** — `NADE_R`, `buildGrenadeMesh(scale)` (Mk2; bahan bersama — JANGAN dispose; **kini dipakai ulang sbg peluru Grenade Launcher Lv1-2**), **`buildRocketMesh(scale)` (2026-07-16 — peluru launcher Lv3: ROKET [tabung zaitun + hulu ledak merah-bata + sirip silang baja + nozel + api knalpot amber Basic]; moncong = +Z lokal, weapons.js meng-lookAt-kan searah arah terbang saat spawn [terbang lurus, tanpa re-orientasi per frame]; geo/mat bersama Lambert/Basic [JANGAN dispose] di-warm preload; `userData.rocket = true` utk smoke; menyamai prop `launcher3` avatar — visual saja, damage/AoE/kecepatan sama)**. **`spawnGrenade`/`updateGrenades`/`nudgeGrenade` = DORMAN sejak 2026-07-11** (granat lempar dihapus; `grenades` array selalu kosong, `updateGrenades` no-op, `grenadeCollide` scene hook tak terpanggil) — dipertahankan karena `buildGrenadeMesh` + `explodeAt` masih hidup.
- **robots.js** — `CLAW_TIME`, `ellipGeo(r,sx,sy,sz,ws,hs)` (elipsoid berbagi via `geo.scale`), **`buildRobotMesh(cls='C')`** (`{group, rig}`; **OVERHAUL MESIN 2026-07-12: rangka ROBOT [geometri bersama `RG`] menggantikan tubuh manusia — torso rangka + PELAT DADA armor warna kelas + panggul + inti daya & VISOR EMISIF senada + ransel daya + sendi bola pinggul/siku; ujung lengan = CAKAR (C/boss) atau **SENAPAN DIPEGANG (B = tangan KANAN saja; A = DUA senapan kiri+kanan, 2026-07-12** — grup senjata diputar searah lengan menodong: receiver+laras+moncong+magasin+popor; **`rig.muzzle`** = ujung laras kanan, **`rig.muzzleL`** = kiri [A saja] — titik spawn peluru musuh, null utk lengan cakar; **`rig.gunR`/`gunL`** = grup senapan [utk animasi JUGGLE idle kelas A], null utk lengan cakar**)**; palet `CLASS_LOOK` (PELAT ARMOR + INTI DAYA): C HIJAU / B KUNING / A MERAH / boss ungu-gelap; **MATA/VISOR (+ ujung antena) = MERAH `EYE_RED` utk SEMUA kelas (user 2026-07-14)** — identitas kelas dari pelat+core; rig pivot & tinggi TETAP jadi `animateRobotRig` byte-identik & gore.js membaca rig yang sama; Lambert emissive = program shader sama, tanpa recompile**; `tintRobot`/`applyClassTint`/`ROBOT_VARIANTS`/`ROBOT_SKIN_TONES` DIHAPUS — warna dibangun langsung per kelas, boss juga), `reachForScale(scl, base?)` (**PENTING**: reachMul utk robot berskala — badan pejal `bodyBlockRadius×scl` mendorong player; tanpa ini kelas besar A/boss mendorong player keluar jangkauan cakarnya sendiri; scl 1 → tepat 1.0), `attackerAngle(ax, az)` (sudut penyerang relatif LAYAR utk `showHitDir` — top-down: atas layar = −z dunia, murni `atan2(dx, -dz)`, bukan dari yaw pivot), `animateRobotRig(z, dt)` (**lengan PENEMBAK 2026-07-12: menggantung+mengayun saat jalan, TERANGKAT mengacungkan senapan saat `z.aiming` [di-set scene AI, lerp `z.aimT`] — B hanya lengan KANAN [kiri tetap di bawah], A KEDUA lengan; recoil `z.recoilT`/`z.recoilSide` = laras naik sesaat di lengan yang menembak; lengan melee tetap pose menjulur lama; **SERANGAN CAKAR 3 FASE (overhaul 2026-07-13, menggantikan overlay sabetan sinus lama): ANCANG-ANCANG [`z.windT`/`z.windDur` dari ticker updateRobots — lengan penyerang tercocok tinggi ke belakang-luar easeOut + GEMETAR menahan tenaga, lengan lain kontra turun, torso memuntir `inner.rotation.y` + condong belakang + KUDA-KUDA merunduk, kepala menunduk mengincar] → SABET [`z.clawT`, k<0.38 cubic ease-in MELEDAK: lengan menyabet ~183° menyilang badan, torso menghentak untwist + LUNGE maju `inner.position.z` 1.7] → RECOVERY [smoothstep kembali ke pose jalan]; di luar serangan sisa puntiran/lunge/tunduk diluruskan halus dt·7 (walk/standing tak menulis `inner.rotation`); **state `'idle'` → delegasi ke `animateRobotIdle` lalu return; robot yang BARU aktif meluruhkan sisa pindaian kepala `head.rotation.y` + sandaran `inner.rotation.z`**), **`animateRobotIdle(z, r, dt)` internal (IDLE per KELAS, 2026-07-14, spesifikasi user — robot campaign dorman 'idle'. **BADAN & KAKI DIAM**: `inner.position.x/y/z` + `inner.rotation.z` + paha/betis SEMUA diluruhkan ke netral [tanpa lompat/goyang]. Yang bergerak hanya KEPALA & LENGAN. **Semua kelas:** kepala celingak-celinguk kiri-kanan [`head.rotation.y` sapuan ke target acak + jeda]. **GESTUR per kelas** [progres `z.gest` 0→1 sekali, jeda `z.gestT` acak; amplop `env=sin(g·π)`]: **C** (& boss melee) sesekali MENAIK-TURUNKAN kedua tangan/cakar [`armX = rest − env·1.75`]; **B** sesekali MENGGOSOK senapan dgn tangan KIRI [lengan kiri menjangkau ke senapan `env` + usapan bolak-balik `sin(g·7π)`, `armL.rotation.z` menyilang ke tengah]; **A** sesekali JUGGLE senapan KANAN — `rig.gunR` terbang naik `position.y += env·8` + berputar `rotation.x += g·4π` [2 putaran, balik tepat], lengan kanan menyentak lalu menyambut; field `idleInit/scanA/scanTarget/scanHold/idleTempo/gest/gestActive/gestSide/gestT`, init malas; saat aktif, jalur non-idle meluruhkan `head.rotation.y`+`inner.rotation.z`+`inner.position.x` & mengembalikan `gunR` ke tangan bila teraktivasi saat juggle; murni visual)**, **`celebrateRobot(z, dt)` internal (2026-07-12 — SELEBRASI KEMENANGAN: dipanggil updateRobots selama `isPlayerDying()` MENGGANTIKAN AI scene+serangan+rig; robot berhenti, menoleh halus ke jasad player, kedua lengan [senapan B/A ikut] teracung memompa ke langit "HOREEE" + lompatan girang [separuh siklus di udara] + dada membusung + kepala mendongak; delay tersadar/tempo/tinggi lompat/gaya (pompa serempak vs melambai bergantian `z.celebAlt`) DIACAK per robot [field `z.celebT/celebRate/celebPhase/celebHop/celebAlt`, init malas] → sorak MENJALAR di kerumunan; robot campaign 'idle' tetap dorman; pose via pendekatan eksponensial = transisi mulus dari pose apa pun; visual murni)**, `disposeRobot(z)`, **`killRobot(i, {cause,dirx,dirz}?)`** (**GORE 2026-07-11, tema COOLANT 2026-07-12**: SPLICE dari `robots` DULU [bangkai inert] + skor via `robotScore` [kelas C 100 / kelas ranged A·B 150 / boss `boss.score`] + `stats.kills` [skor via `robotScore`: kelas C 100 / ranged A·B 150 / boss `bosses.giant.score`], LALU serahkan mesh ke gore.js: semburan COOLANT hijau `spawnBloodBurst` + `gibRobot` + `spawnCorpse` + genangan coolant `spawnBloodDecal`; `cause:'explosion'` = HANCUR ekstra-gore (2× semburan coolant 360° ~52 percikan + 5 anggota + ~10 serpihan logam ekstra [tone `0x3d444c`] + ~9 genangan TERCECER); **`cause:'melee'` (2026-07-13) = TERBELAH DUA: semburan kipas + cincin 360° di pinggang + 2 serpihan penampang + `bisectCorpse` [TANPA gibRobot — kedua paruh utuh anggotanya]**; peluru = muncrat + kadang satu anggota; **tanpa headshot**), **`queueBoom(x,y,z,r,hurtPlayer?,playerDmg?,dmg?)`** (antre ledakan `explodeAt` diproses SETELAH loop robot [anti splice reentrant] — kini hanya peluru Grenade Launcher [bullets.js/robots.js, friendly, **meneruskan `b.damage`-nya lewat param `dmg` → AoE ikut level upgrade shop**; `dmg` null → default `CFG.grenade.damage`]; exploder dihapus 2026-07-12 → cabang `hurtPlayer` dorman), `resetRobotsFx()` (kosongkan antrean ledakan — dipanggil resetGame), **`fireRobotBullet(z, tx?, ty?, tz?, monasDmg?)`** (robot ranged tembak 1 peluru → `enemyBullets`, mesh `GEO.bullet`+`MAT.enemyBullet`; **kelas A BERGANTIAN kiri/kanan per tembakan** [toggle `z.fireSide`, delay antar tembakan tetap `fireDelaySec`] — **spawn dari UJUNG LARAS yang menembak** [`rig.muzzle` kanan / `rig.muzzleL` kiri, fallback dada]; target default player, override (tx,tz) utk sasaran lain [survival: pusat Monas]; `monasDmg` distempel ke peluru; **set `z.recoilT`/`z.recoilSide` = hentakan laras NAIK di lengan penembak — TIDAK lagi men-set `clawT`** [dulu tampak mencakar ke bawah]), **`damagePlayerHp(raw)`** (2026-07-13 — SEMUA damage player [cakar/peluru musuh/boom] lewat sini: armor memotong `reduce` tier dari HP, DURABILITY menerima damage BASE penuh [spesifikasi user], durability 0 → `breakPlayerArmor` internal = pecahan pelat gib + puff debu + `sfxMelee` + feed "ARMOR DESTROYED!" + armorLvl kembali 0; godMode = HP & armor utuh), **`PLAYER_BLOOD_HEX`** (0xb51a1a — warna darah MERAH player), **`updateEnemyBullets(dt, step)`** (gerak+hit peluru MUSUH→player: sweep segmen `segPointDist2`; dodge i-frame / player sudah tumbang = MELESET, **god-mode = darah+flash tetap tampil dan hanya HP-nya yang utuh** [fix 2026-07-12 — dulu seluruh efek di dalam guard godMode sehingga kena peluru tak berdarah]; **kena player → semburan DARAH MERAH `spawnBloodBurst(..., PLAYER_BLOOD_HEX)`** [juga di titik cakar & boom hurtPlayer]; **HP habis → `startPlayerDeath()` game.js, BUKAN gameOver langsung** [berlaku di ketiga titik damage; guard `player.hp > 0` menghentikan damage lanjutan pada yang sudah tumbang]; diblok dinding `activeScene.bulletBlocked` — **peluru ber-`monasDmg` yang terblokir memanggil hook scene `enemyBulletHitMonas(dmg, pos)`**; dipanggil `updateGame` setelah `updateRobots`), `updateRobots(dt, step)` (loop bersama: **selama `isPlayerDying()` → cabang SELEBRASI `celebrateRobot` menggantikan AI+serangan+rig [semua robot berhenti menyerang player & Monas; peluru player yang melayang tetap diuji]**; **peluru `b.explosive` kena robot → `queueBoom` + buang peluru [ledakan AoE menyusul]**; `activeScene.robotAI` → **TICKER WINDUP cakar (2026-07-13, sebelum cabang serang — jalan terus walau target berpindah): `z.windT` habis → `clawT` sabetan + swoosh `sfxRobotBite` + resolusi damage [player: RECHECK jangkauan `clawRange+clawStrikeGrace` × reachMul — mundur/dodge selama ancang-ancang = LUPUT; Monas: hook scene `monasGnawHit(z)`]** → **serang bila `chaseDist` ada**: kelas MELEE (C/boss) INISIASI ancang-ancang [set `windT=clawWindupSec`, `windTarget='player'`, cooldown dari sini = irama serangan tetap; damage `z.clawDmg`, indikator arah saat mendarat]; kelas RANGED (`z.ranged`, B/A) **STOP-AND-SHOOT (2026-07-12)** — scene AI menghentikannya di `0.95×z.range` saat LOS bebas, lalu `fireRobotBullet` tiap `z.fireDelaySec` digerbang `z.losOK !== false && chaseDist < z.range` [range per kelas: B 10 m = 70 u, A 12 m = 84 u — tak menembak tembus dinding] → rig → sweep hit peluru [**HORIZONTAL/2D bidang xz — y diabaikan** supaya hitbox pendek (robot skala kecil) tak lolos di bawah lintasan peluru; radius silinder `bodyHitRadius`×`z.scl`; damage = `max(1, b.damage×player.dmgMul − z.armor)`; `stats.hits`] → `processPendingBooms` [ledakan Grenade Launcher DI LUAR loop — anti bug splice reentrant; ledakan berantai iteratif] → **`separateRobots()`** [pemisahan robot-robot, DI AKHIR setelah semua gerak]). **`separateRobots()`** (export, 2026-07-16: cegah robot menumpuk — pasang yang jaraknya < `separationRadius`×skala dijumlah didorong menjauh sebesar `separationRelax`×overlap [soft, konvergen, tak jitter]; `'idle'` = JANGKAR tak digeser [dua idle dilewati], `'jumping'` dilewati; efek: saat player dikepung, yang tak kebagian tempat MENUNGGU di belakang cincin; **tiap robot yang bergeser DIJEPIT balik ke area sah via hook `activeScene.clampRobot(z, oldX, oldZ)`** [bugfix 2026-07-16 — tanpa ini dorongan menyangkutkan robot ke dinding; `separateRobots` menyimpan posisi valid pra-separasi sbg jatuh-balik hug-slide]; O(n²), n kecil). Field per-robot KELAS: `kind` (='C'/'B'/'A'/'boss'), `scl, armor, attack, clawDmg, ranged, fireDelaySec, bulletSpeed, range` (unit, = `rangeMeters×CAMP_M`, distempel scene spawner), `fireCd, losOK` (di-set scene AI tiap frame = `aim.direct`), `aiming`/`aimT` (stance membidik — scene AI/lerp rig), `fireSide` (A: giliran laras ±1), `recoilT`/`recoilSide` (hentakan laras naik), **`windT`/`windDur`/`windTarget`** (ancang-ancang cakar 2026-07-13: sisa/total windup + target `'player'`/`'monas'`; `clawSide` kini di-toggle saat INISIASI), `reachMul` (target Monas-vs-player kini per-frame berbasis jarak di survival robotAI — bukan field statis).
- **tank.js** — **BOSS TANK stage 4 (2026-07-14, menggantikan robot raksasa)**: `buildTankMesh()` (`{group, turret, tracks, wheels, mgMuzzle, cannonMuzzle, mortarMuzzle, cannonFlash, mortarGlow, paintMats}` — **DIROMBAK 2026-07-15 ke bentuk TANK TIGER I Jerman PD2, KOSMETIK murni [kontrak return & mekanik TAK berubah]**: superstruktur boxy menggantung, roda jalan besar saling-tumpang [geo ban dibaked poros→sumbu-X lalu dibungkus grup yaw-90° → `spinTracks(rotation.x)` = menggelinding], sprocket/idler, meriam 88 mm panjang + rem moncong dobel-baffle, mantlet, kubah komandan, kotak stowage buritan, spatbor, alat, mata rantai cadangan; **CAT DIGANTI 2026-07-16 ke skema faksi robot 2045 [panduan gaya world/palette.js]: gunmetal/abu-zaitun gelap senada rangka robot + lensa sensor ber-emissive MERAH senada mata robot — bukan dunkelgelb WWII lagi, bentuk Tiger I tetap**; hanya material Lambert/Basic [sudah dipanaskan → tanpa recompile saat spawn]; TURRET berputar [cannon +Z lokal] + tabung MORTAR di belakang cannon + senapan mesin bola di GLACIS depan; anchor muzzle sekadar titik-spawn kosmetik [proyektil tetap terbang ke player]), `spawnTank({homeX, homeZ, wallX, faceX})` (bikin+tambah ke scene, mulai di timur `homeX+160`; **HP/skor = `CFG.campaign.bosses.tank.hp`/`.score`**; entitas MANDIRI, BUKAN anggota `robots`), `updateTank(tank, dt)` (step=dt·60 lokal; dipanggil `stage4.updateMode`), `damageTank(tank, dmg)`, `disposeTank(tank)`. **Fase**: `'spawn'` (menggelinding easeOut dari timur, MENABRAK dinding di `wallX` → `smashWall` [gib beton+debu+shake] + callback `tank.onWallSmash`) → `'battle'` (turret melacak player belok-terbatas; **hit-test peluru PLAYER sendiri** `tankBulletHits` [sweep `segPointDist2` vs pusat hull, `CFG.campaign.bosses.tank.hitRadius`; peluru `explosive` = damage langsung + `queueBoom` visual]; **3 serangan BERGANTIAN** siklus `attackIdx` 0→1→2, jeda `gapSec` yang utk cannon/mortar dimulai **SETELAH proyektilnya MELEDAK** [`blastPending` membekukan hitung mundur selama proyektil terbang; `detonate` [id-guarded] men-set `cd=gapSec` saat ledakan → jeda 5 dtk BARU jalan → serangan berikut; MG jeda mulai usai burst]; telegraf `chargeK` menyalakan muzzle serangan BERIKUTNYA): (0) **CANNON** `fireCannon` → shell lurus ke posisi player-saat-tembak, meledak saat tiba [`cannonDamage`, radius = `CFG.grenade.killRadius × cannonBlastRatio`]; (1) **SENAPAN MESIN** `fireMG` → `mgBurst` peluru ke `enemyBullets`, tiap peluru ke posisi player SAAT ITU [`mgDamage`/peluru, interval `mgIntervalSec`]; (2) **MORTAR** `fireMortar` → **BURST `mortarBurst` (3) proyektil LOB PARABOLA balistik** berjeda `mortarBurstGapSec` (0.5 dtk) dari tabung belakang turret yang MENGHADAP KE DEPAN (2026-07-16: dulu 1 lob; burst pakai pola MG-burst via `tank.mortarLeft`/`mortarTimer` di `updateTank`, sebelum cek `blastPending`; `blastPending` di-set saat burst mulai → hitung mundur `gapSec` beku selama seluruh burst + semua terbang, baru jalan saat lob TERAKHIR meledak karena `fireMortar` menaikkan `pendingId` tiap tembak → yang terakhir = max, `detonate` id-guard membersihkannya di ledakan terakhir); tiap lob mendarat di posisi player SAAT tembakan itu (impact mengejar player yang bergerak); **mesh = `mortarShell()` (export, 2026-07-16): shell mortir REALISTIS acuan M73 [badan ogive zaitun + fuze kuningan + boom & 4 sirip baja] menggantikan bola kuning lama; hidung = +Y lokal, `updateMortars` mengorientasikannya sepanjang kecepatan (menukik nose-first saat turun); geo/mat shared malas, Lambert (tanpa recompile)**; (**LOB BER-APEX 2026-07-16 — rombak "parabola aneh"**: dulu waktu terbang = jarak/`mortarSpeed` → tembakan DEKAT ber-vy0 NEGATIF menukik datar seperti peluru; kini puncak = ujung tertinggi + min(riseCap dari `mortarMaxSec` [lob terjauh tetap mendarat sebelum umur habis], max(`mortarApexMeters`×CAMP_M, jarak×`mortarApexRatio`)) → **vy0 SELALU ke atas** [dekat = lob hampir vertikal, jauh = parabola panjang], waktu terbang murni gravitasi [naik √(2hUp/g) + turun √(2hDown/g)], kecepatan mendatar = jarak/waktu — `mortarSpeed` kini DORMAN; `updateMortars` integrasi `vy -= g·dt` + **jejak asap kelabu** tiap 0.12 dtk [`spawnGroundPuff` menumpang pool explosions] + kepulan tembak di mulut tabung, meledak saat turun melewati `landY`) [hindari = MENJAUH dari titik jatuh / TUMBLE i-frame; `mortarDamage`, radius killRadius × `mortarBlastRatio`; `mortarTurnRadPerSec` kini dorman]) → `'dead'` (`killTank`: ledakan besar + serpihan + skor boss + cat digelapkan + `hideZap`; `updateDeath` asap susulan). **PAGAR LISTRIK (2026-07-16, `updateShock` — tank TIDAK BISA DIDEKATI):** player dalam radius `shockRadiusMeters`×CAMP_M dari pusat tank tersengat `shockDps` HP/DETIK yang **MENEMBUS ARMOR** (HP dikurangi LANGSUNG, sengaja TANPA `damagePlayerHp` → durability armor utuh; godMode kebal [visual tetap tampil], i-frame dodge = meleset total; HP habis → `startPlayerDeath` menjauhi tank); umpan balik = **busur petir zig-zag tank→dada player** (`layZap` — pool 9 segmen kotak MeshBasic biru-es, dibuat SEKALI malas & di-share [tanpa recompile], `hideZap` saat tak dipakai/mati/dispose) + percikan biru-es `spawnBloodBurst` + `flashDamage` + `showHitDir` tiap 0.22 dtk + `sfxHit` tiap 1 dtk + cam-shake kecil; saat TAK ada yang dekat, lambung sesekali BERDESIS busur idle pendek (telegraf "berlistrik" → player belajar radius tanpa mati konyol); aktif di fase spawn maupun battle. **Damage player cannon/mortar lewat `queueBoom(...,hurtPlayer:true,...)`** (armor/godMode/i-frame dodge reuse `processPendingBooms`); MG lewat `enemyBullets` bersama. Impor `queueBoom`+`attackerAngle` dari robots.js, `startPlayerDeath` dari game.js (circular aman — dibaca dalam fungsi). **Tanpa kolisi BADAN player** (zona setrum = pagar penjauhnya).
- **futuristicDesk.js** — **`buildFuturisticDeskMesh(sx, sy, sz)`** (2026-07-15, dipakai `deskModel` di stage 1/2/3 sbg "object meja"): bungkus `class FuturisticDesk` (**DIROMBAK TOTAL 2026-07-16 low-poly realistis, ~10 mesh SEMUA Lambert** [dulu ~60 mesh Standard: grid 45 tombol keyboard, monitor melayang, kaki miring + pad glow]: top slab + 2 panel kaki samping + panel penutup belakang + monitor DUDUK di meja [alas/leher/layar teal] + keyboard 1 lempeng) dalam Group, di-**skala NON-UNIFORM** `(sx/6, sy/1.5, sz/3)` agar mengisi footprint blocker sx×sz dgn permukaan meja di ketinggian sy; berdiri di y=0. Material SEMUA `MeshLambertMaterial` (program ter-warm preload → tanpa recompile; jalur `renderer.compile` dunia stage tetap belt-and-suspenders). Impor THREE global (bukan ESM). `update(time)` (pulsa layar) TIDAK dipanggil (meja statis). Di stage 1/2/3 `deskModel` juga menaruh SATU KURSI (`buildFuturisticChairMesh`, dekorasi tanpa blocker) di sisi depan meja.
- **futuristicChair.js** — **`buildFuturisticChairMesh(scale=4.5)`** (2026-07-15, dipakai `deskModel` stage 1/2/3): `class FuturisticChair` (**DIROMBAK TOTAL 2026-07-16 low-poly realistis, ~16 mesh kecil SEMUA Lambert TANPA emissive** [dulu ~23 mesh Standard: sphere 16×16 "hover caster" glow, cincin hidrolik & strut emissive]: dasar bintang-5 + KASTOR silinder menapak lantai + gas lift + dudukan + sandaran miring + 2 sandaran tangan) di-skala UNIFORM, dasar model sudah di y=0 (builder TANPA lift). **Dekorasi murni — TANPA blocker** (kecil, tak ikut nav/collision). Impor THREE global. `update()` no-op (kompat API).
- **futuristicCupboard.js** — **`buildFuturisticCupboardMesh(sx, sy, sz)`** (2026-07-15, dipakai `cupboardModel` stage 1/2/3 sbg RAK/LEMARI): `class FuturisticCupboard` (**DIROMBAK TOTAL 2026-07-16 low-poly realistis, 8 mesh SEMUA Lambert** [dulu ~30 mesh Standard: kotak berongga 5 dinding + 3 rak isi server/vial/artefak + pintu kaca geser + hover-pad + PointLight]: plinth + badan + pelat atas + 2 daun pintu + 2 gagang + strip status teal; **TANPA PointLight sama sekali**; model lokal 2×3.9×1.9, dasar y=0) di-skala NON-UNIFORM mengisi footprint, dasar di y=0. `cupboardModel` men-TILE beberapa lemari (≤4, tiap unit ~kotak) sepanjang sisi terpanjang footprint SHELF supaya tak melar, TAPI hanya 1 blocker footprint penuh (nav/collision sama seperti balok SHELF lama). `update()` (pintu/hover) TIDAK dipanggil.
- **futuristicCrate.js / futuristicSofa.js / futuristicConsole.js / futuristicBench.js / futuristicRubble.js / futuristicPlanter.js / futuristicStall.js / futuristicSink.js** — prop per-objek (class `Crate`/`Sofa`/`Console`/`Bench`/`Rubble`/`Planter`/`Stall`/`Sink`, `{group}` + `update(t)` kompat), DIPECAH dari `futuristicProps.js` (dihapus 2026-07-15). **DIROMBAK TOTAL 2026-07-16 — low-poly realistis, SEMUA MeshLambertMaterial (program ter-warm preload → tanpa recompile), tiap model ≤ 10 mesh, dasar model di y=0 (builder TANPA lift):** Bench = papan + 2 kaki panel (dulu hover-pad glow); Console = kabinet + panel layar MIRING teal + bibir keyboard; Crate = badan + tutup + tepi marka amber (LineSegments); Sofa = rangka + SANDARAN TANGAN kiri-kanan (dulu tak ada) + 3+3 bantalan kain merah-bata; Rubble = bongkahan beton + tulangan REBAR besi (dulu kabel glow); Planter = pot + tanah + batang + tajuk 2 kerucut daun (senada pohon taman); Stall = counter + top kayu + 4 tiang baja MENOPANG atap (dulu atap melayang di tiang glow) + papan menu teal; Sink = pedestal & bak porselen + keran riser+CUCURAN (dulu tanpa cucuran + "air hologram"). Emissive hanya layar/papan teal ≤ `EMISSIVE_MAX`; Bench/Chair/Sofa/Planter/Rubble/Sink TANPA emissive. Warna dari token `PAL` (panduan gaya). THREE global; material dibuat di dalam constructor (bukan top-level). **DI-WIRE ke stage 1/2/3 (2026-07-15)** lewat drop-in **`buildFuturistic{Crate,Sofa,Console,Bench,Rubble,Planter,Stall,Sink}Mesh(sx, sy, sz)`** (dimensi model lokal di-HARDCODE spt `buildFuturisticCupboardMesh` — TANPA Box3; skala NON-UNIFORM mengisi footprint sx×sz tinggi sy, dasar model SUDAH di y=0, dibungkus Group). Dipakai helper stage **`propModel(build, c, r, sx, sy, sz, dx, dz, standable)`** (bangun model dari cell grid + daftarkan blocker footprint SAMA seperti `furBlock` lama → nav-grid/collision/BFS-konektivitas IDENTIK) MENGGANTIKAN balok furnitur berwarna: **Crate** = cover tersebar (s1 Main Hall & Storage, s2 Open Office & END, s3 Storage & koridor atas), **Sofa** = Break Room (s1) / Waiting (s2), **Console** = panel/konsol lantai (s2 Control, s3 Electrical) + di s1 juga terminal DI ATAS meja via `monitorModel` (dekorasi TANPA blocker, diangkat ke y=top meja), **Bench** = meja lab (s2 Lab/Research), **Rubble** = runtuhan koridor (s2 Hallway), **Planter** = sudut ring Atrium (s3), **Stall** = bilik Restroom (s1), **Sink** = deret wastafel Restroom (s1). **Stage 4 (outdoor) juga memakai** Stall (kios pelataran = cover/blocker), Bench + Planter (dekor pelataran stasiun, TANPA blocker), Crate (tumpukan parkiran = cover), Rubble (puing jalan = cover) — ditaruh via helper lokal `mkPropCover`/`mkPropDecor` (cover dijauhkan dari koridor; konektivitas union START→END tetap hijau di flood-fill smoke); Sink/Console indoor-only → tak dipakai di stage 4. Karena SEMUA prop balok stage 1/2/3 kini jadi model, `fur`/InstancedMesh furnitur tiap stage indoor KOSONG → blok render di-guard `if (fur.length)` (helper `furBox`/`furBlock` tetap ada tapi tak terpakai). (`Crate` tetap punya `LineSegments`/`EdgesGeometry`/`LineBasicMaterial` tepi amber; `Rubble`: `DodecahedronGeometry` bongkahan). `update(t)` no-op/pulsa ringan (kompat API, TIDAK dipanggil — statis).
- **futuristicMeetingTable.js** — **`buildFuturisticMeetingTableMesh(sx, sy, sz)`** (2026-07-15, dipakai `meetingModel` — HANYA stage 1 ruang Conference): `class FuturisticMeetingTable` (**DIROMBAK TOTAL 2026-07-16 low-poly realistis, 4 mesh SEMUA Lambert** [dulu 2× `ExtrudeGeometry` rounded-rect ber-bevel + proyektor hologram + bola data + kerucut sinar + 8 terminal glow]: top slab kayu jati `PAL.wood` + 2 pedestal kotak + hub kabel/media teal di tengah permukaan; permukaan y≈1.5, dasar y=0) di-skala NON-UNIFORM mengisi footprint, dasar di y=0. Impor THREE global. `update()` no-op (kompat API).
- **futuristicSedan.js** — **`buildFuturisticSedanMesh(scale=7, bodyColor=null)`** (dipakai `stage4.mkSedan` sbg varian "object mobil" cover, mendampingi SUV). **DIROMBAK TOTAL 2026-07-16 jadi low-poly ringan**: `class FuturisticSedan` = sedan "3 kotak" (bodi bawah + kap depan + bagasi + kabin kaca + pelat atap + 2 bumper + strip head/taillight) ~18 mesh box + silinder 12-segmen, **SEMUA `MeshLambertMaterial`** (program shader sudah di-warm preload → tanpa recompile & murah; dulu `ExtrudeGeometry` ber-bevel + `MeshStandardMaterial`). **Roda dibenahi**: geometri di-bake `rotateX(90°)` → poros Z (tegak, menggelinding searah panjang bodi), menapak tanah di y=0, dan MENONJOL keluar sisi bodi (z ±0.94 > setengah-lebar bodi 0.88 — dulu terkubur di dalam bodi). Model lokal: depan = +X, dasar roda y=0 → **builder TANPA lift** (dulu diangkat 0.9·scale); Group ber-orientasi panjang bodi → sumbu Z. Warna dari `SED_PALETTE` stage4 / default `PAL.gunmetal` (panduan gaya). `update(delta,{speed})`/`dispose()` = kompat API, TIDAK dipanggil (mobil statis). Diuji smoke (sweep palet + >=4 roda silinder + tanpa-lift).
- **futuristicSUV.js** — **`buildFuturisticSUVMesh(scale=7, bodyColor=null)`** (dipakai `stage4.mkCar` sbg "object mobil" cover — MENGGANTIKAN `suv.js/buildSUVMesh`). **DIROMBAK TOTAL 2026-07-16 jadi low-poly ringan**: `class FuturisticSUV({bodyColor, scale, enableLights})` = SUV "2 kotak" (bodi tinggi + greenhouse kaca + pelat atap + 2 rel atap + 2 bumper + bar grille teal [satu-satunya aksen] + 2 headlight + strip taillight + ban serep di pintu belakang) ~21 mesh box + silinder 12-segmen, **SEMUA `MeshLambertMaterial`** (program shader sudah di-warm preload → tanpa recompile & murah; dulu ~64 mesh `MeshStandardMaterial`+`MeshPhysicalMaterial` transmission [torus arch, 5 jari-jari/roda, exhaust ring]). **Roda dibenahi**: dulu silinder di-rotasi `rotation.z` → poros sejajar PANJANG mobil (ban "rebah"); kini geometri di-bake `rotateX(90°)` → poros Z tegak, menapak y=0, menonjol keluar sisi bodi (z ±0.98 > setengah-lebar 0.92); ban serep poros X. Model lokal: depan = +X, dasar roda y=0; Group ber-orientasi panjang bodi → sumbu Z. Jalur `renderer.compile` stage 4 (runLeaveShop/warmupAll/campaignJumpToStage) tetap ada = belt-and-suspenders (bukan lagi kebutuhan mobil). Warna bodi dari `SUV_PALETTE` stage4 / default `PAL.gunmetal`. `update(delta,{speed})`/`dispose()` = kompat API, TIDAK dipanggil (mobil statis). Diuji smoke (sweep palet + >=4 roda silinder + tanpa-lift).
- **suv.js** — `buildSUVMesh(scale=7, bodyColor=null)` (Group model SUV Lambert/Basic; panjang bodi → sumbu Z, y=0): **TIDAK LAGI DIPAKAI** sejak stage4 beralih ke `futuristicSUV.js` (2026-07-15); dulu disimpan sbg alternatif ringan, tapi sejak rombakan 2026-07-16 `futuristicSUV.js` sendiri sudah low-poly Lambert — modul ini tinggal kandidat hapus.
- **drops.js** — `MEDKIT_MAT`, `MAG_GEO`, `MAG_MAT` (bersama, jangan dispose), `buildMedkitMesh()`, `buildMagMesh()` (Group magazen kurva: 3 segmen miring + alas + bibir + peluru brass — dipakai spawnDrop & supply stage 1; ganti balok kuning lama), `spawnDrop(pos)` (peluang CFG.drops + `activeScene.clampDropPos`; **drop granat DIHAPUS 2026-07-11 — cuma paket peluru 'mag'**), `updateDrops(dt, T)` (bob, pickup dgn aturan full-item + feed "already full" cd 1.2s, kedaluwarsa; **drop 'mag' = PAKET PELURU: +`CFG.weapons.<w>.ammoPickup` per senjata DIMILIKI [termasuk launcher], cap `maxAmmo`**; **medkit pickup = tambah 1 medkit genggam [maks `CFG.player.maxMedkits`] — dipakai via tombol 4**).
- **effects.js** — `initEffects(scene)` (pool 3 lampu + **72** sprite percikan cairan — **tekstur splat NETRAL putih, DIWARNAI per-spawn via material.color (2026-07-12): default `COOLANT_HEX` hijau [robot], darah MERAH player lewat param warna [`PLAYER_BLOOD_HEX` robots.js]; nama fungsi `Blood*` dipertahankan**), `explodeAt(pos, radius?, dmg?)` (visual + DAMAGE robot dlm radius: `max(1, d − z.armor)` dgn d = param `dmg` [peluru launcher meneruskan `b.damage`-nya yang sudah ber-level upgrade] atau default `CFG.grenade.damage` [boss `z.kind==='boss'` tetap `boss.grenadeDamage` khusus — tak terpengaruh upgrade] — robot ber-HP tinggi bisa selamat di pinggir radius; **mati oleh ledakan → `killRobot(i,{cause:'explosion',dir…})` = dismember**), `spawnGroundPuff(x, z, color, scale, y)`, **`spawnBulletFloorHit(x, z, y?)`** (2026-07-16: efek tembakan di lantai saat peluru berakhir di BATAS KURSOR tanpa kena apa pun — 2 ground puff [percik amber + debu] di tinggi lantai `activeScene.groundHeight(x,z,y)`; menumpang pool explosions, tanpa material/lampu baru; dipanggil bullets.js), **`spawnBlood(x, y, z, vx?, vy?, vz?, color?)`** (percikan pool tetap; kecepatan opsional = MUNCRAT + jatuh; `color` default `COOLANT_HEX`), **`spawnBloodBurst(x, y, z, dirx, dirz, n, power?, spread?, color?)`** (semburan `n` percikan kerucut `spread` rad searah damage; `spread`=6.283 → 360° utk ledakan — dipakai saat kena tembak & saat mati; `color` = hijau robot / merah player), `updateExplosions(dt)`, `updateBloodPool(dt)` (integrasi kecepatan+gravitasi percikan), `resetBloodPool()`, **`borrowBloodSprite()`** (warmup preload). Top-down: `spawnBlood` menggeser sprite ke arah **viewCam**. (Bangkai/gib/genangan coolant lantai = modul terpisah **gore.js**.)
- **gore.js** — **GORE (2026-07-11; re-tema COOLANT 2026-07-12)**: `initGore(scene)` (pool TETAP gib `GIB_COUNT` 64 [serpihan logam, warna default `0x3d444c`] + genangan coolant `DECAL_COUNT` 44 [tekstur blob HIJAU, tint default `0x2fbf66`], dibuat sekali & disembunyikan di scene → warmup preload mengompilasi shader-nya, nol recompile), `spawnCorpse(group, rig, {dirx,dirz,dur?,fast?})` (jadikan mesh robot BANGKAI: pose lemas → toppling `rotateX` di atas kaki → memudar [material di-`transparent`] + ambles → dispose), **`bisectCorpse(group, rig, {dirx,dirz,restY?})`** (2026-07-13, kill PEDANG via killRobot `cause:'melee'`: belah rig di bidang pinggang LOKAL y 6.35 [bebas skala kelas] — anak `inner` ber-posisi lokal ≥ 6.3 [torso/dada/inti/ransel/bahu/leher/kepala/kedua lengan] dipindah ke grup paruh ATAS baru ber-origin TEPAT di bidang potong [pose inner dinetralkan dulu; grup robot yaw-only → translasi sederhana mempertahankan pose dunia 1:1], panggul+kaki tinggal di grup asli = paruh BAWAH; kedua penampang diberi CAKRAM TEPI-POTONG menyala oranye [`CUT_GEO` CircleGeometry bersama; Lambert emissive = program visor robot, nol recompile]; paruh atas TERLEMPAR berputar [pivot pinggang, quaternion premultiply] ke arah campuran bidik+KIRI-bidik (arah jalan bilah) sambil MENETESKAN coolant tiap 0.06 dtk → mendarat [+decal]; paruh bawah BERDIRI 0.5–0.85 dtk [kaki melemas bertahap, stump MENYEMBUR tiap 0.09 dtk] lalu roboh `rotateX`; keduanya memudar+ambles bersama → material [DIBAGI antar paruh — dikumpulkan unik] dispose SEKALI; fallback `spawnCorpse` bila rig tak ada; array `bisected` internal, dibersihkan `resetGore`), `gibRobot(rig, group, level, dirx, dirz, restY)` (`'heavy'`=ledakan: lengan/paha/kepala lepas; `'light'`=peluru/melee: kadang satu — SEMBUNYIKAN pivot di bangkai + lempar gib sewarna pelat anggota itu [`limbTone`]), `spawnGibs(x,y,z,count,dirx,dirz,power,tone,restY)` (serpihan balistik + spin + mantul + genangan coolant saat mendarat), `spawnBloodDecal(x,z,s,tone?)` (genangan coolant pipih di tanah), `updateGore(dt)` (dipanggil updateGame SETELAH updateRobots), `resetGore()` (dipanggil resetGame — dispose bangkai, sembunyikan pool). Geometri/material gib & tekstur decal DIBAGI (jangan dispose); material bangkai = per-instance robot (dispose bersama bangkai).

### src/scenes/
- **menu.js** — `initMenu(onPick)`: **MENU UTAMA `#mainMenu` (z-32, tampil paling awal)** via `initMainMenu()` — tombol **Start Game** (sembunyikan menu → tampilkan `#modeSelect`), **Settings** (buka `#settingsPanel` = kualitas grafis `#qualityRow` DIPINDAH ke sini; `initSettingsQuality()` hanya SIMPAN `localStorage gibsQuality` + tandai aktif, penerapan sebenarnya tetap di `initQualityUI`/startGame), **Credits** (`#creditsPanel`), **Exit Game** (`exitGame()`: confirm → `window.close()` + fallback layar perpisahan); Back tiap panel kembali ke daftar (kelas `.subview`/`.open`); kartu `#modeSelect` (`data-mode`, `#modeBack` → kembali ke menu utama) + baris `#diffRow` difficulty (localStorage `gibsDifficulty`); helper `beginMode(mode, stage)` → `applyDifficulty(diff)`+`setDifficulty(diff)` SEBELUM `onPick(mode, {stage})`. **Prompt Continue (2026-07-15):** klik kartu **Campaign** memanggil `loadCampaignStage()` — bila stage tersimpan **> 1**, tampilkan `#continuePrompt` ("Continue?"): **Yes** → `beginMode('campaign', saved)` (lanjut di checkpoint), **No** → `clearCampaignSave()` + `beginMode('campaign', 1)` (New Game). Tanpa save (atau stage 1) langsung `beginMode(mode, 1)`. + cutscene 4 slide (hanya survival; campaign lompat ke blocker).
- **survival/world.js** — `PARK, FENCE_H, ROAD_W, FOUNTAIN, treeColliders, MONAS_HALF` (22 = half dasar Monas 44×44); `buildSurvivalWorld()` (ground/jalan/pagar/props/Monas/city); `buildSurvivalNav(fountainWalkable=true)` (nav-grid: Monas+pohon penghalang; bak walkable=vault survival / bak PEJAL utk stage 3 tanpa vault); **dunia taman DIBAGI dua scene**: `ensureParkWorld()` (bangun sekali), `getSurvivalNav()` (grid bak-walkable), `getParkNavSolidFountain()` (grid bak-pejal); **`resolveMonas(pos, oldX, oldZ, radius)`** (AABB Monas dorong keluar PER-SUMBU = menyusur, TIDAK menempel/macet — seperti slideWalk campaign), `resolveObstacles(pos, r, feetY)` (pohon + bak; return true bila bak menghalangi = pemicu vault), `segmentHitsFountain(...)`, `groundHeightAt(x, z, feetY)`. **Kanopi kabut event** (`createFogCanopy()` dipanggil dari `buildSurvivalWorld` = dibangun sekali, ikut warm-up preload): piringan overhead tembus-pandang di y 26, tekstur radial dengan LUBANG bening `CFG.survival.monasFogClearMeters` di pusat=Monas; kendali `setFogCanopy(k)` (opacity 0..1, sembunyi saat k~0) + `driftFogCanopy(dt)` (putar pelan; lubang tetap terkunci di Monas). **Runtuhnya Monas (2026-07-13):** bagian atas obelisk (kaki+obelisk+cawan+api) dibangun di grup engsel `monasTop` (origin `MONAS_HINGE` x6.5/y8 = permukaan pelataran/tepi dasar) supaya bisa TUMBANG; API `startMonasCollapse()` / `updateMonasCollapse(dt)` (kembalikan true saat selesai) / `resetMonasCollapse()` / `isMonasCollapsing()`. 3 fase (TREMBLE getar+debu+api meredup / TOPPLE percepatan `rotation.z` + benturan: debu+puing marmer [box geo bersama + material stone/marble Monas] + `addCamShake` + api padam / SETTLE pantul+debu mengendap); durasi dari `CFG.survival.monasCollapse{Tremble,Topple,Settle}Sec`; api diambil-alih dari decor (`setFlameLight(null)`) selama runtuh; reset menegakkan lagi + dispose puing.
- **survival/index.js** — `survivalScene` (+hook `shopKey`/`shopActive`). **Wave berbasis "clear" (overhaul 2026-07-07)** via `wave.phase` (`'fighting'`→`'cleared'`→`'shopping'`): `startWave(n)` (jatah `robotsPerWaveBase + (n-1)·Step`, cadence & cap dari `CFG.survival`, picu event bila `n % eventEveryWaves`), `updateMode` (spawn sampai jatah habis dibatasi `maxConcurrent`; bersih [`toSpawn===0 && robots.length===0`] → 'cleared' + "WAVE CLEARED" + hitung mundur `shopCountdownSec` → `openShop()` + 'shopping'; catatan: fase shopping mem-PAUSE game [shop modal] jadi updateMode diam sampai Next Wave), `startNextWave()` (export; tutup shop + `startWave(num+1)` + **`requestLock()` untuk resume** — dipanggil dari gesture klik/SPACE), `shopKey(key)` (HANYA SPACE/Enter → Next Wave; pembelian item lewat KLIK di shop.js). `spawnRobot()` (lompat pagar dua fase + roll KELAS C/B/A per wave; stempel `z.range` = rangeMeters×CAMP_M), **`monasHp`/`monasMaxHp` scene-local** (max di-reset dari `CFG.survival.monasMaxHp` di `enter()`; objektif PRIORITAS; `monasHp ≤ 0` TIDAK langsung game over — set `monasFalling`, panggil `startMonasCollapse()` [world.js] + "THE MONUMENT IS FALLING!"; `updateMode` mengemudikan `updateMonasCollapse(dt)` [lewati mesin wave selama runtuh] → saat selesai `gameOver(false,'THE MONUMENT HAS FALLEN')`; `enter()` panggil `resetMonasCollapse()`; **hook `robotsCelebrate()`= `monasFalling`** → robots.js membuat SEMUA robot bersorak selama runtuh, sama seperti saat player mati [gate `isPlayerDying() || activeScene.robotsCelebrate()`]) + item shop Monas `healMonas()` (export; +25% max HP, tolak bila penuh), `strengthenMonas()` (export; naikkan max ke TINGKAT berikutnya di `CFG.survival.strengthenMonasStages` mis. 3000→4500→6000 + heal sebesar kenaikan; `monasStage` scene-local, plafon di tier terakhir → tolak), getter `getMonasHp/getMonasMaxHp` + `isMonasFullyStrengthened()` (shop tandai 'Maxed') + `getMonasState()`/`setMonasState()` (snapshot/pulihkan Monas utk UNDO beli shop klik-kanan), `EVT` event wave = **kabut Monas SAJA** (fog jarak + blackout lama DIBUANG 2026-07-11): `'monasfog'` = kanopi kabut berbasis-posisi via `setFogCanopy`/`driftFogCanopy` (world.js), lubang bersih `monasFogClearMeters` di Monas; di-roll di `startWave` (eligible ≥ `monasFogFromWave`, peluang `min(monasFogChanceMax, monasFogChanceBase + monasFogChancePerWave·(n−fromWave))`), `robotAI` (jumping→chasing via `navAim`; **target per-frame: `atkMonas = monasHp>0 && distToEye > playerAggroMeters·CAMP_M` — serang Monas default, kejar player hanya dalam radius aggro**; **serangan Monas per kelas (2026-07-12): MELEE menggerogoti tepi AABB [stop 6] — sejak 2026-07-13 INISIASI ancang-ancang (`windT=clawWindupSec`, `windTarget='monas'`, cooldown dari sini; komit/lock di-roll saat inisiasi) dan damage-nya jatuh via hook **`monasGnawHit()`** [scene ini] = `damageMonas(monasClawDamage)` yang dipanggil ticker windup robots.js saat sabetan mendarat; PENEMBAK B/A BERHENTI di `0.95×z.range` lalu `fireRobotBullet(z, 0,0,0, monasClawDamage)` ke PUSAT Monas tiap `fireDelaySec` — peluru membawa `monasDmg`, terblokir siluet Monas → hook `enemyBulletHitMonas(dmg,pos)` [scene ini] = `spawnGroundPuff` + `damageMonas`; komit/lock Monas di-roll pada TEMBAKAN pertama juga**; penyerang Monas return `{}`). `hudStatus`: fighting `Wave N — X left · Monas P%`, cleared hitung mundur, shopping "Field Shop open".
- **survival/shop.js** — shop antar-gelombang **MENU KLIK modal** (2026-07-08; game DI-PAUSE + pointer dilepas oleh input.js selama `shopActive()`; dibuka OTOMATIS oleh scene setelah wave selesai). Katalog data-driven `catalog()` → item `{id,name,desc,cost,weapon?,apply()}`: ammo (senjata DIMILIKI saja, termasuk launcher), **health** (Replenish Health — sembuh instan 100%), **medkit** (beli 1 medkit genggam → `player.medkits`; dipakai via tombol 4), **healMonas**, **strengthenMonas**, **radar** (set `player.hasRadar`), **kartu senjata GABUNGAN per senjata (2026-07-17, `weaponItem(w)`, id = kunci senjata `pistol`/`shotgun`/`rifle`/`launcher` — menggantikan kartu beli terpisah + kartu `up_<w>`): belum dimiliki → kartu menjual SENJATANYA (harga `shotgunCost`/`rifleCost`/`launcherCost`; pistol TIDAK pernah dijual — `weaponItem('pistol')` return null saat tak dimiliki); sudah dimiliki → kartu YANG SAMA menjual upgrade Lv2 lalu Lv3 (nama ber-Romawi tingkat yang dijual, mis. "Shotgun II"; pola Vitality),** **item KARAKTER (2026-07-13): `armor1`/`armor2`/`armor3` (TIGA KARTU TERPISAH [revisi 2026-07-13, `armorTierItem(n)`] dari `CFG.armor.tiers` — blok 30/50/70% damage + durability 100/200/300; tiap tier bisa dibeli LANGSUNG kapan pun [boleh lompat ke III], membeli = MENGENAKAN tier itu dgn durability penuh; tier yang dipakai & UTUH → note 'Worn' (ditolak), tier LEBIH RENDAH dari yang dipakai → 'Owned' + pesan `ownedMsg`, tier yang dipakai tapi RUSAK boleh dibeli lagi = REPAIR penuh; HANCUR [durability 0] → armorLvl 0 → ketiga kartu terbuka lagi), `hpup` Vitality I/II/III (3 tingkat; naikkan `player.maxHp` ke tangga `CFG.player.hpUpgrades` [150/200/250] + heal sebesar kenaikan; label Romawi mulai dari I), `ammoup` Ammo Capacity I/II/III (3 tingkat; tiap tier +20% kap dasar tiap senjata kecuali launcher +15% — `CFG.weapons.ammoUpgrades`, naikkan `player.ammoLvl` → kap efektif `maxAmmoFor`)**, **upgrade di kartu gabungan: menaikkan `player.weaponLvl[w]` Lv2 lalu Lv3 (= `maxWeaponLevel`, maks), tiap level +`upgradeDamagePct` (25%) dari damage BASE (efek via `weaponDamage()` weapons.js; boom launcher ikut), harga per tingkat `CFG.shop.upgradeCosts[w]`, level maks → `ownedNote` 'Maxed' (cek `it.upgrade`; varian dimiliki TIDAK dicap 'Owned' — guard `it.weapon && !it.upgrade`) + pesan tolak per-item `maxedMsg`** (`apply()` → null sukses / string alasan; Monas via impor `healMonas`/`strengthenMonas`/`isMonasFullyStrengthened` dari index.js — circular; Strengthen bertingkat, plafon → 'Maxed'; deskripsi SENGAJA tanpa angka max HP). **Beli senjata:** slot kosong → `buyWeapon` append + `syncOwnedFromWeapons` + `refreshOwnedWeapon`; **slot penuh (`maxWeapons` 3)** → `shopPurchase` return `'choose-replace'` + `pendingWeapon`, UI `renderReplace` (tombol per senjata), lalu **`shopReplaceWeapon(oldW)`** (ganti di slot itu + potong skor) / **`shopCancelReplace()`**; `isReplacePending()`/`pendingWeaponName()`. `shopPurchase(id)` (dipakai handler KLIK DOM DAN test headless: return null/alasan/`'choose-replace'`; potong skor via `addScore(-cost)` hanya bila sukses; **BELI hanya lewat tombol Buy** — klik baris daftar cuma memilih/preview), **`requestNextWave()`/`isConfirmOpen()`** (Start Next Wave dua langkah: panggil pertama → prompt "Are you ready?" Yes/No [`confirmNext`], panggil kedua / klik Yes → `startNextWave`), `openShop/closeShop/isShopOpen`, `render()` (createElement → handler klik/hover nyata; **TAB (2026-07-15; urutan direvisi 2026-07-17 — GENERAL PERTAMA & jadi tab default saat shop dibuka): baris `.shopTabs`→`.shopTab` [General/Weapons/Armor/Upgrades] mengelompokkan katalog — `itemTab(it)` mengklasifikasi (weapon=`it.weapon||it.upgrade`, armor=`it.armorTier`, upgrade=`ammoup`/`hpup`/`strengthenMonas`, sisanya general), `tabItems(tab)`/`visibleTabs()` (tab kosong—mis. Monas di campaign—disembunyikan), `firstTabId()`; helper uji `shopTabDebug()`.** Semua tab pakai SATU grid seragam `.shopGrid` (2 kolom, **tinggi TETAP 42vh** + `overflow-y:auto`) DAN panel deskripsi `.shopDesc` **tinggi TETAP 104px** (+scroll) → ukuran panel tak berubah saat pindah tab (dulu deskripsi armor yang panjang menambah tinggi); tab **Weapons** kini loop grid seragam biasa (kartu gabungan 2026-07-17 — pemasangan `.rowStart` [Beli]+[Upgrade] sebaris DIHAPUS; kelas CSS-nya boleh tinggal tak terpakai), urutan kartu = `WEAPON_ORDER`. **TANPA tombol Buy (2026-07-15):** kartu `.shopCard` via `makeCard(it,desc)` = nama + `.shopCardPrice` (harga/status); **KLIK-KIRI kartu = langsung beli (`doPurchase`), KLIK-KANAN = batalkan pembelian TERAKHIR (`doUndo`→`shopUndoLast()`)** — undo satu langkah via `snapshotState()`/`restoreState()` (pulihkan semua state yang bisa diubah beli [field player + ammo/level senjata + Monas via `getMonasState`/`setMonasState` index.js] + kembalikan skor; `refreshOwnedWeapon` hanya bila slot senjata berubah); hover = preview. [nama + tombol `.shopCardBuy` "Buy · cost" per kartu = satu-satunya jalur beli; klik kartu hanya PILIH/preview, `.sel` = terpilih] + panel `.shopDesc` lebar-penuh di bawah [deskripsi+harga via `showDesc`], atau `.shopReplace*` saat pendingWeapon, atau `.shopConfirm*` saat confirmNext; bawah `.shopScore` + `.shopNext` → `requestNextWave`). Impor `WEAPON_DEF`/`refreshOwnedWeapon` dari weapons.js + `syncOwnedFromWeapons` dari state.js. Next Wave dari SPACE (scene `shopKey` → `requestNextWave`). Semua teks English. **KONTEKS shop (2026-07-14): `openShop(ctx)` — Survival (default) vs Campaign (`ctx.mode`).** Campaign menyembunyikan HANYA item Monas (`SURVIVAL_ONLY` = {healMonas, strengthenMonas}) via filter `catalog()` — **Radar/Shotgun/Rifle/Launcher + upgrade-nya KINI DIJUAL di campaign** (2026-07-14); `ctx` juga mengatur head/label tombol/prompt konfirmasi + AKSI "lanjut" (`ctx.onNext`: Survival = `startNextWave`; Campaign = `runLeaveShop` via `campaign/transition.js`) + **latar OPAK** (`#shopOverlay.campaignShop`) saat mode campaign (= SHOP SCENE terpisah). `requestNextWave`/render/renderConfirm memakai `shopCtx`.
- **campaign/common.js** — `spawnCampaignRobot(x, z, stage, cls='C')` (idle; cls: C/B/A [CFG.robot.classes; B/A = penembak ranged, `z.range` distempel dari rangeMeters×CAMP_M] / **boss** [CFG.campaign.bosses.giant — langsung chasing, melee, frame gelap raksasa dari `buildRobotMesh('boss')`, granat luka berkurang, skor khusus]; hp/speed/attack/scale per kelas dibangun `buildRobotMesh(cls)`; `reachMul` via `reachForScale`), `campaignRobotAI(z, dt, step, {walkable, resolve, los?, nav?})` (culling `cullDistance`, aktivasi `activateMeters`×CAMP_M + LOS opsional/tembak; kejar via `navAim(nav)` + slide per-sumbu; **stop: ranged berhenti di `0.95×z.range` saat `aim.direct` [stop-and-shoot] else stop-range × `z.reachMul`; `z.losOK = aim.direct` + `z.aiming = !z.moving && aim.direct` tiap frame [stance senapan terangkat]**), **`campaignClampRobot(z, oldX, oldZ, {walkable, resolve})`** (2026-07-16 — hook `clampRobot` campaign: `stage.resolve` furnitur + slide per-sumbu `walkable`, jatuh-balik ke posisi valid pra-separasi; dipakai `separateRobots`), `countStageRobots(n)`.
- **campaign/transition.js** — **SHOP SCENE + DUA LOADING antar-stage (2026-07-14, overhaul jadi scene terpisah)**: `beginStageTransition(nextScene)` (dipanggil `playerCollide` sebuah stage saat mencapai tangga END — `busy` guard lalu `setScene(campaignShopScene)`; setScene MID-FRAME aman karena hook gameplay shop scene semua no-op & `updateMode`/`checkWin` di-guard di game.js). **`campaignShopScene`** (EXPORTED, scene tersendiri `id:'campaign-shop'`): `enter()`→`runEnterShop()`; **`shopActive:()=>true`** (SELAMA scene aktif, bukan hanya modal terbuka — input.js menekan menu jeda sepanjang loading+belanja) + `shopKey` (SPACE/Enter→`requestNextWave`) + `restartScene:()=>stage1Scene` + SEMUA hook gameplay no-op (playerCollide/groundHeight 0/bulletBlocked false/robotAI skip/clampDropPos/hudStatus/radarLandmarks). **`runEnterShop()` async (LOADING #1)**: `setPaused(true)` [pointer TETAP terkunci — TAK exitPointerLock → tak ada pointerlockchange → tak ada menu jeda; hanya updateGame berhenti] → `showLoading`→`loadingStep`→`minHold(MIN_LOADING_MS 900)`→`hideLoading`→`openShop({mode:'campaign', nextLabel:'Start Next Stage', onNext: runLeaveShop})` [openShop yg exitPointerLock utk kursor; shopActive()=true → tanpa menu jeda]. **`runLeaveShop()` async (LOADING #2 = onNext)**: `closeShop`→`showLoading`→`loadingStep`→`setScene(next,{transition:true})` [robot + posisi awal — SEMUA dunia sudah di-pre-build `stage1.enter` (2026-07-16), jadi tiap transisi konsisten ~`MIN_LOADING_MS`; dulu stage 3/4 lazy → LOADING #2-nya jauh lebih lama]→`busy=false` [stage baru aktif, player di START]→`renderer.compile` (jaring pengaman, sudah di-warm startGame)+beberapa render nyata (unggah tekstur viewpoint baru)→`minHold`→`hideLoading`+`blocker` "klik utk lanjut" (pointer-lock butuh gesture yg hilang pasca-async; `isPaused` TETAP true sampai klik). Katalog campaign SELLS Radar/Shotgun/Rifle/Launcher + upgrade (hanya Monas disembunyikan); skor = mata uang (bertahan antar-stage). Stage 4 finish = `gameOver(true)` (tanpa shop). **`campaignJumpToStage(n)`** (EXPORTED, 2026-07-14 — cheat `skip-to-stage-N`, dipakai hook `cheatSkipToStage` di keempat stage): buang SEMUA robot + `resetRobotsFx` + `clearArray` entitas transien → `setScene([_,stage1,stage2,stage3,stage4][n])` (enter: posisi player + tempat robot 1/3/4 — dunia sudah pre-built) → **stage 2 spesial** panggil `placeStage2Robots()` (robotnya normalnya via `stage1.enter`); n di luar 1..4 → null. Impor `stage1Scene`/`stage2Scene`/`stage3Scene`/`stage4Scene` + `placeStage2Robots` (semua circular aman: dibaca DI DALAM fungsi).
- **campaign/stage1.js** — `stage1Scene` (id `campaign-1`) + `S1, s1grid, s1Cell, S1_START, S1_EXIT, s1Wall, stage1Walk, s1LOS, s1SegHitsWall, resolve, s1Nav (nav-grid SETENGAH sel = 7 unit: dinding dari grid denah + furnitur/undakan di-bake via resolve — dibangun di AKHIR buildWorld setelah blockers terisi), buildWorld, placeRobots, placeSupplies`. Grid = satu-satunya sumber dinding (visual+kolisi+LOS+peluru). **Denah dirombak total 2026-07-11 ke floor-plan referensi user** (Lantai 2): START = tangga TURUN kiri-atas (`S1_START` c3,r4; player turun dari Lt.3) → koridor sempit → Office/Conference/Supply Room → Main Hall pusat → Security/Break/Restroom/Storage → END = tangga TURUN kanan-bawah (`S1_EXIT` c24-28,r23-27; ke lantai bawah = stage 2). 9 spot robot `S1_ROBOTS` `[c,r,jumlah]`; supply di Supply Room + bonus tersebar. **Konektivitas BFS + bebas-pintu + supply-di-lantai sudah diverifikasi** (skrip di scratchpad final.js) — jika mengubah denah/furnitur, jalankan ulang tes itu (BFS dari start meliputi semua sel lantai, tak ada furnitur menutup sel pintu, tiap posisi supply walkable & di luar furnitur). **Top-down 2026-07-11: mesh plafon `visible=false`** (kamera di atas — interior terlihat melewati dinding; fisika pantulan granat di plafon TIDAK berubah). `enter()` = orkestrator campaign: build **SEMUA EMPAT dunia stage** di dalam guard `built` (stage 2 via `buildStage2World`, stage 3 & 4 via `ensureWorld()` masing-masing — **pre-build 2026-07-16** supaya loading antar-stage konsisten; warmupAll startGame ikut meng-compile semuanya) + tempatkan SEMUA (stage2 robots, stage1 robots, supplies) + preset indoor + posisi start. Trigger `S1_EXIT` di `playerCollide` → **`beginStageTransition(stage2Scene)`** (→ SHOP SCENE `campaignShopScene`: loading→shop→loading→stage berikut, `campaign/transition.js`) — bukan lagi `setScene` langsung.
- **campaign/stage2.js** — `stage2Scene` (id `campaign-2`) + `S2, s2grid, s2Cell, S2_START, S2_END, s2Wall, stage2Walk, s2LOS, s2SegHitsWall, resolve, s2Nav, buildWorld, placeRobots`. **DIROMBAK TOTAL 2026-07-13** dari jalan-raya menjadi **gedung indoor kedua** (pola IDENTIK stage1.js: grid sel 2 m, dinding/collision/LOS/hit-peluru dari grid yang sama) mengikuti floor-plan referensi user — grid **40×28** di `x0≈59720` (jauh dari gedung stage1 x≈30000): ATAS = START(tangga turun Lt.3) / OFFICE / HALLWAY(koridor+runtuhan) / STORAGE(arsip); TENGAH = WAITING / OPEN OFFICE / SUPPLY(ammo+medkit) + koridor kanan percabangan; BAWAH = LAB/RESEARCH / CONTROL / END(tangga turun ke stage 3). `buildS2Grid` (carve ruangan+koridor+pintu; **konektivitas BFS-verified** di scratchpad & di smoke test — jangan ubah pintu tanpa tes ulang), lantai/plafon(disembunyikan)/dinding(InstancedMesh)/furnitur(InstancedMesh+blocker, dijauhkan dari pintu)/tangga+portal/lampu per-ruang, `s2Nav` di-bake di akhir. `placeRobots` = 9 spot denah (35 robot kelas C + sebagian penembak B/A) **+ `placeSupplies()`** (SUPPLY room ammo/medkit, tak kedaluwarsa). **TANPA boss** (dibuang atas permintaan user 2026-07-13): menang = capai tangga END → `playerCollide` (slideWalk grid + resolve furnitur) mendeteksi trigger `S2_EXIT` → **`beginStageTransition(stage3Scene)`** (→ SHOP SCENE: loading→shop→loading) — SELALU aktif, persis seperti tangga keluar stage 1 (tak ada `updateMode`/checkWin). Papan/lampu EXIT hijau tetap. `bulletBlocked`=`s2SegHitsWall`, `robotAI`=`campaignRobotAI` dgn `s2LOS`+`s2Nav`, `groundHeight`=blockersGroundHeight. `enter()` = bersihkan robot stage 1 + preset **indoor** + teleport ke START.
- **campaign/stage3.js** — `stage3Scene` (id `campaign-3`, FINAL) + `S3, s3grid, s3Cell, S3_START, S3_END, s3Wall, stage3Walk, s3LOS, s3SegHitsWall, resolve, s3Nav, buildWorld, ensureWorld, worldBuilt, placeRobots`. **DIROMBAK TOTAL 2026-07-13** dari Taman Monas malam menjadi **gedung indoor KETIGA** (pola IDENTIK stage1/stage2) mengikuti denah dgn **ATRIUM/VOID pusat** — grid **42×30** di `x0≈89706`: ring koridor mengelilingi atrium (VOID `S3.VOID` = lubang berpagar di tengah = **dinding grid** [`inVoid` di-skip dari wall-mesh, dirender **pit hitam + pagar rendah 4 sisi**]), ruangan sekeliling: ATAS START(tangga)/ARCHIVE/koridor-sempit/OFFICE, TENGAH READING/ATRIUM/SUPPLY, BAWAH CAFETERIA/ELECTRICAL/STORAGE/END(tangga). `buildS3Grid` (**konektivitas BFS-verified** scratchpad+smoke; VOID di-wall-kan setelah carve atrium), lantai/plafon(hidden)/dinding(InstancedMesh kecuali VOID)/furnitur/tangga/lampu per-ruang, `s3Nav` di akhir. Build lewat **`ensureWorld()`/`worldBuilt()`** (export, guard `built`) — dipanggil `stage1.enter` (pre-build 2026-07-16) dan `enter()` sendiri (jaring pengaman). `placeRobots` = 10 spot denah (39 robot kelas C + sebagian penembak B/A) + `placeSupplies` (SUPPLY room). **Capai tangga END** → `playerCollide` mendeteksi `S3_EXIT` → **`beginStageTransition(stage4Scene)`** (→ SHOP SCENE: loading→shop→loading; turun ke jalan; BUKAN lagi final sejak stage 4 ditambah 2026-07-13); TANPA boss, TANPA checkWin. Hook `bulletBlocked`=`s3SegHitsWall`, `robotAI`=`campaignRobotAI` dgn `s3LOS`+`s3Nav`, preset **indoor**. `restartScene` → stage1.
- **campaign/stage4.js** — `stage4Scene` (id `campaign-4`, FINAL) + `S4_START, S4_END, S4_GATE, stage4Walk, resolve, buildWorld, ensureWorld, worldBuilt, placeRobots` (build lewat `ensureWorld()` ber-guard — dipanggil `stage1.enter` [pre-build 2026-07-16] dan `enter()` sendiri). **BARU 2026-07-13; LAYOUT DIROMBAK TOTAL 2026-07-17 dari denah user: STASIUN DIHAPUS → ALUN-ALUN** — level **LUAR-RUANGAN** (bukan grid) "Jalan Menuju Alun-Alun" di `x≈120000`: area boleh-jalan = **UNION 3 persegi** (`stage4Walk`): PARKIRAN kecil (barat, utara jalan — diperkecil lagi 100×43 m → **60×30 m**; start = pintu keluar gedung) → JALAN RAYA **2 LAJUR** (barat→timur ~500 m, hz 32, marka tengah putus-putus, tanpa median; cover rongsokan slalom, blocker mobil MENGIKUTI yaw via `carHalf`) → **KOMPLEKS ALUN-ALUN `SQ` (~86×86 m) di ujung timur: lapangan `ALUN` di tengah DIKELILINGI jalan RING 2 lajur (`RING_W` 64 = lebar jalan raya; seluruh SQ walkable — ring vs lapangan murni visual: 4 strip aspal bermarka [strip vertikal = Group ber-yaw 90°] + rumput + jalur paving menyilang)**. **GERBANG (`S4_GATE`, mulut ring): panel retak `roadGate` + `gateBlocker` PEJAL (via `resolve`, BUKAN union — nav-grid/flood-fill tetap tembus; blocker ter-bake di nav tapi robot tak pernah perlu menyeberang) menutup masuk alun-alun SELAMA masih ada robot** (mendekat = hint); **ROBOT (13 spot, 44 ekor, C + sebagian B/A) SEMUANYA di BARAT gerbang — alun-alun STERIL**. `buildWorld` = tanah/aspal/beton + tembok keliling VISUAL (clamp = union; keliling SQ penuh kecuali bukaan mulut ring) + cover (mkCar/mkBus/mkContainer/mkBarrier → blocker) + **prop** (`mkPropCover` krat `Crate`/puing `Rubble` = blocker; `mkPropDecor` bangku/planter 4 sudut lapangan/kios `Stall` di ring utara = dekor TANPA blocker — arena duel tank lapang) + lampu (1 di pusat alun) + nav-grid. **ALUR MENANG**: `countStageRobots(4)===0` → `spawnBoss` = **`openGate()`** (mesh hilang + blocker di-splice; dipulihkan `enter()`) + **BOSS TANK spawn DI PUSAT alun-alun (`BOSS_POS = S4_END`)** menggelinding dari sisi timur lapangan (`wallX` ditaruh JAUH DI BARAT home → fase smash-dinding tank TIDAK pernah terpicu; `bossGate`/`onWallSmash` DIHAPUS); tiap frame `updateTank(tank, dt)`; `tank.dead` → `onBossDown` → **`gameOver(true)` setelah `WIN_DELAY_SEC` 2.5 dtk (MISSION COMPLETE langsung — TANPA trigger finish/`S4_EXIT`)**. Export **`currentTank()`** (ref tank utk smoke). `hudStatus` bar HP **TANK**. `enter()` dispose tank lama + pasang lagi gerbang (mesh + blocker) + reset winT/winFired. `bulletBlocked` false (outdoor), `robotAI`=`campaignRobotAI` (aktivasi jarak, tanpa LOS) + `navGrid`, preset **night**; landmark radar = pusat alun-alun (hijau saat gerbang terbuka). `restartScene` → stage1.

## config/gameplay.json — kunci tuning

| Seksi | Kunci |
|---|---|
| `player` | maxHp, speed, radius, eyeHeight, gravity (jatuh dari tepian; lompat dihapus), **medkitHealPct** (0.7 = +70% HP), **maxMedkits** (2), **medkitUseSec** (2 = detik tahan klik utk pakai medkit), **medkitSlowMul** (0.75 = gerak −25% saat memakai medkit), **deathDelaySec** (jeda sekuens kematian player sebelum layar GAME OVER, 2026-07-12), **hpUpgrades** (tangga MAX HP item Vitality — 3 tingkat [150, 200, 250], 2026-07-15) |
| `armor` | **tiers[]** (2026-07-13: `{reduce, durability}` per tier armor shop — 0.3/100, 0.5/200, 0.7/300; `reduce` = potongan damage ke HP, durability menerima damage BASE penuh, 0 = hancur) |
| `stamina` | max, adsDrainPerSec, meleeCost, regenPerSec, recoverThreshold (sprintDrainPerSec DIHAPUS 2026-07-11; adsDrainPerSec dorman) |
| `dodge` | staminaCost (20), cooldownSec (1.0), durationSec (0.45), speed (2.2 — diturunkan dari 4.2 2026-07-12: salto pendek ~4 m) — dodge/evade Shift (2026-07-11) |
| `movement` | adsMultiplier (dorman), **backpedalWeight (0.5) & strafeWeight (0.5) — HIDUP LAGI 2026-07-12: pengali kecepatan mundur/menyamping relatif KURSOR (dirSpeedMul di player.js; maju searah kursor = penuh)**, walkSpreadPenalty (sprintMultiplier & sprintSpreadPenalty DIHAPUS 2026-07-11) |
| `weapons` | **maxWeapons** (3 = slot senjata maks sejak 2026-07-11), **upgradeDamagePct** (0.25 = +25% damage base per level upgrade shop, 2026-07-12), **maxWeaponLevel** (3 = plafon level upgrade), **ammoUpgrades** (kap peluru per senjata utk tier Ammo Capacity I/II/III — 3 tingkat, tiap tier +20% kap dasar kecuali launcher +15%; dibaca `maxAmmoFor`, 2026-07-15), bulletSpeed, bulletLife, bulletDamage (fallback), spreadBase, spreadBloom, heatPerShot, heatCoolPerSec, adsAccuracy (dorman — ADS dihapus; crouchAccuracy DIBUANG 2026-07-11); per senjata (`rifle`/`pistol`/`shotgun`/**`launcher`**): **maxAmmo** (kap kolam peluru — rifle 500 / pistol 150 / shotgun 300 / **launcher 25**), **ammoPickup** (isi ulang per drop peluru — 80/30/50/**8**), fireDelayMs, cameraKick (recoil visual prop avatar), **damage** (dibawa peluru, DIKALI level upgrade via `weaponDamage()`; rifle 40/pistol 25/shotgun 10-per-pelet/**launcher 100 AoE**) (+ shotgun: pellets, pelletSpread; + **launcher: roundSpeed** — peluru MELEDAK saat kena, radius = `grenade.killRadius`+3.5) |
| `melee` | range, cooldownSec, **damage** (2026-07-13: damage SAPUAN pedang per robot — 150 = semua kelas biasa mati sekali tebas [C 60/B 90/A 120], boss 1800 hanya tergerus; dulu instant-kill 1 target) |
| `grenade` | **killRadius + damage (100) = LIVE** (dipakai `explodeAt` + peluru Grenade Launcher); max/start/fuseSec/throwSpeed/throwUpward/throwNearSpeed/throwNearUpward/pushSpeed = DORMAN (granat lempar dihapus 2026-07-11) |
| `robot` | bodyHitRadius (5, radius hit badan — hit-test HORIZONTAL/2D, tanpa headshot), bodyBlockRadius (7.5, dorong-keluar fisik), clawCooldownSec (1), **clawWindupSec** (0.5 — jeda ANCANG-ANCANG sebelum damage cakar mendarat, 2026-07-13; cooldown dihitung dari inisiasi jadi irama serangan tetap), **clawStrikeGrace** (2.0 — kelonggaran jangkauan saat sabetan mendarat [lunge], ditambahkan ke clawRange pada recheck), clawRange, stopRange, **separationRadius** (4.0 — radius separasi robot-robot ×skala, `separateRobots`; ≤ ½ lebar-guna sel grid supaya 2 robot muat 1 lorong; dulu 5.5, diturunkan 2026-07-16), **separationRelax** (0.5 — faktor relaksasi dorong separasi), repathSec (rate-limit findPath), stuckSec (macet → repath paksa), turnRadPerSec (laju putar heading), **rangedBulletLife** (120, umur peluru robot; `rangedRange` global DIHAPUS — range kini per kelas), score.{normalKill 100, specialKill 150} (special = kelas ranged A/B), **classes.{C,B,A}.{hp,speed,armor,attack,ranged,scale}** (+ranged **rangeMeters**,fireDelaySec,bulletSpeed) — C: 60/0.55/melee/atk5/HIJAU; B: 90/0.75/RANGED/atk5/**range 10 m**/delay1.0/bspd7/KUNING; A: 120/0.95/RANGED/atk10/**range 12 m**/delay1.5/bspd5/MERAH (menggantikan variants runner/brute/exploder yg DIHAPUS 2026-07-12) |
| `survival` | robotsPerWaveBase/Step (jatah robot per wave), shopCountdownSec (jeda "Wave Cleared"→shop), spawnIntervalBase/Step/Min, maxRobotsBase/Step/Cap (cap robot di lapangan), **robotSpeedWaveMin/Step/Max** (faktor ramp kecepatan per-wave dikali speed KELAS: wave 1 = Min [0.72], +Step [0.02] tiap wave, dijepit Max [1.0] → penuh ~wave 15; HP/speed dasar kini dari `CFG.robot.classes`, bukan lagi robotHp*/robotSpeed* yg DIHAPUS), **classBFromWave/classBChance/classBChancePerWave/classBChanceMax** (peluang spawn kelas B NAIK per wave sejak FromWave 3, dijepit Max), **classAFromWave/classAChance/classAChancePerWave/classAChanceMax** (kelas A sejak wave 6), monasMaxHp, **strengthenMonasStages** (tangga max-HP Monas item shop Strengthen — mis. [3000,4500,6000], tier terakhir = plafon), playerAggroMeters (radius aggro player, m; robot serang Monas di luar radius ini, kejar player di dalamnya), **monasLockAggroMeters** (radius aggro yang menyusut setelah robot mulai memukul Monas — mis. 5 m; jauh lebih sulit dialihkan), **monasLockChance** (peluang robot yang sudah memukul Monas TERKUNCI penuh = radius 0, tak pernah mengejar player — mis. 0.5), monasClawDamage, **monasFogFromWave** (wave pertama eligible event kabut = "setelah wave 3", mis. 4), **monasFogChanceBase** (peluang kabut di wave eligible pertama, mis. 0.20), **monasFogChancePerWave** (kenaikan peluang tiap wave, mis. 0.03), **monasFogChanceMax** (plafon peluang, mis. 0.75), **monasFogClearMeters** (radius zona bersih di sekitar Monas saat kabut, mis. 15 m), **monasFogDurationSec** (mis. 60), **monasCollapseTrembleSec/monasCollapseToppleSec/monasCollapseSettleSec** (durasi 3 fase animasi runtuhnya Monas, mis. 1.4/2.2/1.6) |
| `campaign` | robotSpeedScale (kali speed kelas), activateMeters, cullDistance, stage3Robots (grunt kelas C default; stage 3 campur ~60/30/10 C/B/A), **bosses.{...}** (2026-07-16 — SEMUA boss dipisah di bawah `campaign.bosses` karena akan ada beberapa boss; tiap boss self-contained). **bosses.giant.{hp,speed,scale,clawDamage,reachMul,spawnWhenRemaining,score,grenadeDamage}** (robot raksasa lama — kini DORMAN [tak ada scene men-spawn 'boss'; kind==='boss' membacanya di config.js/effects.js/common.js/robots.js]). **bosses.tank.{hp, score, gapSec, aimSec, spawnRollSec, hitRadius, cannonDamage, cannonBlastRatio (× killRadius granat = radius blast), cannonShellSpeed, mgDamage, mgBurst, mgIntervalSec, mgBulletSpeed, mortarDamage, mortarBlastRatio, mortarBurst (3 lob/serangan), mortarBurstGapSec (0.5 dtk jeda antar lob), mortarSpeed (DORMAN sejak lob ber-apex 2026-07-16), mortarGravity (lob parabola; mortarTurnRadPerSec dorman), mortarMaxSec (umur shell + sumber riseCap lob), mortarApexMeters (tinggi minimum puncak lob, meter — 2026-07-16), mortarApexRatio (puncak ikut tumbuh dgn jarak — 2026-07-16), shockRadiusMeters (radius PAGAR LISTRIK anti-didekati — 2026-07-16), shockDps (HP/detik setruman, MENEMBUS armor — 2026-07-16)}** (BOSS TANK stage 4, entities/tank.js; kini punya `hp`/`score` SENDIRI, tak lagi meminjam giant) |
| `drops` | magChance, grenadeChance (dorman — drop granat dihapus 2026-07-11), lifetimeSec |
| `shop` | ammoCost, **healthCost** (Replenish Health instan 100%), **medkitCost** (item Medkit genggam), healMonasCost, strengthenMonasCost, radarCost (harga item Radar; Survival mulai tanpa radar), shotgunCost, rifleCost, **launcherCost** (harga Grenade Launcher), **upgradeCosts.{pistol,shotgun,rifle,launcher}** (2026-07-12: `[hargaLv2, hargaLv3]` per senjata utk item Upgrade), **armorCosts** ([tier I, II, III]), **healthUpCosts** ([Vitality II, III]), **ammoUpCosts** ([Ammo Capacity II, III]) (2026-07-13) |
| `difficulty` | easy/normal/hard.{robotHpMul, robotDamageMul, spawnIntervalMul} — diterapkan `applyDifficulty()` dari CFG_BASE |

Catatan: nilai visual murni (FOV, amplitudo animasi, warna) sengaja BUKAN di JSON. (Sistem magazen + reload dihapus 2026-07-11 — rig animasi reload di weapons.js dorman.)

## Menjalankan & menguji

- **Wajib HTTP server** (ES modules + fetch config tidak jalan di `file://`): `python -m http.server 8000` → `http://localhost:8000`. `index.html` menampilkan pesan error yang jelas bila dibuka via file://.
- `package.json` hanya metadata `{"type":"module"}` untuk tooling Node (`node --check src/**/*.js`) — tetap TANPA dependensi/build.
- **Test headless = [`tools/smoke.mjs`](tools/smoke.mjs) (masuk repo 2026-07-13; `node tools/smoke.mjs`, dari folder mana pun, nol dependensi)** — stub THREE/DOM/Audio menjalankan MODUL ASLI `src/` (**`localStorage` = Map in-memory nyata** supaya save/checkpoint teruji); saat ini **219 assert** (a.l. pre-build dunia campaign: `ensureWorld` idempoten + stage 3 & 4 `worldBuilt()` sejak enter stage 1 — loading antar-stage konsisten): buildRobotMesh per kelas, gerbang tembak stop-and-shoot B/A + LOS, peluru musuh→player, **cakar C ber-WINDUP (tidak instan; damage menyusul; mundur = luput)**, **IDLE per kelas (badan & kaki diam; kepala celingak-celinguk; C angkat-turun tangan / B gosok senapan / A juggle senapan naik & kembali; sisa pindaian meluruh saat aktif)**, killRobot gore (ledakan + **terbelah-dua pedang**: kepala/lengan pindah ke paruh atas, kaki tinggal), peluru monasDmg + hook, dua-laras A bergantian, stance membidik, **sapuan melee busur (bunuh semua di depan / belakang selamat / HP raksasa tergerus)**, prop avatar + kalibrasi gunTip + salto dodge, upgrade senjata & `weaponDamage`, `explodeAt(dmg)`, umpan-balik god-mode, invarian rantai-hadap avatar, sekuens kematian + selebrasi robot, matematika armor/pecah/`maxAmmoFor`, kartu armor terpisah + Vitality + Ammo Capacity di shop, **kontrak API + durasi fase config-driven runtuhnya Monas**, **campaign STAGE 2 (gedung indoor: build dunia + BFS konektivitas grid + 35 robot 9-spot + supply + robotAI + tangga END → SHOP SCENE terpisah [loading → shop, Monas difilter, Radar/Shotgun/Rifle/Launcher DIJUAL] → Start Next Stage = loading → stage 3)**, **campaign STAGE 3 (gedung indoor final: build + BFS grid dgn ATRIUM/VOID + 39 robot 10-spot + supply + robotAI + tangga END → SHOP SCENE → Start Next Stage ke stage 4)**, **campaign STAGE 4 (outdoor, layout ALUN-ALUN 2026-07-17: union walkable parkiran->jalan->alun-alun [END = pusat alun] + semua robot & supply walkable DAN di barat gerbang [alun steril] + 44 robot 13-spot + robotAI + GERBANG tertutup selagi robot hidup [playerCollide terdorong] lalu TERBUKA saat semua mati + alur bunuh-semua->BOSS TANK spawn di PUSAT alun (home = S4_END) [entitas mandiri, bukan `robots`; HP=`bosses.tank.hp`; spawn+3 serangan jalan tanpa error; **PAGAR LISTRIK: dekat tank = shockDps HP/dtk MENEMBUS armor (durability utuh), god-mode kebal, di luar radius aman**; **mortar = LOB BER-APEX config-driven (vy0 selalu naik, puncak = formula apexMeters/apexRatio/riseCap, mendarat <10 u dari posisi player saat tembakan; injeksi parabola lama tetap hijau)**; belum menang selagi hidup; HP 0 -> hancur -> jeda win -> MISSION COMPLETE tanpa trigger])**, **cheat skip-to-stage-N (lompat langsung ke stage 2/3/4 via `cheatSkipToStage`→`campaignJumpToStage`; invalid ditolak; survival tanpa hook)**, **SAVE GAME / checkpoint campaign (`saveGame.js`: tulis/baca/hapus round-trip; nilai di luar 1..4 = 0; enter() stage menulis checkpoint; `gameOver(won)` menghapus; RESTART STAGE game-over = campaignJumpToStage(loadCampaignStage) mendarat di stage checkpoint, bukan 1)**, **IDLE AFK bertahap (diam 30/60/90 dtk -> wave/crouch/lie via `afkDebug()`; gerak & musuh mengejar mereset)**, **model furnitur futuristik (8 builder drop-in `buildFuturistic{Crate,Sofa,Console,Bench,Rubble,Planter,Stall,Sink}Mesh`: tiap-nya menghasilkan Group ter-skala berdiri di y>=0 tanpa NaN; footprint blocker tak berubah → build stage 1-4 + BFS/nav/robot-count/konektivitas-S4 tetap hijau)**, **panduan gaya "GIBS 2045" (token PAL lengkap; sweep material SEMUA prop futuristik + Desk/Chair/Cupboard/MeetingTable/Sedan/SUV: tanpa neon terlarang cyan/magenta, emissive lingkungan <= `EMISSIVE_MAX` — stub `Mat` kini menyimpan `emissiveIntensity`)**, **rombak mobil low-poly (Sedan & SUV: >=4 roda silinder tegak menapak + builder tanpa-lift [inner y=0])**, **penjaga ringan: SEMUA prop & kendaraan <= 25 mesh (rombak prop 2026-07-16)**, **peluru launcher per level (Lv1 granat Mk2 / Lv3 roket `userData.rocket`, keduanya eksplosif + damage ber-level)**. **ATURAN: assertion HARUS config-driven** (baca `cfgMod.CFG`, jangan hardcode angka tuning — user sering me-retune gameplay.json; test gagal setelah retune murni config ≈ test-nya yang hardcode). Method stub yang kurang (fakeEl/THREE) = celah harness, bukan bug game — lengkapi stub-nya. **Alur kerja tiap perubahan gameplay: tambah/ubah assert → `node tools/smoke.mjs` (semua pass) → `node --check` file yang disentuh → sinkronkan CLAUDE.md + MODULES.md.**
